"""synthex_brand_intelligence — brand intelligence pipeline package.

This __init__.py exists so hatchling can locate the package during
``pip install -e .`` (flat-layout projects require a directory matching
the project name, synthex_brand_intelligence, to be present).

The actual entry point is synthex_orchestrator.py in the parent directory.
"""
