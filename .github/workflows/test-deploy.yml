name: Test Deploy Webhook

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Test Webhook
        run: |
          echo "Testing Vercel webhook..."
          curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_QcJrayyUbPteT2ez5JKpImPLgCWZ/wyQpfX55Zx" \
            -H "Content-Type: application/json" \
            -w "\nHTTP Status: %{http_code}\n"
          echo "Webhook call completed"