---
phase: 22-analytics-dashboard-v2
plan: 02
type: execute
model: sonnet
model_rationale: "Plan 02 (35% context) builds drill-down UI and populates metric table tabs using established Radix Sheet and table patterns. Complexity score: ~2.0. Routing: Sonnet"
---

<objective>
Build post detail drill-down and populate the remaining MetricsTable tabs with real data.

Purpose: The analytics page has a stubbed drill-down (`alert()` on post click) and MetricsTable only shows the Overview tab. This plan completes the interactive experience.
Output: Clicking a top post opens a detail Sheet with engagement breakdown. MetricsTable shows Engagement, Audience, and Content data in their respective tabs.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-analytics-dashboard-v2/22-01-SUMMARY.md

# Key source files (will be modified by Plan 01 first):
@app/dashboard/analytics/page.tsx
@components/analytics/top-posts.tsx
@components/analytics/metrics-table.tsx
@components/analytics/types.ts
@components/ui/sheet.tsx

**Established patterns:**
- Radix Sheet component exists at `components/ui/sheet.tsx` (Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription)
- Glassmorphic cards: `variant="glass"`, bg-white/5, border-white/10
- Platform icons pattern: `platformIcons` map in top-posts.tsx
- Number formatting: `formatNumber()` in analytics-stats.tsx

**Performance API topContent shape:**
```typescript
topContent: Array<{
  id: string;
  content: string;
  platform: string;
  engagement: number;
  engagementRate: number;
  publishedAt: Date;
}>
```

**Performance API platforms shape:**
```typescript
platforms: Array<{
  platform: string;
  engagement: number;
  engagementRate: number;
  posts: number;
  bestTime: string;
}>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PostDetailSheet for post drill-down</name>
  <files>components/analytics/post-detail-sheet.tsx, components/analytics/index.ts, app/dashboard/analytics/page.tsx</files>
  <action>
1. Create `components/analytics/post-detail-sheet.tsx`:
   - Import Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription from `@/components/ui/sheet`
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `post: TopPost | null` (use extended type with engagement breakdown)
   - Layout inside SheetContent (side="right", className="w-[400px] sm:w-[480px] bg-[#0a0f1a] border-white/10"):
     - Platform badge (icon + name) at top
     - Content preview (full text, not truncated)
     - Published date formatted with date-fns `format()`
     - Engagement breakdown section: grid of 4 metric cards showing Likes, Comments, Shares, Impressions with icons
     - Engagement Rate displayed as large percentage
     - "View Full Post" button linking to `/dashboard/content`
   - Use glassmorphic styling (bg-white/5 cards, text-slate-400 labels, text-white values)
   - Handle null post gracefully (don't render if null)

2. Add `PostDetailSheet` to `components/analytics/index.ts` barrel export.

3. In `components/analytics/types.ts`:
   - Extend or add `TopPostDetail` type that includes all fields from `topContent` response: id, content, platform, engagement, engagementRate, publishedAt
   - Ensure TopPost type includes these fields (or create a mapping)

4. In `app/dashboard/analytics/page.tsx`:
   - Add state: `selectedPost` (TopPostDetail | null), `isDetailOpen` (boolean)
   - Replace `handleViewPostDetails` from `alert()` to: find post by id from `data.topContent`, set selectedPost, set isDetailOpen true
   - Render `<PostDetailSheet open={isDetailOpen} onOpenChange={setIsDetailOpen} post={selectedPost} />` at bottom of JSX

Do NOT use Sheet from `@radix-ui/react-dialog` directly — use the project's `components/ui/sheet.tsx` wrapper.
  </action>
  <verify>
   - `npm run type-check` passes
   - No alert() stubs remain in analytics page
   - PostDetailSheet component exported from index
  </verify>
  <done>
   - Clicking a top post opens a slide-out Sheet with full engagement details
   - Sheet displays: content, platform, date, engagement breakdown, engagement rate
   - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate MetricsTable Engagement, Audience, and Content tabs</name>
  <files>components/analytics/metrics-table.tsx, app/dashboard/analytics/page.tsx</files>
  <action>
1. In `components/analytics/metrics-table.tsx`:
   - Add new prop types for the additional tabs:
     - `engagementData?: Array<{ platform: string, likes: number, comments: number, shares: number, total: number }>`
     - `contentData?: Array<{ platform: string, topPosts: number, avgEngagementRate: number, bestTime: string }>`
   - Keep existing `data` prop for Overview tab

   - Implement **Engagement tab** (TabsContent value="engagement"):
     - Table columns: Platform, Likes, Comments, Shares, Total Engagement
     - Use engagementData prop
     - Show em-dash when no data

   - Implement **Content tab** (TabsContent value="content"):
     - Table columns: Platform, Posts, Avg Engagement Rate, Best Posting Time
     - Use contentData prop
     - Show em-dash when no data

   - Implement **Audience tab** (TabsContent value="audience"):
     - Table columns: Platform, Engagement Rate, Growth Trend, Best Time
     - Derive from existing data/engagementData (audience data is limited since we don't have follower counts per platform from the performance API — show engagement rate as proxy)
     - Show em-dash when no data

   - All tabs follow existing table styling: border-b border-white/5, text-white for values, text-slate-300 for secondary, text-slate-400 for headers

2. In `app/dashboard/analytics/page.tsx`:
   - Compute engagementData from `data.platforms`: for each platform, the performance API returns aggregate engagement but not broken down into likes/comments/shares. Since this breakdown isn't available from the performance API, show total engagement and engagement rate. Map platforms to engagement tab format.
   - Compute contentData from `data.platforms`: map platform, posts count, engagementRate, bestTime directly from API response.
   - Pass `data={overviewData}`, `engagementData={engagementTabData}`, `contentData={contentTabData}` to MetricsTable.
   - Compute overviewData from `data.platforms` for the Overview tab: map platform → { platform, followers: 0, posts, engagement: engagementRate, reach: engagement, growth: 0 } (followers and growth not available from performance API, show 0).
  </action>
  <verify>
   - `npm run type-check` passes
   - All 4 MetricsTable tabs render without errors
   - Engagement, Content, and Audience tabs show platform data or em-dashes
  </verify>
  <done>
   - MetricsTable shows data in all 4 tabs (Overview, Engagement, Audience, Content)
   - Data comes from performance API platforms array
   - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes without errors
- [ ] Clicking a top post opens PostDetailSheet with engagement data
- [ ] PostDetailSheet closes properly via X or overlay click
- [ ] All 4 MetricsTable tabs render with data
- [ ] No alert() stubs remain on analytics page
- [ ] No TypeScript errors introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Post drill-down works end-to-end
- MetricsTable fully populated across all tabs
- Phase 22 complete — analytics page has enhanced visualizations, drill-downs, date ranges
</success_criteria>

<output>
After completion, create `.planning/phases/22-analytics-dashboard-v2/22-02-SUMMARY.md`
</output>
