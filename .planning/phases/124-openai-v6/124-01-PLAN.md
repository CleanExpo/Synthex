---
phase: 124-openai-v6
plan: 01
type: execute
domain: backend
---

<objective>
Upgrade the openai npm package from 4.x to 6.x and fix all breaking changes.

Purpose: Keep the dependency current and eliminate technical debt before v11.0 launch. openai v6 ships improved TypeScript types, updated streaming shape, revised APIError hierarchy, and corrected client init patterns. Staying on v4 means drift from the official SDK's safety and ergonomic improvements.

Output:

- `package.json` bumped from `^4.104.0` → `^6.0.0`
- `src/services/openrouter.ts` (only file that imports from `openai`) updated for v6 API surface
- `npm run type-check` passes: 0 errors
- `npm test` passes: 0 failures
  </objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/services/openrouter.ts

**Discovery findings (2026-03-20):**

Blast radius is SMALLER than the SYN-407 warning suggested:

- `import OpenAI from 'openai'` appears in exactly ONE file: `src/services/openrouter.ts`
- All `lib/ai/providers/` code (the active production path) uses `axios` directly — zero SDK exposure
- `lib/ai/embedding-service.ts` calls the OpenAI REST API via raw `fetch()` — no SDK
- `lib/services/content-generator.ts` references `'openai'` only as a string model-prefix for OpenRouter — no SDK
- `src/` is the legacy Express server (index.ts.disabled confirms it is not in production request path) but IS compiled by TypeScript via `"include": ["**/*.ts"]` in tsconfig.json — type errors here WILL break `npm run type-check` and the Vercel build

**Current usage in `src/services/openrouter.ts`:**

```ts
import OpenAI from 'openai';
const openrouter = new OpenAI({ baseURL: '...', apiKey: key }); // constructor
const response = await client.chat.completions.create({
  // non-streaming only
  model,
  messages,
  temperature,
  max_tokens,
});
```

**Breaking changes in openai 4.x → 6.x that affect this file:**

| Change                                  | v4                              | v6                                                                                                                  | Impact on file                        |
| --------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| `max_tokens` param                      | `max_tokens`                    | Renamed `max_completion_tokens` for new models; `max_tokens` still accepted for legacy but TypeScript type may warn | Check type-check output               |
| `APIError` class                        | Re-exported from `openai/error` | Now exported from root `openai` package                                                                             | Not imported in this file — no impact |
| Streaming `on('content')`               | Available                       | Changed shape                                                                                                       | Not used in this file — no impact     |
| Constructor options                     | Same shape                      | Same shape                                                                                                          | No change needed                      |
| `chat.completions.create()` return type | `ChatCompletion`                | `ChatCompletion`                                                                                                    | Same — no change                      |

**Tests:** `tests/unit/lib/embedding-service.test.ts` mocks `fetch()` directly (no SDK). No tests import openai SDK. Zero test changes needed.

**Constraint:** This is `src/` legacy code — do NOT refactor, move, or restructure. Fix only what v6 breaks. The `src/` directory is intentionally preserved as-is.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump openai to v6 and install</name>
  <files>package.json, package-lock.json</files>
  <action>
    1. Edit `package.json` — change `"openai": "^4.104.0"` to `"openai": "^6.0.0"` in dependencies.
    2. Run `npm install` to fetch v6 and update package-lock.json.
    3. Run `npm show openai version` to confirm the installed version is 6.x.

    Do NOT change any other dependencies. This is a single-package bump only.
    Do NOT use `npm install openai@latest` as that could fetch a pre-release or unwanted version — use the exact pin `^6.0.0`.

  </action>
  <verify>node -e "const p=require('./node_modules/openai/package.json'); console.log(p.version)" — must print 6.x.x</verify>
  <done>package.json shows `"openai": "^6.0.0"`, node_modules/openai/package.json version starts with 6.</done>
</task>

<task type="auto">
  <name>Task 2: Fix breaking changes in src/services/openrouter.ts and verify</name>
  <files>src/services/openrouter.ts</files>
  <action>
    1. Read `src/services/openrouter.ts` — already read above, key usage is:
       - `new OpenAI({ baseURL, apiKey })` — constructor
       - `client.chat.completions.create({ model, messages, temperature, max_tokens })` — non-streaming
       - `response.choices[0]?.message?.content` — response access

    2. Run `npm run type-check` FIRST to see the ACTUAL TypeScript errors from v6. Report exactly which lines fail.

    3. Fix only the errors reported. Common v6 fixes:
       - If `max_tokens` is type-erroring: rename to `max_completion_tokens` in the create() call
       - If import shape changed: update the import statement
       - Do NOT change logic, do NOT add new features, do NOT touch anything outside `src/services/openrouter.ts`

    4. Run `npm run type-check` again — must report 0 errors.

    5. Run `npm test` — must report 0 failures (existing 1547 pass, 0 fail baseline from STATE.md).

    STOP: If type-check shows errors in files OTHER than `src/services/openrouter.ts`, report them before fixing — do not blindly fix unknown errors.

    Anti-pattern: Do NOT upgrade the API usage patterns (e.g., switching to streaming, changing model names, refactoring the class). Minimal change only.

  </action>
  <verify>npm run type-check (0 errors) && npm test (0 failures)</verify>
  <done>
    - `npm run type-check` exits 0 with 0 errors
    - `npm test` exits 0 with same or more passes, 0 failures
    - `src/services/openrouter.ts` still exports `OpenRouterService` and `openRouterService` singleton
    - openai 6.x is the installed version
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the upgrade</name>
  <files>package.json, package-lock.json, src/services/openrouter.ts</files>
  <action>
    Stage and commit the three affected files only:

    ```bash
    git add package.json package-lock.json src/services/openrouter.ts
    git commit -m "$(cat <<'EOF'
    feat(deps): upgrade openai SDK 4.x → 6.x (Phase 124, SYN-407)

    - Bumps openai from ^4.104.0 to ^6.0.0
    - Fixes breaking type changes in src/services/openrouter.ts
    - All other AI code (lib/ai/providers/) uses axios directly — unaffected
    - npm run type-check: 0 errors
    - npm test: 0 failures

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    Do NOT push. Do NOT `git add -A` (would stage unintended files).

  </action>
  <verify>git log --oneline -1 shows the commit with "Phase 124" in the message</verify>
  <done>Commit exists on main branch with all three files staged and no others.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `node -e "require('./node_modules/openai/package.json').version"` prints 6.x.x
- [ ] `npm run type-check` exits 0 with 0 TypeScript errors
- [ ] `npm test` exits 0 — 0 failures (same as baseline)
- [ ] `git log --oneline -1` shows Phase 124 / SYN-407 commit
- [ ] `git diff HEAD~1 --name-only` shows only: `package.json`, `package-lock.json`, `src/services/openrouter.ts` (+ possibly zero src file changes if no breaking types found)
</verification>

<success_criteria>

- openai SDK is on v6.x in package.json and node_modules
- Zero TypeScript errors introduced
- Zero test failures introduced
- Only the three expected files changed
- SYN-407 Phase 124 objective met
  </success_criteria>

<output>
After completion, create `.planning/phases/124-openai-v6/124-01-SUMMARY.md`:

---

phase: 124-openai-v6
plan: 01
subsystem: dependencies
provides: [openai-v6]
affects: []
tags: [upgrade, openai, sdk]
key-decisions:

- Minimal-change migration — only fixed errors reported by type-check
  key-files:
- package.json
- src/services/openrouter.ts

---

# Phase 124 Plan 01: openai 4.x → 6.x Summary

**[Substantive one-liner here after execution]**

## Accomplishments

- [List what actually changed]

## Files Created/Modified

- `package.json` — openai bumped to ^6.0.0
- `package-lock.json` — regenerated
- `src/services/openrouter.ts` — [describe any fixes made, or "no changes needed — no breaking types found"]

## Decisions Made

[Any non-obvious choices during migration]

## Issues Encountered

[Errors found and how fixed, or "None"]

## Next Step

Phase 124 complete. Ready for Phase 125 (React 18 → 19 + Next.js 15 → 16).
Note: Phase 125 has MUCH larger blast radius — requires dedicated planning session before execution.
</output>
