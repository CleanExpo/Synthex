---
phase: 24-custom-reports
plan: 02
type: execute
domain: frontend
---

<objective>
Create the report builder page route and integrate into dashboard navigation.

Purpose: Make the report builder accessible to users via a dedicated page, sidebar navigation, command palette, and CTA from the existing reports page.
Output: Working /dashboard/reports/builder page with full navigation integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-custom-reports/24-01-SUMMARY.md

# Key files to modify:
@app/dashboard/layout.tsx
@components/CommandPalette.tsx
@app/dashboard/reports/page.tsx
@components/reports/index.ts

# Pattern references:
@app/dashboard/predictions/page.tsx
@app/dashboard/predictions/loading.tsx
@app/dashboard/predictions/error.tsx
@app/dashboard/analytics/page.tsx

**Established patterns:**
- Dashboard pages: 'use client', import component, render in div with p-6
- Loading states: glassmorphic skeleton with animate-pulse
- Error states: DashboardError component from components/dashboard/error-fallback.tsx
- Sidebar nav: { icon, label, href } array in layout.tsx
- Command palette: { id, title, description, icon, action, category, keywords } in CommandPalette.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create report builder page route</name>
  <files>app/dashboard/reports/builder/page.tsx, app/dashboard/reports/builder/loading.tsx, app/dashboard/reports/builder/error.tsx</files>
  <action>
Create three files for the builder route:

**page.tsx:**
- 'use client' directive
- Import { ReportBuilder } from '@/components/reports'
- Render ReportBuilder inside a div with className="p-6"
- Follow the pattern from app/dashboard/predictions/page.tsx (simple wrapper that delegates to component)

**loading.tsx:**
- Glassmorphic skeleton loading state
- Show skeleton header (h-8 w-64), sidebar skeleton (h-96 w-full), and main content area skeleton (h-96 w-full)
- Use animate-pulse, bg-white/5, rounded-lg pattern
- Follow pattern from app/dashboard/predictions/loading.tsx

**error.tsx:**
- 'use client' directive
- Import DashboardError from '@/components/dashboard/error-fallback'
- Standard error boundary following app/dashboard/predictions/error.tsx pattern
  </action>
  <verify>Files exist at correct paths. npm run type-check passes.</verify>
  <done>Builder page renders ReportBuilder component at /dashboard/reports/builder, with loading and error states</done>
</task>

<task type="auto">
  <name>Task 2: Add navigation to sidebar, command palette, and reports page</name>
  <files>app/dashboard/layout.tsx, components/CommandPalette.tsx, app/dashboard/reports/page.tsx</files>
  <action>
**Sidebar (layout.tsx):**
- Add a new nav item after the existing Reports entry: { icon: Layout, label: 'Report Builder', href: '/dashboard/reports/builder' }
- Import Layout from '@/components/icons' if not already imported
- Place it right after the Reports nav item to group them visually

**Command Palette (CommandPalette.tsx):**
- Add a new command after the existing 'reports' command:
  { id: 'report-builder', title: 'Report Builder', description: 'Create custom report templates with drag-drop widgets', icon: Layout, action: () => router.push('/dashboard/reports/builder'), category: 'navigation', keywords: ['report', 'builder', 'custom', 'template', 'widget', 'drag', 'drop'] }
- Import Layout icon if not already imported

**Reports page CTA (page.tsx):**
- In the ReportsHeader area, add a "Build Custom Report" button that links to /dashboard/reports/builder
- Use Next.js Link component or router.push
- Button style: variant="outline" with gradient-primary text or bg-cyan-500/10 border-cyan-500/20 text-cyan-400 to make it stand out
- Place it next to the existing "Generate Report" button
- Import Link from 'next/link' and Layout icon
  </action>
  <verify>npm run type-check passes. Grep for '/dashboard/reports/builder' in layout.tsx, CommandPalette.tsx, and reports page.tsx to confirm all three are present.</verify>
  <done>Report Builder accessible from sidebar nav, command palette search, and reports page CTA button</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete custom report builder with drag-drop widgets, wired to real APIs, accessible from sidebar, command palette, and reports page</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/dashboard/reports
    3. Confirm: "Build Custom Report" button is visible next to "Generate Report"
    4. Click "Build Custom Report" — should navigate to /dashboard/reports/builder
    5. Verify: Report Builder page loads with sidebar (templates, details, export) and main canvas area
    6. Test: Click "Add Widget" — modal should open with widget type, title, data source, size options
    7. Test: Add 2-3 widgets, verify they appear in the grid canvas
    8. Test: Drag a widget to reorder — should work smoothly
    9. Test: Click "Save Report" with a name — should save to API (check network tab for POST to /api/reports/templates)
    10. Verify: Sidebar nav shows "Report Builder" link
    11. Verify: Command palette (Cmd+K) shows "Report Builder" when typing "builder"
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run type-check` succeeds without errors
- [ ] Builder page loads at /dashboard/reports/builder
- [ ] Sidebar shows Report Builder nav item
- [ ] Command palette includes Report Builder command
- [ ] Reports page has "Build Custom Report" CTA
- [ ] Drag-drop widget reordering works
- [ ] Save and export call real APIs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Report builder fully accessible and functional
- Phase 24 complete
</success_criteria>

<output>
After completion, create `.planning/phases/24-custom-reports/24-02-SUMMARY.md`
</output>
