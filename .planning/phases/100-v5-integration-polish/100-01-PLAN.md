# Plan 100-01: Verification + Fixes

**Phase:** 100 — v5.0 Integration Testing & Polish
**Milestone:** v5.0 AI-Native GEO & Citation Engine
**Date:** 2026-03-11

## Objective

Run verification passes across the entire v5.0 codebase (Phases 85-99) to ensure TypeScript
compliance, lint cleanliness, complete navigation coverage, API route safety, and adequate
UI states for all 15 new dashboard pages.

## Risk

MEDIUM — Fixes may touch multiple files. No database migrations required.

## Tasks

### Task 1: TypeScript type-check

```bash
npm run type-check 2>&1
```

Fix ALL errors before proceeding. Zero errors is the required outcome.

### Task 2: ESLint audit of v5.0 files

```bash
npm run lint 2>&1 | head -100
```

Fix any errors in Phases 85-99 files.

### Task 3: Navigation audit

Check `app/dashboard/layout.tsx` for all v5.0 sidebar entries:
- /dashboard/geo
- /dashboard/voice
- /dashboard/quality
- /dashboard/eeat
- /dashboard/brand
- /dashboard/pr
- /dashboard/awards
- /dashboard/backlinks
- /dashboard/prompts
- /dashboard/sentinel
- /dashboard/experiments
- /dashboard/citation

Add missing entries to appropriate sidebar group.

### Task 4: Command palette audit

Check `components/command-palette/commands.ts` for v5.0 commands.
Add missing commands for any phase pages not represented.

### Task 5: API route spot check

Verify auth + rate limiting + validation on representative routes:
- `app/api/geo/optimise/route.ts`
- `app/api/quality/analyse/route.ts`
- `app/api/backlinks/route.ts`
- `app/api/citation/overview/route.ts`

### Task 6: UI states quick pass

Check 3-4 dashboard pages for loading / empty / error states.
Fix any completely missing states.

## Success Criteria

- [ ] `npm run type-check` exits 0
- [ ] `npm run lint` exits 0 (or 0 errors, warnings acceptable)
- [ ] All 12 v5.0 pages in sidebar
- [ ] Command palette has v5.0 commands
- [ ] API routes have auth guards
- [ ] Pages have loading states

## Commits

- `fix(100-01): type-check and lint fixes for v5.0 codebase`
- `fix(100-01): navigation + command palette audit fixes`
- `fix(100-01): missing loading/empty states for v5.0 pages`
