---
phase: 110-bundle-query-perf
plan: 01
type: execute
---

<objective>
Run bundle analysis, fix barrel export bloat, add missing dynamic imports for heavy pages, and audit Prisma query performance across v5.0/v6.0 API routes.

Purpose: Reduce client bundle size and ensure query efficiency before Core Web Vitals phase.
Output: Measurable bundle size reduction, barrel import fixes, query audit report.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Discovery findings (Phase 110 pre-planning):**
- Bundle analyzer already configured (`npm run analyze`, @next/bundle-analyzer)
- `optimizePackageImports` covers: heroicons, radix-ui, framer-motion, react-icons, date-fns, lodash, lucide-react, recharts
- 15+ dynamic imports already in place for charts/modals with `ssr: false`
- `components/ui/index.ts` barrel exports ~33 components — consumers may import from barrel vs direct
- Prisma queries generally use `select` to avoid over-fetching, but v5.0/v6.0 routes not yet audited
- Query optimizer with Redis caching exists at `lib/database/query-optimizer.ts`
- Connection pooling: max 3, 30s idle, slow query detection >1000ms
- No N+1 detection middleware

**Key files:**
@next.config.mjs — bundle config, optimizePackageImports, external packages
@components/ui/index.ts — largest barrel export
@components/dashboard/index.ts — dashboard barrel
@lib/prisma.ts — Prisma client singleton + query logging
@lib/database/query-optimizer.ts — Redis query cache
@components/LazyLoad.tsx — dynamic import utility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bundle analysis and fix top issues</name>
  <files>
    next.config.mjs (potential tweaks),
    components/ui/index.ts (audit),
    any pages with barrel imports from components/ui or components/dashboard
  </files>
  <action>
    1. Run `ANALYZE=true npm run build` to generate bundle report (or `npm run analyze` if script exists)
    2. Capture the output — identify the top 5 largest client chunks by size
    3. For each large chunk, trace back to the importing page/component
    4. Fix identified issues:

    **Barrel export audit:**
    - Grep for `from '@/components/ui'` (barrel import) vs `from '@/components/ui/button'` (direct)
    - If barrel imports are common, convert the top offenders to direct imports
    - Same for `from '@/components/dashboard'`
    - Do NOT remove the barrel files — just fix consumer imports where it matters for bundle

    **Missing dynamic imports:**
    - Check v5.0/v6.0 pages (authority, citation, eeat, backlinks, sentinel, prompts, pr, awards, brand, quality, forecasting, optimisation) for heavy components that should be dynamically imported
    - Any component using recharts, tiptap, jspdf, or framer-motion animations that isn't already dynamic should be wrapped with `next/dynamic` + `{ ssr: false }`
    - Use the existing `LazyLoad.tsx` pattern where appropriate

    **optimizePackageImports additions:**
    - If analysis reveals other large libraries not in the list, add them to next.config.mjs

    Keep changes minimal and targeted — only fix what the analysis shows as impactful (>50KB savings).
  </action>
  <verify>
    `npm run type-check` passes.
    Re-run `ANALYZE=true npm run build` and compare total client JS size before/after.
    Document: before size, after size, what was changed.
  </verify>
  <done>Bundle analysis complete, top issues fixed, client JS size documented with before/after comparison</done>
</task>

<task type="auto">
  <name>Task 2: Audit Prisma queries in v5.0/v6.0 API routes</name>
  <files>
    Read-only audit of app/api/ routes for v5.0/v6.0 features.
    Fix files only if critical over-fetching found.
  </files>
  <action>
    Audit all API routes added in Phases 85-107 (v5.0 + v6.0) for query performance:

    **Routes to audit (by feature area):**
    - app/api/entity-coherence/ — Entity analysis
    - app/api/authority/ — Authority engine
    - app/api/geo/ — GEO optimiser
    - app/api/writing/ — Writing methodology
    - app/api/quality/ or app/api/content-quality/ — Slop detection
    - app/api/eeat/ — E-E-A-T scoring
    - app/api/brand/ — Brand builder
    - app/api/pr/ or app/api/journalists/ — PR manager
    - app/api/awards/ — Awards orchestrator
    - app/api/backlinks/ — Backlink prospector
    - app/api/prompts/ — Prompt intelligence
    - app/api/sentinel/ or app/api/algorithm/ — Algorithm sentinel
    - app/api/experiments/ — A/B testing
    - app/api/citation/ — Citation dashboard
    - app/api/forecasting/ — Prophet forecasting
    - app/api/optimisation/ — Bayesian optimisation

    **For each route, check:**
    1. Does `findMany` use `select` or `include`? (select preferred for read-only)
    2. Are results paginated (`take` + `skip` or cursor)?
    3. Are there sequential queries that could be `Promise.all()`?
    4. Are there `include` with >1 level nesting?
    5. Is there a `where` clause (never unbounded queries)?

    **Grade each route:** ✅ (optimal) | ⚠️ (could improve) | ❌ (critical: unbounded/N+1)

    **Fix any ❌ routes:**
    - Add `select` to replace broad `include`
    - Add `take` limit to unbounded `findMany`
    - Wrap sequential queries in `Promise.all()`
    - Add pagination where missing

    **For ⚠️ routes:** Document but don't fix (minor improvements).

    Produce audit table for SUMMARY.md.
  </action>
  <verify>
    `npm run type-check` passes after any fixes.
    Audit table covers all v5.0/v6.0 API route categories.
    Any ❌-graded routes fixed.
  </verify>
  <done>All v5.0/v6.0 API routes audited for query performance. Critical issues fixed. Audit table in SUMMARY.md.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes with 0 errors
- [ ] Bundle analysis results documented (before/after if changes made)
- [ ] Prisma query audit covers all v5.0/v6.0 API routes
- [ ] Any critical bundle or query issues fixed
- [ ] No functional regressions (existing tests pass)
</verification>

<success_criteria>

- Bundle analysis run and documented
- Barrel import issues identified and top offenders fixed (if impactful)
- Missing dynamic imports added for heavy v5.0/v6.0 components
- v5.0/v6.0 Prisma query audit complete with grading
- Critical query issues fixed (unbounded queries, N+1 patterns)
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/110-bundle-query-perf/110-01-SUMMARY.md`

Update `.planning/STATE.md`:
- Phase: 111 of 113 — NOT STARTED
- Progress: █████░░░░░ 50% (3/6 phases)
- Last activity: Phase 110 complete

Update `.planning/ROADMAP.md`:
- Mark Phase 110 as [x] with completion note
</output>
