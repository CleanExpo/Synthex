<objective>
Add a welcome email sequence (D+0, D+3, D+7) to reduce signup→first-value time.
Trigger D+0 from onboarding complete, schedule D+3 and D+7 via cron or stored timestamp.
</objective>

<execution_context>
@lib/email/billing-emails.ts
@lib/email/index.ts
@app/(onboarding)/onboarding/complete/page.tsx
@app/api/onboarding/complete/route.ts
</execution_context>

<context>
Email infrastructure: `lib/email/` — UnifiedEmailService (SendGrid/Resend queue), lazy Resend singleton
Billing emails pattern: fire-and-forget `.catch()`, no await in webhook path
Existing welcome email: `email.sendWelcomeEmail()` exists but is basic (called from email verification flow)
Onboarding complete: client-side POST to `/api/onboarding/complete` → saves to DB → redirects to dashboard
Goal: 3-email nurture sequence to drive first-value actions
Australian English throughout.
</context>

<tasks>
<task type="auto" name="Welcome email templates">
Add 3 welcome sequence functions to `lib/email/billing-emails.ts` (fire-and-forget, Resend):

1. `sendWelcomeSequenceDay0(email, name)` — triggers immediately on onboarding complete
   Subject: "Welcome to Synthex — let's get you your first result"
   Content: Celebrate signup → top 3 first actions (connect a platform, generate first content, connect API key) → dashboard CTA

2. `sendWelcomeSequenceDay3(email, name)` — tips email
   Subject: "3 things our best users do in their first week"
   Content: Tips: (1) AI content generation, (2) schedule a week of posts, (3) check analytics after 24h → CTA to content generator

3. `sendWelcomeSequenceDay7(email, name)` — check-in + upgrade nudge (only for free tier)
   Subject: "How's your first week with Synthex going?"
   Content: Week recap prompt → highlight what Pro unlocks → upgrade CTA

All use same Resend lazy singleton pattern as billing-emails.ts.
Follow exact same inline HTML template style (dark theme, gradient header).
</task>

<task type="auto" name="Trigger D+0 and schedule D+3/D+7">
1. Find `/api/onboarding/complete` route handler
2. After successful save, fire `sendWelcomeSequenceDay0(email, name)` — fire-and-forget (`.catch()`)
3. Store `emailSequenceStartedAt: new Date().toISOString()` in user.preferences JSON alongside existing onboarding data
4. Create `/api/cron/welcome-sequence` route:
   - Auth: verify `Authorization: Bearer ${CRON_SECRET}` header
   - Query users where `preferences->>'emailSequenceStartedAt'` is non-null
   - Send D+3 email to users where now - startedAt >= 3 days and `emailSequenceDay3Sent` not set
   - Send D+7 email to users where now - startedAt >= 7 days and `emailSequenceDay7Sent` not set (free tier only: check `subscription` null or plan='starter')
   - Update preferences to set `emailSequenceDay3Sent` / `emailSequenceDay7Sent` after send
5. Add to `vercel.json` crons: `{ "path": "/api/cron/welcome-sequence", "schedule": "0 9 * * *" }` (9am daily)
</task>
</tasks>

<verification>
- TypeScript: `npm run type-check` passes (zero new errors)
- Day0 trigger: confirm sendWelcomeSequenceDay0 called in complete route
- Cron route: manual curl with CRON_SECRET returns 200
</verification>

<success_criteria>
- 3 email functions in lib/email/billing-emails.ts, fire-and-forget
- D+0 triggered from /api/onboarding/complete
- /api/cron/welcome-sequence handles D+3 and D+7 sends with preferences gating
- vercel.json updated with cron schedule
- npm run type-check passes
</success_criteria>

<output>
Files to create/modify:
- lib/email/billing-emails.ts (add 3 functions)
- app/api/onboarding/complete/route.ts (trigger D+0, store timestamp)
- app/api/cron/welcome-sequence/route.ts (NEW — cron handler)
- vercel.json (add cron entry)
Commit: `feat(email): add welcome email sequence D+0/D+3/D+7 (SYN-52)`
</output>
