<objective>
Improve onboarding UX (explicit tour CTA on complete page) and extend ProductTour with 3 v2.0 steps
(Workflows, Insights, Billing upgrade nudge for free users).
</objective>

<execution_context>
@components/ProductTour.tsx
@app/(onboarding)/onboarding/complete/page.tsx
@app/dashboard/layout.tsx
</execution_context>

<context>
ProductTour: `components/ProductTour.tsx` — 12 steps (welcome → dashboard → content → personas → command palette → analytics → reports → experiments → schedule → seo → video → complete)
Tour trigger: localStorage `showTourOnDashboard=true` flag set at onboarding complete → detected by ProductTour useEffect
v2.0 new routes: /dashboard/workflows, /dashboard/insights (added in v2.0)
Free tier: Starter plan (no subscription or plan='starter')
Australian English throughout.
</context>

<tasks>
<task type="auto" name="Extend ProductTour with v2.0 steps">
In `components/ProductTour.tsx`, insert 3 new steps before the final 'complete' step:

1. `workflows` step:
   - id: 'workflows'
   - title: 'AI Workflow Engine'
   - content: 'Build multi-step AI workflows that research, write, and publish content automatically. Set it and forget it.'
   - target: '[href="/dashboard/workflows"]'
   - position: 'right'

2. `insights` step:
   - id: 'insights'
   - title: 'Performance Insights'
   - content: 'AI-powered insights show you exactly what to improve — content gaps, best posting times, and competitor analysis.'
   - target: '[href="/dashboard/insights"]'
   - position: 'right'

3. `upgrade` step (conditional — shown to free/starter users only based on localStorage `userPlan` key):
   - id: 'upgrade'
   - title: 'Unlock the Full Platform'
   - content: 'Upgrade to Pro to remove limits and access unlimited AI generations, workflows, and all integrations.'
   - target: '[href="/dashboard/billing"]'
   - position: 'right'

For the `upgrade` step: in the `TourStep` interface add optional `showIf?: () => boolean`. In the tour rendering, filter `tourSteps` by `!step.showIf || step.showIf()` before display. Set `showIf: () => localStorage.getItem('userPlan') === 'starter' || !localStorage.getItem('userPlan')`.

Total steps after: 14 (welcome + 10 existing + 3 new + complete).
</task>

<task type="auto" name="Onboarding complete page — explicit tour CTA">
In `app/(onboarding)/onboarding/complete/page.tsx`, after the existing success state (when `saved === true`):

1. Change the existing "Go to Dashboard" button to split into TWO buttons:
   - Primary: "Start Tour" (sets localStorage `showTourOnDashboard=true` then routes to dashboard)
   - Secondary/ghost: "Skip tour" (routes to dashboard directly)

2. Add a brief "What's next" section above the buttons listing 3 actions:
   - Connect your first social platform
   - Generate your first piece of content
   - Set up an AI workflow

This makes the tour explicitly opt-in with a clear CTA rather than auto-starting.

Also store `userPlan` in localStorage from the saved user data response so ProductTour upgrade step can check it.
Read the save response from `/api/onboarding/complete` — it should return `{ plan }` in the JSON. If not already returned, add it.
</task>
</tasks>

<verification>
- TypeScript: `npm run type-check` passes
- ProductTour renders 14 steps (or 13 for Pro users who skip upgrade step)
- Onboarding complete page shows "Start Tour" + "Skip tour" buttons after save succeeds
</verification>

<success_criteria>
- ProductTour has 3 new steps: workflows, insights, upgrade (conditional)
- TourStep interface has optional showIf field
- Onboarding complete page has explicit "Start Tour" / "Skip tour" CTAs
- /api/onboarding/complete returns plan in response body
- npm run type-check passes
</success_criteria>

<output>
Files to modify:
- components/ProductTour.tsx (add 3 steps, showIf filter)
- app/(onboarding)/onboarding/complete/page.tsx (split CTA, store userPlan)
- app/api/onboarding/complete/route.ts (return plan in response — if not already)
Commit: `feat(onboarding): extend ProductTour v2.0 steps + explicit tour CTA (SYN-53)`
</output>
