---
phase: 50-sponsor-crm
plan: 02
type: execute
---

<objective>
Create Sponsor CRM dashboard with pipeline view, sponsor management, deal tracking, and deliverables.

Purpose: Let creators visualize their sponsorship pipeline and manage brand relationships.
Output: CRM components, dashboard page, sidebar and command palette integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-sponsor-crm/50-01-SUMMARY.md

**From 50-01:**
@hooks/useSponsorCRM.ts (CRM data hook)
@lib/sponsors/sponsor-service.ts (CRM service)

**UI patterns to follow:**
@app/dashboard/revenue/page.tsx (dashboard with form modal)
@app/dashboard/roi/page.tsx (multi-section dashboard)
@components/revenue/RevenueEntryList.tsx (list with actions)
@components/roi/InvestmentForm.tsx (modal form)

**Established patterns:**
- Glassmorphic cards (bg-gray-900/50, border-white/10)
- Color-coded badges and stages
- PageHeader with filters
- Loading skeletons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline and sponsor components</name>
  <files>components/sponsors/PipelineOverview.tsx, components/sponsors/SponsorList.tsx, components/sponsors/SponsorForm.tsx</files>
  <action>
**PipelineOverview.tsx:**
- Kanban-style row showing deal counts per stage
- Stages: Negotiation, Contracted, In Progress, Delivered, Paid
- Color coding: yellow (negotiation), blue (contracted), purple (in_progress), emerald (delivered), cyan (paid)
- Show total pipeline value
- Clickable stage cards to filter

**SponsorList.tsx:**
- Card grid of sponsors
- Each card shows: logo/avatar, name, company, status badge, deal count, total value
- Status badges: lead (gray), active (green), past (orange)
- Click to view sponsor detail
- Edit/delete actions

**SponsorForm.tsx:**
- Modal form for add/edit sponsor
- Fields: name, company, email, phone, website, status, notes
- Status as select dropdown
- Submit and cancel buttons
- Loading state during submission
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>Pipeline overview and sponsor management components complete</done>
</task>

<task type="auto">
  <name>Task 2: Create deal and deliverable components</name>
  <files>components/sponsors/DealList.tsx, components/sponsors/DealForm.tsx, components/sponsors/DeliverableList.tsx, components/sponsors/DeliverableForm.tsx</files>
  <action>
**DealList.tsx:**
- List of deals (used in sponsor detail view)
- Each row: title, value, stage badge, date range, deliverable progress
- Stage badges with colors matching pipeline
- Click to expand deliverables
- Edit/delete actions

**DealForm.tsx:**
- Modal form for add/edit deal
- Fields: title, description, value, currency, stage, startDate, endDate
- Stage as select dropdown
- Date pickers for start/end

**DeliverableList.tsx:**
- List of deliverables within a deal
- Each row: title, type badge, platform, status badge, due date
- Type badges: post (blue), story (pink), reel (purple), video (red), mention (orange), review (cyan), other (gray)
- Status badges: pending (gray), in_progress (yellow), submitted (blue), approved (green), rejected (red)
- Checkbox-style completion toggle
- Edit/delete actions

**DeliverableForm.tsx:**
- Modal form for add/edit deliverable
- Fields: title, description, type, platform, status, dueDate, contentUrl
- Type and status as select dropdowns
- Platform as optional select
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>Deal and deliverable management components complete</done>
</task>

<task type="auto">
  <name>Task 3: Create Sponsor CRM dashboard page</name>
  <files>app/dashboard/sponsors/page.tsx, app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
**page.tsx - Sponsor CRM Dashboard:**

Layout:
1. **PageHeader:** "Sponsor CRM" with status filter (all/lead/active/past)
2. **PipelineOverview:** Visual pipeline stages with counts
3. **Sponsor Grid:** SponsorList showing sponsors
4. **Selected Sponsor Panel:** When sponsor selected, show deals + deliverables

State management:
- selectedSponsor: Sponsor | null
- showSponsorForm, showDealForm, showDeliverableForm modals
- editingSponsor, editingDeal, editingDeliverable

Use useSponsorCRM hook for data.
Handle loading with skeletons, error with retry.

**layout.tsx update:**
Add to sidebarItems after "ROI":
```typescript
{ icon: Handshake, label: 'Sponsors', href: '/dashboard/sponsors' },
```

**CommandPalette.tsx update:**
Add command:
```typescript
{
  id: 'sponsor-crm',
  title: 'Sponsor CRM',
  description: 'Manage brand deals and deliverables',
  icon: Handshake,
  action: () => router.push('/dashboard/sponsors'),
  category: 'navigation',
  keywords: ['sponsor', 'crm', 'brand', 'deal', 'partnership', 'deliverable', 'contract']
}
```

Note: Import Handshake from icons (check if exists, use Briefcase as fallback)
  </action>
  <verify>Navigate to /dashboard/sponsors, see pipeline and sponsors</verify>
  <done>Sponsor CRM dashboard accessible via sidebar and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] Pipeline overview shows deal stages
- [ ] Sponsor list displays correctly
- [ ] Deal management works
- [ ] Deliverable tracking works
- [ ] Navigation integrated (sidebar + command palette)
</verification>

<success_criteria>
- All tasks completed
- Pipeline visualizes deal stages
- Sponsors can be managed from UI
- Deals and deliverables can be tracked
- Phase 50 complete
</success_criteria>

<output>
After completion, create `.planning/phases/50-sponsor-crm/50-02-SUMMARY.md`
</output>
