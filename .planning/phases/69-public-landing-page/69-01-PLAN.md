---
phase: 69-public-landing-page
plan: 01
subsystem: marketing
tags: [landing, seo, og-images, conversion, pricing]

objective: |
  Ship a conversion-optimised public landing page at synthex.social root with proper
  SEO metadata, dynamic OG images, a free-tier pricing card, and billing toggle —
  addressing the critical conversion gaps identified in the UX audit.

execution_context:
  - app/page.tsx — existing landing page (uses components/landing/*)
  - components/landing/* — 13 section components (hero, features, stats, video, FAQ, CTA, footer, nav, particles, animations)
  - components/marketing/* — 9 reusable marketing primitives (HeroSection, FeatureCard, PricingCard, CTASection, SocialProof, etc.)
  - app/pricing/page.tsx — existing pricing page with 3 plans (Professional $249, Business $399, Custom)
  - lib/seo/metadata.ts — generateMetadata(), PAGE_METADATA for all pages
  - app/layout.tsx — root layout with JSON-LD structured data (Organization, SoftwareApplication, WebSite, VideoObject, HowTo)
  - components/stripe/checkout-button.tsx — CheckoutButton component used in pricing CTAs
  - No OG image exists in public/ despite being referenced as /og-image.png
  - No dynamic OG image API route exists

context: |
  The landing page already exists with a full set of sections (hero, features, video,
  stats, FAQ, CTA, footer) and GSAP animations. The root layout has comprehensive
  JSON-LD structured data. The SEO metadata infrastructure (lib/seo/metadata.ts) is
  mature with generateMetadata() helpers and PAGE_METADATA presets.

  However, the UX audit (2026-03-03) identified critical conversion issues:
  1. Pricing page has no free/trial tier card — $249/mo is the lowest visible plan
  2. FAQ says "no credit card required" but CTA routes through Stripe checkout
  3. No monthly/annual billing toggle (proven conversion lever)
  4. OG image is referenced in metadata but the file does not exist in public/
  5. app/page.tsx is 'use client' — bad for SEO crawlability (should be server component)

  The landing page components (components/landing/*) are separate from the reusable
  marketing primitives (components/marketing/*). The landing page uses its own
  dedicated set; the pricing page uses MarketingLayout from the marketing primitives.

tasks:
  - id: "01"
    type: auto
    name: Convert landing page to server component and fix SEO metadata
    description: |
      1. Convert app/page.tsx from 'use client' to a server component for SEO.
         Move interactive elements (GSAP animations, floating particles) into
         client-only wrapper components that are already client components.
         The landing section components in components/landing/* are already
         'use client' — the parent page.tsx just needs to remove its own
         'use client' directive and import them normally.
      2. Add proper Next.js metadata export to app/page.tsx using the existing
         PAGE_METADATA.home from lib/seo/metadata.ts (currently app/page.tsx
         has no metadata export — only the root layout provides defaults).
      3. Verify the JSON-LD FAQPage schema (buildFaqSchemaJson()) renders
         correctly as a server component child.
    files:
      create: []
      modify:
        - app/page.tsx
    verification: |
      - app/page.tsx has no 'use client' directive
      - app/page.tsx exports metadata using PAGE_METADATA.home
      - npm run type-check passes
      - Page renders correctly with all sections

  - id: "02"
    type: auto
    name: Generate OG image and add dynamic OG image API route
    description: |
      1. Create a static OG image at public/og-image.png (1200x630) using
         Next.js ImageResponse (app/api/og/route.tsx). The image should
         feature the SYNTHEX branding — dark navy background (#0a1628),
         cyan accent gradient, logo chevron, headline text
         "AI Marketing Automation", and the synthex.social URL.
      2. Create app/api/og/route.tsx using next/og (ImageResponse) to serve
         dynamic OG images. Accept query params: ?title=...&description=...
         This allows per-page OG images (e.g. /api/og?title=Pricing).
      3. Update lib/seo/metadata.ts generateMetadata() to use the dynamic
         OG route as default image: /api/og?title=${encodeURIComponent(title)}
      4. Create a simple static fallback at public/og-image.png by exporting
         a pre-rendered version from the OG route (or create a simple SVG-based
         PNG). This serves as fallback for crawlers that cannot execute JS.
    files:
      create:
        - app/api/og/route.tsx
      modify:
        - lib/seo/metadata.ts
    verification: |
      - GET /api/og returns a valid PNG image with correct dimensions
      - GET /api/og?title=Pricing returns image with "Pricing" text
      - npm run type-check passes
      - OG image renders in social media preview tools

  - id: "03"
    type: auto
    name: Add free-tier card and billing toggle to pricing page
    description: |
      Address the two critical pricing page conversion issues from the UX audit:

      1. Add a "Starter" free-tier card as the first (leftmost) plan in the
         pricing grid. This card should:
         - Name: "Starter"
         - Price: "Free" (no period suffix)
         - Description: "14-day trial — no credit card required"
         - Features: 2 social accounts, 10 AI posts/month, Basic analytics,
           1 persona profile
         - CTA: "Start Free Trial" linking to /signup (NOT Stripe checkout)
         - Do NOT use CheckoutButton — use a plain Link to /signup

      2. Add a monthly/annual billing toggle above the pricing grid:
         - Default to monthly
         - Annual prices: Professional $199/mo (billed annually, save 20%),
           Business $319/mo (billed annually, save 20%)
         - Show "Save 20%" badge when annual is selected
         - The Starter (free) card is unaffected by the toggle
         - The Custom card shows "Contact Sales" regardless of toggle

      3. Update the grid from 3 columns to 4 columns (md:grid-cols-2 lg:grid-cols-4)
         to accommodate the new Starter card.

      4. Ensure the pricing FAQPage JSON-LD schema is preserved.
    files:
      create: []
      modify:
        - app/pricing/page.tsx
    verification: |
      - Pricing page shows 4 cards: Starter, Professional, Business, Custom
      - Starter card CTA links to /signup (not Stripe checkout)
      - Billing toggle switches between monthly and annual prices
      - Annual prices show ~20% discount
      - Business card retains "Most Popular" badge
      - npm run type-check passes

  - id: "04"
    type: auto
    name: Add "How It Works" section to landing page
    description: |
      The landing page has hero, features, video, stats, FAQ, CTA sections but
      no "How It Works" step-by-step section. A how-it-works.tsx component
      already exists at components/landing/how-it-works.tsx but is not imported
      into app/page.tsx.

      1. Review components/landing/how-it-works.tsx and verify it renders a
         3-step process (matching the HowTo JSON-LD in the root layout).
      2. Import and add the HowItWorks section to app/page.tsx between the
         FeaturesSection and VideoSection.
      3. Add a Testimonials section using the existing components/landing/testimonials.tsx
         component (also exists but is not imported). Place it between
         StatsSection and FAQSection.
      4. Verify all landing page sections render in a logical flow:
         NavBar → Hero → Features → HowItWorks → Video → Stats → Testimonials → FAQ → CTA → Footer
    files:
      create: []
      modify:
        - app/page.tsx
    verification: |
      - Landing page includes HowItWorks section
      - Landing page includes Testimonials section
      - Section order is logical: Hero → Features → HowItWorks → Video → Stats → Testimonials → FAQ → CTA → Footer
      - npm run type-check passes
      - No visual regressions in dev server

success_criteria:
  - Landing page is a server component with proper metadata export
  - OG images render correctly (static fallback + dynamic API route)
  - Pricing page shows a free Starter tier with /signup CTA (not Stripe checkout)
  - Pricing page has monthly/annual billing toggle with 20% annual discount
  - All unused landing components (HowItWorks, Testimonials) are wired in
  - npm run type-check passes on all modified files
  - JSON-LD structured data remains intact

output:
  - Server-rendered landing page with SEO metadata
  - Dynamic OG image API route at /api/og
  - 4-tier pricing page with free tier and billing toggle
  - Complete landing page with all sections wired
---
