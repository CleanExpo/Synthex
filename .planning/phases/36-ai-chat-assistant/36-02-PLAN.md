---
phase: 36-ai-chat-assistant
plan: 02
type: execute
model: sonnet
model_rationale: |
  Plan 2 (40% context) implements React UI components, hook, and dashboard
  integration following established patterns from dashboard pages.
  Complexity score: 3.5 (standard UI work with established patterns).
  Routing: Sonnet
---

<objective>
Build AI Chat Assistant UI with conversation interface and dashboard integration.

Purpose: Provide accessible chat UI for content creators to get AI-powered help with content ideas and strategy.
Output: Working /dashboard/ai-chat page with streaming chat interface, navigation, and command palette integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing UI patterns:
@components/dashboard/sidebar.tsx
@components/dashboard/command-palette.tsx
@hooks/use-api.ts

# Prior phase outputs (patterns to follow):
@.planning/phases/35-seo-audit-automation/35-02-SUMMARY.md

# Plan 01 outputs (dependencies):
# lib/ai/chat-assistant/index.ts
# app/api/ai/chat/conversations/route.ts
# app/api/ai/chat/conversations/[conversationId]/messages/route.ts

**Tech stack:** React 19, Next.js 15 App Router, Tailwind CSS, Radix UI
**Established patterns:** useApi hook, PageHeader component, glassmorphic styling
**Key decisions:**
- Dashboard empty state pattern: inline EmptyState with icon, message, CTA button
- Loading states use glassmorphic styling (bg-white/5, bg-[#0f172a]/80, border-cyan-500/10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAIChat Hook</name>
  <files>hooks/use-ai-chat.ts</files>
  <action>
Create React hook for AI chat state management in hooks/use-ai-chat.ts:

**Types:**
```typescript
interface ChatConversation {
  id: string;
  title: string;
  status: 'active' | 'archived' | 'deleted';
  messageCount: number;
  lastMessageAt: string | null;
  createdAt: string;
}

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  createdAt: string;
}
```

**Hook: useAIChat()**
- conversations: ChatConversation[] — list of user's conversations
- isLoading: boolean
- error: string | null
- createConversation(title?: string): Promise<ChatConversation>
- deleteConversation(id: string): Promise<void>
- refreshConversations(): Promise<void>

**Hook: useAIChatConversation(conversationId: string)**
- conversation: ChatConversation | null
- messages: ChatMessage[]
- isLoading: boolean
- error: string | null
- sendMessage(content: string): Promise<void> — handles SSE streaming
- streamingContent: string — current streaming response
- isStreaming: boolean

**SSE handling in sendMessage:**
1. Set isStreaming = true
2. POST to /api/ai/chat/conversations/[id]/messages with { message }
3. Use EventSource or fetch with reader for SSE
4. Parse events: message_start, data (tokens), message_complete
5. Append tokens to streamingContent
6. On complete: add full message to messages array, clear streamingContent
7. Set isStreaming = false

Reference: hooks/use-api.ts for fetch patterns
Don't: Use SWR or TanStack Query — direct fetch with EventSource for SSE
Don't: Poll for updates — use SSE streaming directly
  </action>
  <verify>npm run type-check passes, hooks/use-ai-chat.ts exports useAIChat and useAIChatConversation</verify>
  <done>Hooks provide conversation list management and streaming chat functionality</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatAssistant Component</name>
  <files>components/ai/chat-assistant.tsx, components/ai/chat-message.tsx, components/ai/chat-input.tsx</files>
  <action>
Create chat interface components in components/ai/:

**chat-message.tsx:**
- ChatMessage component: { message: ChatMessage, isStreaming?: boolean }
- User messages: right-aligned, bg-cyan-500/20, border-cyan-500/30
- Assistant messages: left-aligned, bg-white/5, border-white/10
- Streaming indicator: pulsing dot when isStreaming
- Markdown rendering for assistant responses (use @/components/ui/markdown or simple prose)
- Timestamp display (relative time)

**chat-input.tsx:**
- ChatInput component: { onSend, disabled, placeholder }
- Textarea with auto-resize (max 4 rows)
- Send button with Lucide Send icon
- Disable when streaming or empty
- Submit on Enter (Shift+Enter for newline)
- Character count display (max 3000)

**chat-assistant.tsx:**
- ChatAssistant component: { conversationId: string }
- Use useAIChatConversation(conversationId)
- Message list with scroll to bottom on new messages
- Empty state: "Start a conversation about your content strategy"
- Streaming message at bottom (assistant bubble with streamingContent)
- ChatInput at bottom (sticky)
- Error display with retry button

**Styling:**
- Container: h-full flex flex-col
- Message list: flex-1 overflow-y-auto space-y-4 p-4
- Input area: border-t border-white/10 p-4 bg-[#0f172a]/50
- Glassmorphic styling throughout (bg-white/5, backdrop-blur)

Reference: components/dashboard/ for styling patterns
Don't: Use external chat libraries — build simple, focused components
Don't: Add typing indicators beyond the streaming content itself
  </action>
  <verify>npm run type-check passes, components/ai/chat-assistant.tsx exports ChatAssistant</verify>
  <done>Chat UI components exist with message display, streaming support, and input handling</done>
</task>

<task type="auto">
  <name>Task 3: Create AI Chat Dashboard Page and Navigation</name>
  <files>app/dashboard/ai-chat/page.tsx, app/dashboard/ai-chat/loading.tsx, app/dashboard/ai-chat/error.tsx</files>
  <action>
Create dashboard page and wire into navigation:

**app/dashboard/ai-chat/page.tsx:**
- 'use client' (needs hooks)
- Use useAIChat() for conversation list
- Two-column layout:
  - Left sidebar (w-80): conversation list with create button
  - Main area: ChatAssistant component for selected conversation
- Conversation list items: title, message count, last activity
- Create conversation button: "New Chat" with Plus icon
- Selected conversation highlighted
- Empty state when no conversations: "Start your first AI chat"
- Subscription check: Show upgrade CTA if not Creator+

**app/dashboard/ai-chat/loading.tsx:**
- Skeleton with glassmorphic styling
- Two-column layout skeleton

**app/dashboard/ai-chat/error.tsx:**
- Use DashboardError component from components/dashboard/error-fallback.tsx

**Update sidebar navigation (components/dashboard/sidebar.tsx):**
- Add "AI Chat" item in appropriate section
- Icon: MessageSquare from lucide-react
- Path: /dashboard/ai-chat
- Badge: "New" (optional, remove after a few weeks)

**Update command palette (components/dashboard/command-palette.tsx):**
- Add command: { name: 'AI Chat', href: '/dashboard/ai-chat', icon: MessageSquare }
- Category: 'AI' or 'Tools'

Reference: app/dashboard/geo-readiness/page.tsx for two-section layout pattern
Reference: components/dashboard/sidebar.tsx for nav item structure
Don't: Create nested routes for individual conversations — use query params or state
Don't: Add complex conversation management UI — keep it simple (list + chat)
  </action>
  <verify>Navigate to http://localhost:3000/dashboard/ai-chat loads page, sidebar shows AI Chat link</verify>
  <done>AI Chat dashboard page exists with conversation management, navigation link, and command palette entry</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes without errors
- [ ] `npm run build` succeeds
- [ ] hooks/use-ai-chat.ts exports useAIChat and useAIChatConversation
- [ ] components/ai/chat-assistant.tsx renders chat interface
- [ ] /dashboard/ai-chat page loads with conversation list
- [ ] Sidebar shows AI Chat navigation item
- [ ] Command palette includes AI Chat command
</verification>

<success_criteria>

- All 3 tasks completed
- useAIChat hooks with SSE streaming support
- Chat UI components (messages, input, assistant)
- Dashboard page with conversation management
- Navigation and command palette integration
- No TypeScript errors
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-ai-chat-assistant/36-02-SUMMARY.md`

Phase 36 complete after this plan.
</output>
