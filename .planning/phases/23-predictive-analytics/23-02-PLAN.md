---
phase: 23-predictive-analytics
plan: 02
type: execute
model: sonnet
model_rationale: "Plan 02 (35% context) adds forecast chart and heatmap visualizations using Recharts patterns from Phase 22. Complexity score: ~2.0. Routing: Sonnet"
---

<objective>
Add engagement forecast chart and best-time heatmap visualizations to the predictive analytics dashboard.

Purpose: Plan 23-01 built the core predictions page with stats, engagement predictor form, and optimal times list. This plan adds the visual components: a time-series forecast chart with confidence bands and an interactive 7x24 heatmap for optimal posting times.
Output: ForecastChart and BestTimeHeatmap components wired into `/dashboard/predictions` page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-predictive-analytics/23-01-PLAN.md

# Key source files — backend (already built, DO NOT modify):
@app/api/predict/trends/route.ts
@app/api/optimize/auto-schedule/route.ts
@lib/ml/posting-time-predictor.ts
@lib/analytics/trend-predictor.ts

# Key source files — from Plan 23-01 (will exist after 23-01 executes):
# components/predictions/types.ts — ForecastPoint, EngagementForecast, OptimalTimeSlot, OptimalTimesResult
# hooks/use-dashboard.ts — fetchForecast, useOptimalTimes
# app/dashboard/predictions/page.tsx — main page with placeholder for forecast chart
# components/predictions/index.ts — barrel exports

# Established Recharts patterns from Phase 22:
@components/analytics/analytics-config.ts
@app/dashboard/analytics/page.tsx

**Types from 23-01 (for reference):**

```typescript
interface ForecastPoint {
  date: string;
  predicted: number;
  lowerBound: number;
  upperBound: number;
}

interface EngagementForecast {
  platform: string;
  metric: string;
  currentValue: number;
  predictions: ForecastPoint[];
  trend: 'rising' | 'stable' | 'declining' | 'volatile';
  growthRate: number;
  confidence: number;
}

interface OptimalTimeSlot {
  day: number;    // 0-6 (Sun-Sat)
  hour: number;   // 0-23
  score: number;  // 0-100
  confidence: number; // 0-1
}
```

**Recharts patterns (from Phase 22):**
- AreaChart with ResponsiveContainer for time-series
- Recharts uses `<Area>`, `<XAxis>`, `<YAxis>`, `<Tooltip>`, `<CartesianGrid>`
- Glassmorphic tooltip styling: bg-[#0f172a]/90, border-white/10
- Colors: cyan-400, emerald-400, violet-400

**Constraining decisions:**
- [Phase 22]: Recharts for all chart visualizations
- [Phase 16]: Loading states use glassmorphic styling
- [Phase 23-01]: ForecastChart placeholder exists in page layout
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ForecastChart and BestTimeHeatmap components</name>
  <files>components/predictions/forecast-chart.tsx, components/predictions/best-time-heatmap.tsx, components/predictions/index.ts</files>
  <action>
1. Create `components/predictions/forecast-chart.tsx`:
   - Import from `recharts`: ResponsiveContainer, AreaChart, Area, XAxis, YAxis, Tooltip, CartesianGrid
   - Import Card, CardContent, CardHeader, CardTitle from `@/components/ui/card`
   - Props: `forecast: EngagementForecast | null`, `isLoading: boolean`
   - Chart renders an AreaChart with 3 areas:
     - `upperBound` area (fill only, light cyan, opacity 0.1) — top of confidence band
     - `lowerBound` area (fill only, light cyan, opacity 0.1) — bottom of confidence band
     - `predicted` line (stroke cyan-400, strokeWidth 2, no fill) — main forecast line
   - Actually, for confidence bands: render a single stacked area from lowerBound to upperBound:
     - Use `<Area dataKey="upperBound" stroke="none" fill="rgba(34,211,238,0.15)" />`
     - Use `<Area dataKey="lowerBound" stroke="none" fill="white" fillOpacity={0} />` and clip or:
     - Simpler: transform data to have `bandWidth = upperBound - lowerBound` and stack on top of lowerBound
     - Best approach: Use two areas — `lowerBound` with transparent fill, `upperBound` with a very light fill, and then `predicted` as the line on top
   - XAxis: date labels (format: "Mon DD" or "MM/DD")
   - YAxis: auto-scaled
   - Custom Tooltip: shows date, predicted value, range (lowerBound - upperBound)
   - Glassmorphic card wrapper
   - Header shows: metric name, trend badge (rising=green, declining=red, stable=yellow, volatile=orange), growth rate %, confidence %
   - Loading state: skeleton with pulsing gradient
   - Null state: empty card with "Select a platform to see engagement forecast"

2. Create `components/predictions/best-time-heatmap.tsx`:
   - Import Card, CardContent, CardHeader, CardTitle from `@/components/ui/card`
   - Props: `slots: OptimalTimeSlot[]`, `isLoading: boolean`
   - Renders a 7-row (days) x 24-column (hours) grid
   - Days: Sun, Mon, Tue, Wed, Thu, Fri, Sat (rows)
   - Hours: 0-23 (columns), show labels every 3 hours (12am, 3am, 6am, 9am, 12pm, 3pm, 6pm, 9pm)
   - Each cell: colored by score (0-100):
     - Score 0-20: bg-slate-800/50 (very dim)
     - Score 21-40: bg-cyan-900/40
     - Score 41-60: bg-cyan-700/50
     - Score 61-80: bg-cyan-500/60
     - Score 81-100: bg-cyan-400/80
   - Build a lookup Map from slots: `key = "${day}-${hour}"` → score
   - Cells with no slot data: bg-slate-800/30 (neutral)
   - Hover tooltip: shows day name, time (AM/PM), score, confidence
   - Use CSS grid: `grid-template-columns: auto repeat(24, 1fr)`
   - Cell size: small squares, responsive (min-w-[12px] h-[20px] on mobile, larger on desktop)
   - Glassmorphic card wrapper with legend (low → high color scale)
   - Loading state: grid of pulsing skeleton cells

3. Update `components/predictions/index.ts`:
   - Add exports for ForecastChart and BestTimeHeatmap
  </action>
  <verify>
   - `npm run type-check` passes
   - ForecastChart and BestTimeHeatmap exported from index.ts
   - Components accept correct prop types
  </verify>
  <done>
   - ForecastChart renders AreaChart with confidence bands and trend indicators
   - BestTimeHeatmap renders 7x24 grid with color-coded scores
   - Both components handle null/loading states
   - Glassmorphic styling throughout
   - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire visualizations into predictions page</name>
  <files>app/dashboard/predictions/page.tsx</files>
  <action>
1. In `app/dashboard/predictions/page.tsx`:
   - Import ForecastChart, BestTimeHeatmap from `@/components/predictions`
   - Import fetchForecast from `@/hooks/use-dashboard`
   - Import EngagementForecast type from `@/components/predictions/types`
   - Add state: `forecastData: EngagementForecast | null`, `isForecastLoading: boolean`
   - Add useEffect that calls fetchForecast when platform changes:
     ```
     useEffect(() => {
       setIsForecastLoading(true);
       fetchForecast({ platform, metric: 'engagement', days: 30 })
         .then(setForecastData)
         .catch(() => setForecastData(null))
         .finally(() => setIsForecastLoading(false));
     }, [platform]);
     ```
   - Replace the placeholder div for "Engagement Forecast" with:
     - Full-width section below the two-column grid
     - Two-column grid (lg:grid-cols-2):
       - Left: ForecastChart with forecastData and isForecastLoading
       - Right: BestTimeHeatmap with slots from useOptimalTimes data and its loading state
   - Add a platform select at the top (near PageHeader) that changes the `platform` state
     - This drives: useOptimalTimes, fetchForecast, and the BestTimeHeatmap
     - Use the same platform options as EngagementPredictor (twitter, instagram, linkedin, facebook, tiktok, youtube)
   - Ensure the platform filter is a glassmorphic select component
  </action>
  <verify>
   - `npm run type-check` passes
   - Page renders ForecastChart and BestTimeHeatmap
   - Platform select drives forecast and heatmap data
   - Loading states show while data fetches
  </verify>
  <done>
   - ForecastChart displays engagement forecast with confidence bands
   - BestTimeHeatmap displays 7x24 grid for selected platform
   - Platform select at top drives both visualizations
   - All loading/empty states handled
   - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes without errors
- [ ] ForecastChart renders with forecast data and confidence bands
- [ ] BestTimeHeatmap renders 7x24 grid with color-coded scores
- [ ] Platform select drives both visualizations
- [ ] Loading states display correctly
- [ ] Glassmorphic styling consistent with rest of dashboard
- [ ] No TypeScript errors introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- ForecastChart shows time-series with confidence bands
- BestTimeHeatmap shows interactive 7x24 grid
- Platform selection drives data refresh
- Consistent glassmorphic styling
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-predictive-analytics/23-02-SUMMARY.md`
Then update STATE.md (Phase 23 complete) and ROADMAP.md (Phase 23 marked complete).
</output>
