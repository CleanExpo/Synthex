---
phase: 55-ui-audit-states
plan: 01
type: execute
domain: frontend
---

<objective>
Create missing loading.tsx and error.tsx files for dashboard routes that lack them.

Purpose: Every dashboard route must have a file-based loading state (Suspense boundary) and error boundary. Discovery found 4 gaps: root dashboard loading, platforms loading, platforms error, and seo error.

Output: 4 new state files — app/dashboard/loading.tsx, app/dashboard/platforms/loading.tsx, app/dashboard/platforms/error.tsx, app/dashboard/seo/error.tsx.
</objective>

<context>
@.planning/STATE.md
@app/dashboard/analytics/loading.tsx     # Reference: well-structured skeleton pattern
@app/dashboard/analytics/error.tsx       # Reference: DashboardError usage pattern
@components/dashboard/error-fallback.tsx # DashboardError component API
@components/skeletons/DashboardSkeleton.tsx  # DashboardSkeleton component
@app/dashboard/platforms/page.tsx        # Platforms page structure (545 lines, client component)
@app/dashboard/seo/page.tsx             # SEO hub page structure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root dashboard loading.tsx</name>
  <files>app/dashboard/loading.tsx</files>
  <action>
    Create a skeleton loading state for the root /dashboard route. Match the glassmorphic skeleton pattern from analytics/loading.tsx:
    - 'use client' directive
    - export default function DashboardLoading()
    - animate-pulse wrapper div with space-y-6
    - Header skeleton: h-9 w-48 title + h-5 w-72 subtitle, right side with 2-3 h-10 button skeletons (bg-cyan-500/10 for the primary)
    - Stats grid: 4 × Card with bg-[#0f172a]/80 border-cyan-500/10, each with icon placeholder + value + label skeleton
    - Main area: 2 Cards side by side — chart placeholder (h-80 bg-white/5 rounded) and list placeholder (5 rows of h-12 items with bg-white/5)
    - All skeleton divs use bg-white/5 rounded

    Do NOT import from DashboardSkeleton — write inline skeleton HTML like analytics/loading.tsx does.
    Do NOT add 'use server' — this must be a client component with animate-pulse.
  </action>
  <verify>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "dashboard/loading" || echo "no errors"</verify>
  <done>app/dashboard/loading.tsx exists, exports default DashboardLoading, uses animate-pulse with glassmorphic skeleton</done>
</task>

<task type="auto">
  <name>Task 2: Create platforms/loading.tsx and platforms/error.tsx</name>
  <files>
    app/dashboard/platforms/loading.tsx
    app/dashboard/platforms/error.tsx
  </files>
  <action>
    **platforms/loading.tsx:**
    - 'use client' directive
    - export default function PlatformsLoading()
    - Skeleton matching platforms page structure:
      - Header skeleton (title + subtitle + 1 button)
      - Grid of 9 platform cards (3×3 on desktop) using [...Array(9)].map():
        - Card with bg-[#0f172a]/80 border-cyan-500/10
        - Icon placeholder (w-12 h-12 bg-white/5 rounded-full)
        - Title skeleton (h-5 w-32 bg-white/5 rounded)
        - Subtitle (h-4 w-24 bg-white/5 rounded mt-1)
        - Status badge placeholder (h-6 w-20 bg-white/5 rounded mt-3)
        - Bottom action row: 2 × h-8 w-24 bg-white/5 rounded buttons
      - animate-pulse on wrapper
      - Imports: Card, CardContent from @/components/ui/card

    **platforms/error.tsx:**
    - Exact same pattern as app/dashboard/analytics/error.tsx
    - 'use client' directive
    - Props: error: Error & { digest?: string }, reset: () => void
    - export default function PlatformsError({ error, reset })
    - Returns: <DashboardError error={error} reset={reset} title="Platforms Error" description="Failed to load platform connections. Please try again." />
    - Import: DashboardError from @/components/dashboard
  </action>
  <verify>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "platforms" || echo "no errors"</verify>
  <done>platforms/loading.tsx exports PlatformsLoading with 9-card grid skeleton; platforms/error.tsx exports PlatformsError using DashboardError</done>
</task>

<task type="auto">
  <name>Task 3: Create seo/error.tsx</name>
  <files>app/dashboard/seo/error.tsx</files>
  <action>
    The seo/ route has loading.tsx but is missing error.tsx. Create it following the exact same pattern as analytics/error.tsx:
    - 'use client' directive
    - Props: error: Error & { digest?: string }, reset: () => void
    - export default function SeoError({ error, reset })
    - Returns: <DashboardError error={error} reset={reset} title="SEO Dashboard Error" description="Failed to load SEO data. Please try again." />
    - Import: DashboardError from @/components/dashboard

    Note: seo/loading.tsx already exists — only error.tsx is needed.
  </action>
  <verify>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "seo/error" || echo "no errors"</verify>
  <done>seo/error.tsx exists, exports SeoError using DashboardError component</done>
</task>

</tasks>

<verification>
After all tasks:
- [ ] app/dashboard/loading.tsx exists and uses animate-pulse glassmorphic skeleton
- [ ] app/dashboard/platforms/loading.tsx exists with 9-card grid skeleton
- [ ] app/dashboard/platforms/error.tsx exists using DashboardError
- [ ] app/dashboard/seo/error.tsx exists using DashboardError
- [ ] npm run type-check passes (no TypeScript errors in new files)
</verification>

<success_criteria>
- All 4 missing state files created
- Each follows the established DashboardError / skeleton pattern exactly
- npm run type-check passes with 0 new errors
</success_criteria>

<output>
After completion, create .planning/phases/55-ui-audit-states/55-01-SUMMARY.md:

# Phase 55 Plan 01: Missing State Files Summary

**[One-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
</output>
