---
phase: 06-cron-jobs
plan: 02
type: execute
model: sonnet
model_rationale: "Plan 02 (35% context) wires weekly digest email delivery + final cron sweep. Complexity: 3.0 → Sonnet ✅"
---

<objective>
Wire weekly digest email delivery and verify all 5 Vercel crons work with real data.

Purpose: The weekly digest cron generates AI digest content and stores it in AIWeeklyDigest, but never sends the email (line 67: `// TODO: Send email via email service`). The email queue infrastructure (lib/email/queue.ts) already exists with SendGrid + Resend providers. This plan connects them and does a final sweep of all 5 crons.
Output: Weekly digest sends actual email to users. All 5 Vercel crons verified working with real data flows.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key source files:
@app/api/cron/weekly-digest/route.ts
@lib/email/queue.ts
@lib/email/email-service.ts
@app/api/cron/proactive-insights/route.ts
@app/api/cron/health-score/route.ts
@app/api/reports/scheduled/execute/route.ts

**Tech stack available:** Next.js 15, TypeScript 5, Prisma 6, BullMQ, SendGrid, Resend
**Established patterns:** emailQueue.enqueue() for async delivery, EmailJob interface with metadata
**Constraining decisions:**
- Email queue (lib/email/queue.ts) handles SendGrid/Resend provider selection
- EmailJob interface requires: to, from, subject, html, metadata
- generateWeeklyDigest() already returns { summary, highlights, actionItems, opportunities }
- AIWeeklyDigest model stores the digest content (already created by cron)
- Proactive insights cron creates in-app notifications (not email) — this is correct behavior
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire weekly digest email delivery</name>
  <files>app/api/cron/weekly-digest/route.ts</files>
  <action>
Add email delivery to the weekly digest cron after the AIWeeklyDigest record is created.

**Changes to route.ts:**

1. **Import** emailQueue from `@/lib/email/queue`:
   ```typescript
   import emailQueue from '@/lib/email/queue';
   ```

2. **Look up user email** — After `generateWeeklyDigest(user.userId)`, look up the user's email and name:
   ```typescript
   const userData = await prisma.user.findUnique({
     where: { id: user.userId },
     select: { email: true, name: true },
   });
   ```

3. **Create HTML email template** — Build a weekly digest email inline (no new file needed). The template should:
   - Have a professional header with "Your Weekly Digest" title
   - Show the summary paragraph
   - List highlights as bullet points
   - List action items as a checklist
   - List opportunities as cards/bullets
   - Include a CTA button linking to the dashboard
   - Match the existing email style (see lib/email/email-service.ts templates for gradient header, container, button patterns)

4. **Enqueue the email** — Replace the `// TODO` comment with:
   ```typescript
   if (userData?.email) {
     await emailQueue.enqueue({
       to: userData.email,
       from: process.env.EMAIL_FROM || 'noreply@synthex.social',
       subject: `Your Weekly Marketing Digest — ${new Date().toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}`,
       html: digestEmailHtml,
       metadata: {
         userId: user.userId,
         type: 'notification' as const,
       },
     });
   }
   ```

5. **Track email count** — Add an `emailsSent` counter alongside `generated` and `errors`. Include it in the response JSON.

**Template content approach:**
Build the HTML string using template literals, inserting digest.summary, digest.highlights (array of strings), digest.actionItems (array of strings), and digest.opportunities (array of strings). Use the same CSS class pattern as the existing email templates in email-service.ts (container, header with gradient, content section, button class).

Don't: Create a separate template file — keep it inline in the route for simplicity.
Don't: Change the digest generation logic — only add email delivery after the DB write.
Don't: Send email if user has no email address — skip silently.
Don't: Let email failure crash the batch — wrap enqueue in try-catch, increment errors if it fails.
  </action>
  <verify>npm run type-check passes. Route still exports GET handler. Grep for the TODO comment confirms it's replaced.</verify>
  <done>Weekly digest cron sends email via emailQueue after creating AIWeeklyDigest record. Email has styled HTML template. Failures don't crash batch. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final cron verification and cleanup</name>
  <files>app/api/cron/weekly-digest/route.ts, app/api/competitors/track/execute/route.ts</files>
  <action>
Verify all 5 Vercel crons work correctly by reading each route and confirming real data flows.

**Verification checklist (read each file, confirm):**

1. **Scheduled Reports** (`app/api/reports/scheduled/execute/route.ts`)
   - ✓ Uses real Prisma queries
   - ✓ Has email delivery (dual-provider)
   - ✓ Has cron auth check
   - Status: Expected to be fully working — just confirm

2. **Competitor Tracking** (`app/api/competitors/track/execute/route.ts`)
   - Should now use fetchCompetitorMetrics from 06-01
   - ✓ Has cron auth check
   - ✓ Creates real CompetitorSnapshot records
   - Status: Verify 06-01 changes are wired

3. **Health Score** (`app/api/cron/health-score/route.ts`)
   - ✓ Uses real Prisma queries
   - ✓ Has cron auth check
   - Status: Expected to be fully working — just confirm

4. **Weekly Digest** (`app/api/cron/weekly-digest/route.ts`)
   - Should now have email delivery from Task 1
   - ✓ Has AI generation
   - ✓ Has cron auth check
   - Status: Verify Task 1 changes are complete

5. **Proactive Insights** (`app/api/cron/proactive-insights/route.ts`)
   - ✓ Uses real Prisma queries for anomaly detection
   - ✓ Generates AI suggestions
   - ✓ Creates notifications (in-app, not email — this is correct)
   - ✓ Has cron auth check
   - Status: Expected to be fully working — just confirm

**If any cron has issues:**
- Fix inline (small fixes only — don't refactor)
- Document in summary

**Final type-check and build:**
- Run `npm run type-check` to confirm no errors
- Run `npm run build` to confirm production build succeeds

Don't: Refactor working crons — only fix actual issues.
Don't: Add email to proactive insights — notifications are the correct delivery channel there.
Don't: Touch the analyze-patterns stub — it's not in vercel.json and not part of Phase 6 scope.
  </action>
  <verify>npm run type-check passes. npm run build passes. All 5 Vercel crons confirmed working with real data.</verify>
  <done>All 5 Vercel crons verified: scheduled-reports, competitor-tracking, health-score, weekly-digest, proactive-insights. All use real data. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run type-check` passes
- [ ] Weekly digest sends email via emailQueue
- [ ] Email has professional HTML template
- [ ] Email failures don't crash the batch
- [ ] TODO comment replaced with real implementation
- [ ] All 5 Vercel crons verified working
- [ ] Response includes emailsSent count
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Weekly digest sends actual emails to Business plan users
- All 5 Vercel crons work with real data flows
- Existing cron auth and error handling preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-cron-jobs/06-02-SUMMARY.md`
</output>
