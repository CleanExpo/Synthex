---
phase: 09-performance
plan: 01
type: execute
model: sonnet
---

<objective>
Clean build configuration: remove dangerous scripts, consolidate duplicates, fix package.json metadata.

Purpose: Eliminate `|| true` build hacks, `git add -A` deploy scripts, and other anti-patterns that mask failures or risk accidental commits. Clean package.json of library-oriented fields that don't belong in a Next.js app.
Output: Clean, safe package.json with ~40 fewer script lines and no dangerous patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@package.json
@vercel.json
@next.config.mjs

**Constraining decisions:**
- Phase 1 archived legacy Express app but src/ directory still exists with imports
- Phase 4 established canonical rate limiter in lib/middleware/ (not express-rate-limit)
- Build uses `npx prisma@6.14.0 generate && next build` (build:vercel)
- NEXT_PRIVATE_SKIP_TYPECHECK=1 added to vercel.json to prevent OOM
- Type checking runs separately via `npm run type-check`
- ESLint runs separately via `npx eslint .` (not during build)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean package.json scripts</name>
  <files>package.json</files>
  <action>
Edit package.json scripts section to remove dangerous and redundant entries:

**Remove dangerous scripts (mask failures or risk data loss):**
- `build:emergency` — uses `|| true` to hide build failures
- `deploy:safe` — uses `git add -A` (stages everything including secrets)
- `deploy:push` — uses `git add -A`
- `deploy:emergency` — uses `git add -A` AND `--no-verify` (skips hooks)
- `fix:deps` — uses `--legacy-peer-deps` (masks dependency conflicts)

**Remove duplicate scripts (identical to another script):**
- `build:force` — identical to `build` (`next build`)
- `build:prod` — alias for `build:production`
- `dev:api` — identical to `dev:next` (`next dev`)
- `typecheck` — alias for `type-check`
- `test:unit` — alias for `test`
- `storybook:build` — alias for `build-storybook`
- `test:e2e` — alias for `e2e`
- `test:e2e:ui` — alias for `e2e:ui`

**Remove obsolete scripts (reference non-existent or legacy tools):**
- `ci:verify` — references `agents/tools/ci-verify.js` (legacy agent system)
- `plan:team` — references `agents/planner/plan-generator.ts` (legacy)
- `check:quick` — references `bash scripts/final-check.sh` (may not exist)
- `check:deploy` — depends on check:quick
- `diagnose` — references `scripts/diagnose-build.js` (legacy deploy tooling)
- `deploy` — references `scripts/deploy-assistant.js` (legacy)

**Keep these (still useful):**
- `build`, `build:vercel`, `build:fresh`, `build:production` — distinct purposes
- `deploy:prod` — clean `vercel --prod --yes`
- All `db:*` scripts — database management
- All `test:*` scripts (except duplicates removed above)
- `clean:cache`, `reinstall`, `analyze`
- `validate:*`, `security:*`, `release:check`
- Storybook, e2e, ws:* scripts

**Do NOT change script implementations** — only remove entire script entries.
  </action>
  <verify>node -e "const p = require('./package.json'); const bad = ['build:emergency','deploy:safe','deploy:push','deploy:emergency','fix:deps','build:force','build:prod','dev:api','typecheck','test:unit','storybook:build','test:e2e','test:e2e:ui']; const found = bad.filter(s => p.scripts[s]); if(found.length) { console.error('Still present:', found); process.exit(1); } console.log('All dangerous/duplicate scripts removed');"</verify>
  <done>Zero dangerous scripts (|| true, git add -A, --no-verify, --legacy-peer-deps). Zero duplicate script aliases. ~15 scripts removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean package.json metadata and verify</name>
  <files>package.json</files>
  <action>
Remove library-oriented fields that don't belong in a Next.js application:

**Remove these top-level fields entirely:**
- `exports` — This is npm library packaging config. Synthex is a Next.js app deployed to Vercel, not published to npm.
- `files` — Same reason. The `dist` directory doesn't exist and isn't used.
- `overrides.three-mesh-bvh` — No three.js dependency exists in the project. This override is orphaned.

**Fix duplicate prisma entry:**
- `prisma` appears in BOTH `dependencies` ("prisma": "6.14.0") AND `devDependencies` ("prisma": "6.14.0")
- Remove from `dependencies` — Prisma CLI is a dev/build tool, not a runtime dependency
- Keep in `devDependencies` at version "6.14.0"
- Note: `@prisma/client` stays in dependencies (it IS a runtime dependency)
- Note: `build:vercel` uses `npx prisma@6.14.0 generate` which installs prisma on-the-fly, so removing from dependencies is safe

**After edits, verify:**
- Run `node -e "const p = require('./package.json'); console.log('exports' in p, 'files' in p, 'overrides' in p, 'prisma' in p.dependencies);"` — should print `false false false false`
- Run `npm test -- --bail` to confirm tests still pass
  </action>
  <verify>node -e "const p = require('./package.json'); const issues = []; if(p.exports) issues.push('exports'); if(p.files) issues.push('files'); if(p.overrides) issues.push('overrides'); if(p.dependencies.prisma) issues.push('prisma in deps'); if(issues.length) { console.error('Issues:', issues); process.exit(1); } console.log('Clean');"</verify>
  <done>No exports/files/overrides fields. Prisma only in devDependencies. Tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` — valid JSON
- [ ] No dangerous scripts remain (|| true, git add -A, --no-verify)
- [ ] No library-oriented fields remain (exports, files, overrides)
- [ ] `npm test -- --bail` passes
</verification>

<success_criteria>

- All dangerous/duplicate/obsolete scripts removed
- Library metadata fields removed
- Prisma deduplication done
- Tests still pass
- Valid package.json
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance/09-01-SUMMARY.md`
</output>
