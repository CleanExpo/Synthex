---
phase: 47-benchmark-reports
plan: 02
type: execute
---

<objective>
Create UI components and dashboard page for benchmark comparison visualization.

Purpose: Let creators visually see how they compare to industry standards.
Output: Comparison gauges, platform cards, and benchmark dashboard.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-benchmark-reports/47-01-SUMMARY.md

**From 47-01:**
@hooks/useBenchmarks.ts (benchmark data hook)
@app/api/analytics/benchmarks/route.ts (benchmark API)
@lib/analytics/benchmark-service.ts (benchmark service)

**UI patterns to follow:**
@app/dashboard/content/performance/page.tsx (dashboard layout)
@components/performance/PerformanceOverview.tsx (stats cards)
@components/audience/BestTimesHeatmap.tsx (custom visualization)

**Established patterns:**
- Glassmorphic cards (bg-gray-900/50, border-white/10)
- Recharts for visualizations
- PageHeader with filters
- Loading skeletons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create benchmark visualization components</name>
  <files>components/benchmarks/BenchmarkGauge.tsx, components/benchmarks/PlatformBenchmarkCard.tsx, components/benchmarks/BenchmarkOverview.tsx</files>
  <action>
Create benchmark visualization components:

**BenchmarkGauge.tsx:**
- Semi-circular gauge showing percentile (0-100)
- Color zones: red (0-25), yellow (25-50), green (50-75), cyan (75-100)
- Center shows rating text (below/average/good/excellent)
- Label below with metric name

**PlatformBenchmarkCard.tsx:**
- Platform icon and name header
- List of metric comparisons:
  - Metric name
  - User value vs benchmark average
  - Mini progress bar showing percentile
  - Delta badge (+/-%)
- Overall platform rating badge

**BenchmarkOverview.tsx:**
- Overall score gauge (large, centered)
- Stats row: platforms analyzed, metrics compared, top performer badge
- Quick insights list

All components:
- Handle loading with skeletons
- Handle empty state gracefully
- Glassmorphic styling
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>Benchmark components display gauges, cards, and overview</done>
</task>

<task type="auto">
  <name>Task 2: Create recommendations component</name>
  <files>components/benchmarks/BenchmarkRecommendations.tsx</files>
  <action>
Create component to display actionable recommendations:

```typescript
interface BenchmarkRecommendationsProps {
  insights: string[];
  recommendations: string[];
  isLoading?: boolean;
}
```

**Layout:**
- Two sections: "Key Insights" and "Recommendations"
- Insights: Bulleted list with info icons
- Recommendations: Numbered list with action icons
- Each recommendation has an impact indicator (high/medium)

**Styling:**
- Insights: Blue/cyan theme
- Recommendations: Green theme for actions
- Glassmorphic container
  </action>
  <verify>Recommendations render with proper styling</verify>
  <done>Recommendations panel displays insights and actions</done>
</task>

<task type="auto">
  <name>Task 3: Create benchmark dashboard page</name>
  <files>app/dashboard/analytics/benchmarks/page.tsx, app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
**page.tsx - Benchmark Reports Dashboard:**

Layout:
1. **PageHeader:** "Benchmark Reports" with platform filter, period selector
2. **Overview:** BenchmarkOverview with overall score
3. **Platform cards:** Grid of PlatformBenchmarkCard components
4. **Recommendations:** BenchmarkRecommendations panel

Use useBenchmarks hook for data.
Handle loading with skeletons, error with retry.

**layout.tsx update:**
Add to sidebarItems after "Reports":
```typescript
{ icon: Target, label: 'Benchmarks', href: '/dashboard/analytics/benchmarks' },
```

**CommandPalette.tsx update:**
Add command:
```typescript
{
  id: 'benchmark-reports',
  title: 'Benchmark Reports',
  description: 'Compare your performance to industry standards',
  icon: Target,
  action: () => router.push('/dashboard/analytics/benchmarks'),
  category: 'navigation',
  keywords: ['benchmark', 'compare', 'industry', 'standards', 'performance', 'percentile']
}
```
  </action>
  <verify>Navigate to /dashboard/analytics/benchmarks, see loading then data</verify>
  <done>Benchmark dashboard accessible via sidebar and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] Gauge renders with correct colors
- [ ] Platform cards show comparisons
- [ ] Dashboard page loads with all sections
- [ ] Navigation integrated (sidebar + command palette)
</verification>

<success_criteria>
- All tasks completed
- Gauges visualize percentile data
- Comparisons are clear and actionable
- Dashboard is accessible and functional
- Phase 47 complete
</success_criteria>

<output>
After completion, create `.planning/phases/47-benchmark-reports/47-02-SUMMARY.md`
</output>
