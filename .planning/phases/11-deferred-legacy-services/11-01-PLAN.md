---
phase: 11-deferred-legacy-services
plan: 01
type: execute
model: sonnet
---

<objective>
Remove legacy src/services files that contain mock data and are not imported by the active app/api codebase.

Purpose: Eliminate dead code that creates confusion and technical debt. These files have mock data (Math.random) that would cause issues if accidentally imported.
Output: Clean src/services directory with only actively-used code remaining.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior decisions:**
- v1.0 established that legacy src/ services are not used by app/api routes
- crypto.randomUUID() replaced all Math.random() ID generation in app/api
- All active platform services live in lib/social/

**Files confirmed dead (not imported by app/api):**
- src/services/analytics.service.js — 100% mock data (Math.random)
- src/services/dashboard-service.ts — Not imported
- src/services/competitor-analysis.js — Broken imports (references non-existent files)
- src/services/white-label.js — Broken imports (references non-existent files)

**Files actively imported by app/api (DO NOT REMOVE):**
- @/src/infrastructure/caching/* — used by app/api/cache/route.ts
- @/src/infrastructure/logging/* — used by app/api/cache/route.ts
- @/src/middleware/cache-middleware — used by 2 routes
- @/src/middleware/session — used by 2 routes
- @/src/middleware/rate-limit — used by 1 route
- @/src/middleware/enhanced-rate-limit — used by 2 routes
- @/src/services/ai/content-variations — used by 1 route
- @/src/services/ai/persona-training-pipeline — used by 1 route
- @/src/services/content/calendar-service — used by 2 routes
- @/src/config/scaling.config — used by 1 route
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 4 confirmed dead legacy services</name>
  <files>
    src/services/analytics.service.js,
    src/services/dashboard-service.ts,
    src/services/competitor-analysis.js,
    src/services/white-label.js
  </files>
  <action>
    Delete the 4 legacy service files that are confirmed not imported by any app/api routes:

    1. `git rm src/services/analytics.service.js` — Contains 100% mock data (Math.random)
    2. `git rm src/services/dashboard-service.ts` — Not imported, duplicates lib/api/dashboard.ts
    3. `git rm src/services/competitor-analysis.js` — Broken imports, dead code
    4. `git rm src/services/white-label.js` — Broken imports, dead code

    Also remove any related .disabled variants:
    - `git rm src/services/analytics.service.ts.disabled` if exists

    Commit: "chore(11-01): remove 4 legacy services with mock data"
  </action>
  <verify>
    - `ls src/services/analytics.service.* 2>/dev/null` returns nothing
    - `ls src/services/dashboard-service.ts 2>/dev/null` returns nothing
    - `ls src/services/competitor-analysis.js 2>/dev/null` returns nothing
    - `ls src/services/white-label.js 2>/dev/null` returns nothing
    - `npm run type-check` passes (no broken imports)
  </verify>
  <done>4 legacy services removed, no broken imports introduced</done>
</task>

<task type="auto">
  <name>Task 2: Audit and remove other dead src/services files</name>
  <files>src/services/*</files>
  <action>
    Scan remaining src/services/ files and remove any that are:
    1. Not imported by app/api routes (grep confirms no imports)
    2. Have broken imports to non-existent files
    3. Contain mock data patterns (Math.random, hardcoded arrays)

    **Files to audit:**
    - src/services/ab-testing.js
    - src/services/advanced-scheduler.js
    - src/services/automated-reporting.js
    - src/services/content-library.js
    - src/services/mobile-api.js
    - src/services/team-collaboration.js
    - src/services/translation.js

    **Audit process for each file:**
    1. `grep -r "from.*[file]" app/` — check if imported
    2. Read first 50 lines — check for broken imports
    3. `grep "Math.random" [file]` — check for mock data

    **KEEP files that are:**
    - Imported by app/api routes (see context list above)
    - Part of actively-used subsystems

    Remove files that fail all 3 checks. Use `git rm` for each removal.

    Commit: "chore(11-01): remove additional dead src/services files"
  </action>
  <verify>
    - `npm run type-check` passes
    - All actively-imported src/ files still exist
    - No broken imports in app/api routes
  </verify>
  <done>Dead src/services files removed, all actively-used files preserved</done>
</task>

<task type="auto">
  <name>Task 3: Document remaining src/ dependencies for future migration</name>
  <files>.planning/phases/11-deferred-legacy-services/SRC-DEPENDENCIES.md</files>
  <action>
    Create a dependency map documenting which src/ files are still actively imported by app/api.

    Format:
    ```markdown
    # src/ Dependencies (for Phase 12+ migration)

    ## Actively Imported by app/api

    | src/ File | Imported By | Migration Priority |
    |-----------|-------------|-------------------|
    | src/infrastructure/caching/cache.factory.ts | app/api/cache/route.ts | Low |
    | src/middleware/enhanced-rate-limit.ts | app/api/generate/route.ts, app/api/rate-limit/route.ts | Medium |
    | src/services/ai/content-variations.ts | app/api/content/variations/route.ts | High |
    ...

    ## Migration Notes
    - Infrastructure files (caching, logging) could move to lib/infrastructure/
    - Middleware files could move to lib/middleware/ or middleware.ts
    - Service files could merge with existing lib/ services

    ## Files Removed in Phase 11
    - src/services/analytics.service.js (mock data)
    - src/services/dashboard-service.ts (dead)
    - src/services/competitor-analysis.js (broken imports)
    - src/services/white-label.js (broken imports)
    ```

    Run `grep -r "from.*src/" app/` to get complete import list.

    No commit for this file (planning artifact).
  </action>
  <verify>
    - SRC-DEPENDENCIES.md exists in phase directory
    - All actively-imported files are documented
    - Migration priorities assigned
  </verify>
  <done>Dependency map created for future migration work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] `npm test -- --bail` passes (no broken tests)
- [ ] No app/api routes have broken imports
- [ ] SRC-DEPENDENCIES.md documents remaining dependencies
- [ ] All removed files were confirmed dead before deletion
</verification>

<success_criteria>

- 4+ legacy services with mock data removed
- No broken imports introduced
- All actively-used src/ files preserved
- Dependency map created for Phase 12+
- Clean git history with descriptive commits
</success_criteria>

<output>
After completion, create `.planning/phases/11-deferred-legacy-services/11-01-SUMMARY.md` with:

```yaml
---
phase: 11-deferred-legacy-services
plan: 01
status: complete
subsystem: legacy-cleanup
tags: [src-services, mock-data, dead-code]
key-files: [SRC-DEPENDENCIES.md]
affects: [12-deferred-components]
---
```

Include:
- Files removed (with reasons)
- Files preserved (with reasons)
- Dependency map summary
- Any unexpected findings
</output>
