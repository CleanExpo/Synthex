# Phase 108, Plan 02 — Platform & Workflow Pages Coverage

## Objective

Add E2E smoke coverage for platform/integration/workflow dashboard pages added in
v3.0–v4.0 that currently have zero test coverage.

**Linear**: SYN-374 (v7.0 Production Hardening & Quality)

## Context

### Current State (after Plan 108-01)
- ≥183 tests: 0 failed, 0 flaky
- intelligence-pages.spec.ts covers 5 AI/GEO/citation pages
- Still no coverage for: platforms, integrations, workflows, sentinel, authority, backlinks, billing

### Target Pages (no current E2E tests)
| Page | Path | Priority |
|------|------|----------|
| Platforms | `/dashboard/platforms` | High — core social connection flow |
| Integrations | `/dashboard/integrations` | High — Zapier/Buffer/Canva |
| Workflows | `/dashboard/workflows` | High — workflow engine (Phase 62-66) |
| Authority | `/dashboard/authority` | Medium — E-E-A-T authority builder |
| Backlinks | `/dashboard/backlinks` | Medium — link prospector |
| Algorithm Sentinel | `/dashboard/sentinel` | Medium — algorithm change detector |
| Billing | `/dashboard/billing` | High — Stripe subscription page |
| Reports | `/dashboard/reports` | Medium — report engine |

### Key Patterns
- Same `setDashboardAuth` from `tests/e2e/fixtures/dashboard.fixture.ts`
- Billing page: mock `/api/user/subscription` → `{ plan: 'free', status: 'active' }`
- Workflows page: mock `/api/workflows**` → `{ workflows: [], total: 0 }`
- Platforms page: mock `/api/platforms**` and `/api/connections**` → empty arrays

## Execution Context

```
tests/e2e/
├── fixtures/dashboard.fixture.ts     # Import setDashboardAuth from here
├── platform-pages.spec.ts            # NEW — platform + integration + billing
└── workflow-pages.spec.ts            # NEW — workflows + authority + backlinks + sentinel
```

## Tasks

### Task 1: Create platform-pages.spec.ts

Create `tests/e2e/platform-pages.spec.ts` covering:

**Platforms page** (`/dashboard/platforms`):
- Renders without 500 error
- Shows main content (not blank)
- Has heading (h1/h2/h3)
- Has "Connect" or "Add Platform" button OR shows connected platforms list

**Integrations page** (`/dashboard/integrations`):
- Renders without 500 error
- Shows main content
- Has heading
- Has at least one integration card or "Connect" element

**Billing page** (`/dashboard/billing`):
- Renders without 500 error
- Shows subscription info or upgrade CTA
- Has heading

Mock these APIs:
```typescript
await page.route('**/api/platforms**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ platforms: [], connections: [] }) }))
await page.route('**/api/connections**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ connections: [] }) }))
await page.route('**/api/integrations**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ integrations: [] }) }))
await page.route('**/api/user/subscription**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ plan: 'free', status: 'active' }) }))
```

### Task 2: Create workflow-pages.spec.ts

Create `tests/e2e/workflow-pages.spec.ts` covering:

**Workflows page** (`/dashboard/workflows`):
- Renders without 500 error
- Shows main content
- Has heading
- Has "Create Workflow" or "New Workflow" button OR shows empty state

**Reports page** (`/dashboard/reports`):
- Renders without 500 error
- Shows main content
- Has heading

**Authority page** (`/dashboard/authority`):
- Renders without 500 error
- Shows main content
- Has heading

**Backlinks page** (`/dashboard/backlinks`):
- Renders without 500 error
- Shows main content
- Has heading

**Sentinel page** (`/dashboard/sentinel`):
- Renders without 500 error
- Shows main content
- Has heading

Mock these APIs:
```typescript
await page.route('**/api/workflows**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ workflows: [], total: 0 }) }))
await page.route('**/api/reports**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ reports: [] }) }))
await page.route('**/api/authority**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ score: 0, factors: [] }) }))
await page.route('**/api/backlinks**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ backlinks: [], total: 0 }) }))
await page.route('**/api/sentinel**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ alerts: [], status: 'active' }) }))
```

## Verification

```bash
PW_SKIP_WEBSERVER=1 BASE_URL=http://localhost:3002 npx playwright test tests/e2e/platform-pages.spec.ts --reporter=line

PW_SKIP_WEBSERVER=1 BASE_URL=http://localhost:3002 npx playwright test tests/e2e/workflow-pages.spec.ts --reporter=line

# Full suite to confirm no regressions
PW_SKIP_WEBSERVER=1 BASE_URL=http://localhost:3002 npx playwright test --reporter=line
```

**Success criteria:**
- platform-pages.spec.ts: ≥9 tests, 0 failed
- workflow-pages.spec.ts: ≥15 tests, 0 failed
- Full suite: ≥200 tests, 0 failed, 0 flaky
- All new page tests confirm pages don't 500

## Output

- Created: `tests/e2e/platform-pages.spec.ts`
- Created: `tests/e2e/workflow-pages.spec.ts`
- Commit: `test(e2e): add platform and workflow page coverage [SYN-374]`

## Phase 108 Complete

After Plan 02, update:
- `.planning/STATE.md` — Phase 108 Done, progress 1/6
- `.planning/ROADMAP.md` — Phase 108 checked off
- `.claude/scratchpad/current-session.md` — session notes
- Linear SYN-374 — add comment with test count
