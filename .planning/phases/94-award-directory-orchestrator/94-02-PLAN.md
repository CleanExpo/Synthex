# Plan 94-02: Dashboard UI + Navigation

**Phase:** 94 — Award & Directory Orchestrator
**Goal:** React components, dashboard page, sidebar entry, command palette entries

---

## Tasks

### Task 1 — AwardCard component
`components/awards/AwardCard.tsx`
- Status badge (researched / in-progress / submitted / won / shortlisted / not-selected)
- Deadline countdown (days until deadline)
- "Generate Nomination" button
- Priority indicator (low / medium / high)

### Task 2 — DirectoryCard component
`components/awards/DirectoryCard.tsx`
- Directory name + DA badge
- AI-indexed indicator chip
- Status badge (identified / submitted / live / needs-update / rejected)
- Free/Paid tag

### Task 3 — NominationEditor component
`components/awards/NominationEditor.tsx`
- Textarea with AI-generated nomination text
- Copy to clipboard button
- Word count display
- "Regenerate" button

### Task 4 — SubmissionDeadlineList component
`components/awards/SubmissionDeadlineList.tsx`
- Sorted list of upcoming deadlines within 90 days
- Colour-coded urgency (red < 7d, amber < 30d, green rest)
- Empty state if no upcoming deadlines

### Task 5 — AwardTemplateGrid component
`components/awards/AwardTemplateGrid.tsx`
- Grid of AWARD_TEMPLATES cards
- Click to open "Add Award" pre-filled form
- Filter by country (Australia / Global)

### Task 6 — DirectoryTemplateGrid component
`components/awards/DirectoryTemplateGrid.tsx`
- Grid of DIRECTORY_TEMPLATES cards
- Click to add directly to tracker
- AI-indexed filter toggle

### Task 7 — Dashboard page
`app/dashboard/awards/page.tsx`
- `'use client'`, Radix Tabs
- Tab 1 — Awards: AwardCard list + "Add Award" modal form
- Tab 2 — Directories: DirectoryCard list + "Add Directory" modal form
- Tab 3 — Deadlines: SubmissionDeadlineList
- Tab 4 — Templates: AwardTemplateGrid + DirectoryTemplateGrid side by side
- SWR data fetching with credentials: 'include'
- Glassmorphic header card

### Task 8 — Sidebar entry
`app/dashboard/layout.tsx`
- Import `Trophy` from `@/components/icons` (already exported as `Award` alias, use `Award` icon)
- Add to `pr-manager` group: `{ icon: Award, label: 'Awards & Directories', href: '/dashboard/awards' }`

### Task 9 — Command palette entries
`components/command-palette/commands.ts`
- Add `Trophy` / `Award` icon import
- Add 2 entries after PR Manager section:
  - `{ id: 'awards-tracker', title: 'Awards & Directories — Tracker', ... }`
  - `{ id: 'awards-deadlines', title: 'Awards & Directories — Deadlines', ... }`

---

## Verification

```bash
npm run type-check
```
