---
phase: 67-codebase-cleanup
plan: 01
type: execute
model: haiku
model_rationale: "Plan 67-01 (25% context) removes two confirmed-unused packages. Complexity score: 1.5 (25% × standard ×1 × low-risk ×0.8 − exact pattern exists −1 = 1.5). Routing: Haiku ✅"
---

<objective>
Remove @tanstack/react-table (zero source imports confirmed) and @tanstack/react-query +
its QueryClientProvider wrapper (package used only as a no-op wrapper — all actual query
logic lives in the custom hooks/use-api.ts).

Purpose: Reduce bundle size, eliminate dependency drift, and prevent future confusion
between the custom use-api.ts hooks and the real react-query library.
Output: Leaner package.json, simplified providers.tsx, passing type-check and tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/providers.tsx
@hooks/use-api.ts
@hooks/index.ts

**Confirmed findings (pre-plan research):**
- `@tanstack/react-table`: zero imports in any .ts/.tsx source file → safe to uninstall
- `@tanstack/react-query`: imported ONLY in `app/providers.tsx` as `QueryClientProvider` wrapper.
  No component calls `useQuery`, `useMutation`, or `useInfiniteQuery` from react-query —
  all 7 hook call-sites use the custom `hooks/use-api.ts` exports instead.
- `canvas-confetti`: legitimately used in `app/(onboarding)/onboarding/complete/page.tsx:119`
  via dynamic import — DO NOT REMOVE.
- `types/canvas-confetti.d.ts`: local type file for the above — DO NOT REMOVE.

**Architecture rule:** Auth is Supabase ONLY. Do not touch any auth-related providers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall @tanstack/react-table and @tanstack/react-query</name>
  <files>package.json, package-lock.json (auto-updated)</files>
  <action>
    Run: npm uninstall @tanstack/react-table @tanstack/react-query

    Both packages confirmed unused in source:
    - react-table: zero imports anywhere
    - react-query: only used as QueryClientProvider wrapper with no actual query calls

    Do NOT remove: canvas-confetti, @types/canvas-confetti, swr — all legitimately used.
  </action>
  <verify>
    cat package.json | grep -E "react-table|react-query"
    Expected: no matches (both removed)
  </verify>
  <done>Neither package appears in package.json dependencies or devDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Remove QueryClientProvider from providers.tsx</name>
  <files>app/providers.tsx</files>
  <action>
    Remove from providers.tsx:
    1. The import: `import { QueryClient, QueryClientProvider } from '@tanstack/react-query';`
    2. The `useState` for `queryClient` (lines 12-21 — the QueryClient instantiation)
    3. The `<QueryClientProvider client={queryClient}>` wrapper and its closing tag

    The remaining provider tree should be:
      LenisProvider > ThemeProvider > AuthProvider > TooltipProvider > children

    Keep ALL other providers intact: LenisProvider, ThemeProvider, AuthProvider,
    TooltipProvider. Do NOT touch the mounted/setMounted hydration guard.

    After editing, run: npm run type-check
    Expected: 0 errors
  </action>
  <verify>
    npm run type-check
    grep -n "QueryClient\|react-query" app/providers.tsx
    Expected: type-check passes, no react-query references remain
  </verify>
  <done>
    providers.tsx has no QueryClient/react-query imports or usage.
    npm run type-check passes with 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify tests pass and commit</name>
  <files>— (verification only)</files>
  <action>
    Run: npm test
    Expected: same pass count as before (≥1506 passing), 0 new failures.

    If tests pass, commit:
    git add package.json package-lock.json app/providers.tsx
    git commit -m "chore(67-01): remove unused @tanstack/react-table and react-query packages

    - @tanstack/react-table: zero source imports confirmed
    - @tanstack/react-query: only used as no-op QueryClientProvider wrapper;
      all query logic lives in custom hooks/use-api.ts
    - providers.tsx simplified: QueryClientProvider wrapper removed

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  </action>
  <verify>npm test (≥1506 passing, 0 new failures)</verify>
  <done>
    Tests pass, commit created.
    Bundle is leaner by two unused packages.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm uninstall succeeded — both packages absent from package.json
- [ ] npm run type-check passes (0 errors)
- [ ] npm test passes (≥1506, no regressions)
- [ ] providers.tsx has no QueryClient references
- [ ] canvas-confetti, swr, and all other packages untouched
</verification>

<success_criteria>

- @tanstack/react-table removed from package.json
- @tanstack/react-query removed from package.json
- providers.tsx compiles cleanly without QueryClientProvider
- All tests pass
- No other packages affected
  </success_criteria>

<output>
After completion, create `.planning/phases/67-codebase-cleanup/67-01-SUMMARY.md`:

# Phase 67 Plan 01: Package Cleanup Summary

**[Substantive one-liner]**

## Accomplishments

- [What was removed and why]

## Files Created/Modified

- `package.json` — removed packages
- `app/providers.tsx` — removed QueryClientProvider

## Decisions Made

[Any decisions about what to keep/remove]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 67-02-PLAN.md — Orphaned API Route Audit
</output>
