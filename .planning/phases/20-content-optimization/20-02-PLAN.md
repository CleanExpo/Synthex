---
phase: 20-content-optimization
plan: 02
type: execute
---

<objective>
Build a content optimization dashboard page that shows real-time quality scores and AI-powered improvement suggestions as users draft posts, integrating template suggestions from Phase 19.

Purpose: Give users a dedicated workspace to draft, score, and optimize content before publishing.
Output: Dashboard page at /dashboard/content/optimize with real-time scoring UI.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-content-optimization/20-01-SUMMARY.md

# Key reference files:
@lib/ai/content-scorer.ts
@app/api/content/score/route.ts
@hooks/useContentScore.ts
@components/AIWritingAssistant.tsx
@app/api/ai-content/optimize/route.ts
@app/api/templates/route.ts

**Tech stack:** Next.js App Router, Radix UI, Tailwind CSS, Framer Motion, Lucide icons
**UI patterns:** Glassmorphic styling (bg-white/5, bg-[#0f172a]/80, border-cyan-500/10), PageHeader + DashboardEmptyState components
**Established patterns:**
- Dashboard pages use 'use client' with fetch-based data loading
- Cards use Radix UI Card components with glassmorphic backgrounds
- Loading states in loading.tsx, error boundaries in error.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create content optimization dashboard page</name>
  <files>app/dashboard/content/optimize/page.tsx, app/dashboard/content/optimize/loading.tsx, app/dashboard/content/optimize/error.tsx</files>
  <action>
Create /dashboard/content/optimize page with:

**Layout (left/right split on desktop, stacked on mobile):**
- Left panel (60%): Content editor textarea with platform selector and goal selector
- Right panel (40%): Real-time score display

**Left panel:**
- Platform selector (dropdown): twitter, linkedin, instagram, tiktok, facebook, youtube
- Goal selector (dropdown): engagement, reach, conversions, brand_awareness, traffic
- Large textarea for content drafting (min 6 rows)
- Character count with platform limit indicator (green/yellow/red)
- "Optimize with AI" button that calls POST /api/ai-content/optimize and replaces content

**Right panel (real-time, updates as user types):**
- Overall score circle (large, color-coded: red <40, yellow 40-70, green >70)
- 5 dimension bars (readability, engagement, platformFit, clarity, emotional) with score labels
- Top 3 suggestions list with lightbulb icons
- "Suggested templates" section: fetch templates matching the selected category/platform from /api/templates and show top 3

**Template suggestions:**
- Fetch templates from /api/templates?category=marketing&platform=twitter (match user's selected platform)
- Show template name, icon, and "Use template" button
- "Use template" pre-fills the textarea with template structure (replacing variables with placeholders)

Use useContentScore hook from hooks/useContentScore.ts for debounced scoring.
Use glassmorphic styling from existing dashboard pages.
Use PageHeader component for page title.
Create loading.tsx and error.tsx following DashboardError pattern.

Reference components/AIWritingAssistant.tsx for UI patterns but DO NOT import or extend it — this is a separate page.
  </action>
  <verify>
npm run type-check passes.
Page file exists at app/dashboard/content/optimize/page.tsx with 'use client' directive.
  </verify>
  <done>Dashboard page renders with content editor, platform/goal selectors, real-time score display, AI optimize button, and template suggestions. Loading and error states present.</done>
</task>

<task type="auto">
  <name>Task 2: Add content optimization to navigation and command palette</name>
  <files>components/DashboardNav.tsx (or equivalent nav component), components/CommandPalette.tsx</files>
  <action>
Add "Content Optimizer" link to the dashboard navigation under the Content section.

Find the navigation component by searching for existing nav items like "/dashboard/content". Add a new entry:
- Label: "Content Optimizer"
- Path: /dashboard/content/optimize
- Icon: Sparkles (from lucide-react / icons barrel)

Add to CommandPalette.tsx:
- Command: "Content Optimizer" or "Optimize Content"
- Action: navigate to /dashboard/content/optimize
- Keywords: optimize, score, quality, improve, suggestions

Follow the exact pattern of existing nav items and command palette entries.
  </action>
  <verify>
npm run type-check passes.
Grep for "/dashboard/content/optimize" finds entries in both navigation and command palette files.
  </verify>
  <done>Content Optimizer appears in dashboard navigation and command palette, linking to /dashboard/content/optimize.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes (no new errors)
- [ ] /dashboard/content/optimize page exists with real-time scoring
- [ ] Navigation includes Content Optimizer link
- [ ] Command palette includes optimize content command
- [ ] Page uses glassmorphic styling consistent with existing dashboard
</verification>

<success_criteria>

- All tasks completed and committed
- Content optimization page shows real-time scores as user types
- AI optimization button works (calls existing optimize API)
- Template suggestions displayed and usable
- Navigation and command palette updated
- No TypeScript errors introduced
- Phase 20 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/20-content-optimization/20-02-SUMMARY.md`

Update .planning/STATE.md and .planning/ROADMAP.md to mark Phase 20 complete.
</output>
