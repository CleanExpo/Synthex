---
phase: 12-deferred-components
plan: 03
type: execute
model: sonnet
---

<objective>
Wire analytics components (PredictiveAnalytics, CompetitorAnalysis, ROICalculator) to real API endpoints.

Purpose: Remove mock data patterns and connect analytics components to production APIs.
Output: Three components using real API calls with proper data visualization.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deferred-components/12-02-SUMMARY.md

**Prior decisions:**
- APIs exist: /api/predict/trends, /api/intelligence/competitors, /api/analytics/*
- Components are standalone (not imported by dashboard pages)
- Pattern: Replace setTimeout/mock functions with fetch() to real endpoints

**Key files:**
- components/PredictiveAnalytics.tsx
- components/CompetitorAnalysis.tsx
- components/ROICalculator.tsx
- app/api/predict/trends/route.ts
- app/api/analytics/predict-engagement/route.ts
- app/api/intelligence/competitors/route.ts
- app/api/analytics/performance/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PredictiveAnalytics to real API</name>
  <files>components/PredictiveAnalytics.tsx</files>
  <action>
    1. Read the component to identify mock patterns (Math.random for predictions, mock trend data)
    2. Replace mock prediction operations with fetch calls:
       - GET /api/predict/trends - Get trend predictions
       - POST /api/analytics/predict-engagement - Predict engagement for content
       - GET /api/analytics/insights - Get AI-powered insights
    3. Add proper error handling with try/catch
    4. Handle loading states for charts/visualizations
    5. Remove all mock data generation functions

    Reference: app/api/predict/trends/route.ts for request/response format
  </action>
  <verify>
    - grep -i "mock\|Math.random" components/PredictiveAnalytics.tsx returns no matches
    - grep "setTimeout" components/PredictiveAnalytics.tsx returns no matches
    - grep "fetch.*predict\|fetch.*analytics" components/PredictiveAnalytics.tsx returns matches
  </verify>
  <done>PredictiveAnalytics uses real prediction API endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Wire CompetitorAnalysis to real API</name>
  <files>components/CompetitorAnalysis.tsx</files>
  <action>
    1. Read the component to identify mock patterns
    2. Replace mock competitor data with fetch calls:
       - GET /api/competitors - List tracked competitors
       - POST /api/competitors - Add competitor
       - GET /api/competitors/[id]/metrics - Get competitor metrics
       - GET /api/intelligence/competitors - Get competitive intelligence
    3. Add proper error handling
    4. Handle loading states for comparison charts
    5. Remove mock data arrays and random metrics

    Reference: app/api/intelligence/competitors/route.ts for format
  </action>
  <verify>
    - grep -i "mock\|Math.random" components/CompetitorAnalysis.tsx returns no matches
    - grep "setTimeout" components/CompetitorAnalysis.tsx returns no matches
    - grep "fetch.*competitors\|fetch.*intelligence" components/CompetitorAnalysis.tsx returns matches
  </verify>
  <done>CompetitorAnalysis uses real /api/competitors and /api/intelligence endpoints</done>
</task>

<task type="auto">
  <name>Task 3: Wire ROICalculator to real API</name>
  <files>components/ROICalculator.tsx</files>
  <action>
    1. Read the component to identify mock patterns
    2. Wire ROI calculations to real data sources:
       - GET /api/analytics/performance - Get actual performance metrics
       - GET /api/stats - Get platform statistics
       - Calculate ROI from real engagement/conversion data

    If no dedicated ROI API exists:
    - Create calculation from existing metrics endpoints
    - Fetch real data: followers, engagement, conversions, costs
    - Calculate: ROI = (gains - costs) / costs * 100

    3. Add proper error handling
    4. Display "insufficient data" state when metrics unavailable
    5. Remove mock calculations and hardcoded values
  </action>
  <verify>
    - grep -i "mock\|Math.random" components/ROICalculator.tsx returns no matches
    - grep "setTimeout" components/ROICalculator.tsx returns no matches (or only UI debounce)
    - Component fetches real data from API endpoints
  </verify>
  <done>ROICalculator uses real performance/stats data for calculations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 components have no mock data patterns
- [ ] All 3 components use real data from API endpoints
- [ ] `npm run type-check` passes
- [ ] No new TypeScript errors introduced
</verification>

<success_criteria>

- PredictiveAnalytics.tsx wired to prediction APIs
- CompetitorAnalysis.tsx wired to competitor/intelligence APIs
- ROICalculator.tsx calculates from real performance data
- All mock patterns removed
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-deferred-components/12-03-SUMMARY.md` with:

```yaml
---
phase: 12-deferred-components
plan: 03
status: complete
subsystem: components
tags: [analytics-components, api-wiring, mock-removal]
key-files: [components/PredictiveAnalytics.tsx, components/CompetitorAnalysis.tsx, components/ROICalculator.tsx]
affects: [12-04-rate-limiters]
---
```
</output>
