---
phase: 12-deferred-components
plan: 04
type: execute
model: sonnet
---

<objective>
Consolidate rate limiter implementations into a single, unified structure.

Purpose: Eliminate 11 duplicate/overlapping rate limiter files, create single source of truth.
Output: Clean lib/rate-limit/ directory with unified implementation, updated imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deferred-components/12-03-SUMMARY.md
@.planning/phases/11-deferred-legacy-services/SRC-DEPENDENCIES.md

**Current rate limiter files (11 total):**

In lib/:
- lib/rate-limit.ts
- lib/rate-limiter-enhanced.ts
- lib/middleware/rate-limiter.ts
- lib/middleware/rate-limiter-v2.ts
- lib/middleware/api-rate-limit.ts
- lib/security/rate-limiter-redis.ts
- lib/scalability/redis-rate-limiter.ts

In src/ (actively imported):
- src/middleware/rate-limit.ts (used by 1 route)
- src/middleware/enhanced-rate-limit.ts (used by 2 routes)
- src/middleware/rate-limiter.js
- src/middleware/rate-limiter.ts

**Files importing rate limiters:**
- app/api/example/redis-demo/route.ts → src/middleware/rate-limit, src/middleware/session
- app/api/generate/route.ts → src/middleware/enhanced-rate-limit
- app/api/rate-limit/route.ts → src/middleware/enhanced-rate-limit
- Various routes → lib/middleware/api-rate-limit

**Target structure:**
```
lib/rate-limit/
  index.ts           # Main exports
  rate-limiter.ts    # Core implementation
  presets.ts         # Rate limit presets (authStrict, readDefault, etc.)
  redis-adapter.ts   # Redis-backed rate limiting
  memory-adapter.ts  # In-memory fallback
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and identify canonical rate limiter</name>
  <files>lib/middleware/api-rate-limit.ts, src/middleware/enhanced-rate-limit.ts</files>
  <action>
    1. Read all 11 rate limiter files to understand implementations:
       - Which use Redis? Which use in-memory?
       - What presets exist? (authStrict, readDefault, etc.)
       - Which are actively imported by app/api routes?

    2. Identify the most complete implementation:
       - lib/middleware/api-rate-limit.ts appears to be primary (used by routes)
       - src/middleware/enhanced-rate-limit.ts has presets

    3. Document in task output:
       - Files to KEEP as base
       - Files to MERGE features from
       - Files to DELETE (unused duplicates)

    Do NOT make changes yet - this is audit only.
  </action>
  <verify>
    - All 11 files have been read and analyzed
    - Clear list of keep/merge/delete files documented
  </verify>
  <done>Rate limiter audit complete with consolidation plan</done>
</task>

<task type="auto">
  <name>Task 2: Create unified rate-limit structure</name>
  <files>
    lib/rate-limit/index.ts,
    lib/rate-limit/rate-limiter.ts,
    lib/rate-limit/presets.ts
  </files>
  <action>
    1. Create lib/rate-limit/ directory
    2. Create unified implementation in lib/rate-limit/rate-limiter.ts:
       - Use best patterns from existing implementations
       - Support Redis (primary) and in-memory (fallback)
       - Type-safe rate limiter factory

    3. Create lib/rate-limit/presets.ts with existing categories:
       - authStrict: 5/min (login, register)
       - authDefault: 10/min
       - writeStrict: 20/min
       - writeDefault: 30/min
       - readStrict: 60/min
       - readDefault: 120/min
       - aiGeneration: 10/min

    4. Create lib/rate-limit/index.ts exporting all utilities

    Reference existing patterns from:
    - src/middleware/enhanced-rate-limit.ts (presets)
    - lib/middleware/api-rate-limit.ts (middleware pattern)
  </action>
  <verify>
    - ls lib/rate-limit/ shows index.ts, rate-limiter.ts, presets.ts
    - npm run type-check passes for new files
  </verify>
  <done>Unified rate-limit structure created</done>
</task>

<task type="auto">
  <name>Task 3: Update imports and remove duplicates</name>
  <files>
    app/api/generate/route.ts,
    app/api/rate-limit/route.ts,
    app/api/example/redis-demo/route.ts
  </files>
  <action>
    1. Update consuming routes to import from lib/rate-limit:
       - app/api/generate/route.ts: import { aiGeneration } from '@/lib/rate-limit'
       - app/api/rate-limit/route.ts: import { getRateLimitStatus, resetRateLimits } from '@/lib/rate-limit'
       - Update any other routes using old imports

    2. Remove redundant files (git rm):
       - lib/rate-limit.ts (moved to lib/rate-limit/)
       - lib/rate-limiter-enhanced.ts (consolidated)
       - lib/middleware/rate-limiter.ts (if unused after update)
       - lib/middleware/rate-limiter-v2.ts (if unused)
       - src/middleware/rate-limiter.js (dead code)
       - src/middleware/rate-limiter.ts (if unused)

    3. Keep src/middleware files that are actively imported (from SRC-DEPENDENCIES.md):
       - src/middleware/rate-limit.ts
       - src/middleware/enhanced-rate-limit.ts
       These can be updated to re-export from lib/rate-limit for backward compatibility.

    4. Run type-check to verify no broken imports
  </action>
  <verify>
    - grep -r "from.*rate-limit" app/api | grep -v lib/rate-limit returns minimal matches
    - npm run type-check passes
    - Removed files no longer exist
  </verify>
  <done>Imports updated, duplicate files removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] lib/rate-limit/ structure exists with clean implementation
- [ ] All consuming routes updated to new imports
- [ ] Duplicate/redundant files removed
- [ ] `npm run type-check` passes
- [ ] Rate limiting still works (existing tests pass)
</verification>

<success_criteria>

- Single unified rate-limit implementation in lib/rate-limit/
- All routes import from @/lib/rate-limit
- Duplicate files removed (target: reduce from 11 to ~4-5 files)
- Type-check passes
- No functionality regression
</success_criteria>

<output>
After completion, create `.planning/phases/12-deferred-components/12-04-SUMMARY.md` with:

```yaml
---
phase: 12-deferred-components
plan: 04
status: complete
subsystem: middleware
tags: [rate-limiting, consolidation, cleanup]
key-files: [lib/rate-limit/index.ts, lib/rate-limit/presets.ts]
affects: []
---
```

Include:
- Files consolidated (before/after count)
- Files removed
- Routes updated
- Phase 12 complete indicator
</output>
