---
phase: 12-deferred-components
plan: 02
type: execute
model: sonnet
---

<objective>
Wire AI feature components (AIPersonaManager, AIABTesting) to real API endpoints.

Purpose: Remove mock data patterns and connect persona/testing components to production APIs.
Output: Two components using real API calls with proper state management.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deferred-components/12-01-SUMMARY.md

**Prior decisions:**
- Real APIs exist at app/api/personas, app/api/ab-testing/tests
- Components are standalone (not imported by dashboard pages)
- Pattern: Replace setTimeout/mock functions with fetch() to real endpoints

**Key files:**
- components/AIPersonaManager.tsx
- components/AIABTesting.tsx
- app/api/personas/route.ts (CRUD for personas)
- app/api/personas/[id]/optimize/route.ts (persona optimization)
- app/api/ab-testing/tests/route.ts (CRUD for tests)
- app/api/ab-testing/tests/[testId]/results/route.ts (test results)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AIPersonaManager to real API</name>
  <files>components/AIPersonaManager.tsx</files>
  <action>
    1. Read the component to identify all mock patterns
    2. Replace mock persona operations with fetch calls:
       - GET /api/personas - List personas
       - POST /api/personas - Create persona
       - PATCH /api/personas/[id] - Update persona
       - DELETE /api/personas/[id] - Delete persona
       - POST /api/personas/[id]/optimize - Optimize persona
       - POST /api/personas/[id]/train - Train persona
    3. Add proper error handling with try/catch
    4. Use SWR or React Query pattern if component already uses them
    5. Remove all mock data generation and setTimeout patterns

    Reference: app/api/personas/route.ts for request/response format
  </action>
  <verify>
    - grep -i "mock" components/AIPersonaManager.tsx returns no matches
    - grep "setTimeout" components/AIPersonaManager.tsx returns no matches
    - grep "fetch.*api/personas" components/AIPersonaManager.tsx returns matches
  </verify>
  <done>AIPersonaManager uses real /api/personas endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Wire AIABTesting to real API</name>
  <files>components/AIABTesting.tsx</files>
  <action>
    1. Read the component to identify all mock patterns (Math.random for results, mock test data)
    2. Replace mock AB testing operations with fetch calls:
       - GET /api/ab-testing/tests - List tests
       - POST /api/ab-testing/tests - Create test
       - GET /api/ab-testing/tests/[testId] - Get test details
       - PATCH /api/ab-testing/tests/[testId] - Update test
       - DELETE /api/ab-testing/tests/[testId] - Delete test
       - GET /api/ab-testing/tests/[testId]/results - Get results
    3. Add proper error handling
    4. Remove mock statistics generation (Math.random for conversion rates, etc.)
    5. Handle loading states for async operations

    Reference: app/api/ab-testing/tests/route.ts for request/response format
  </action>
  <verify>
    - grep -i "mock\|Math.random" components/AIABTesting.tsx returns no matches
    - grep "setTimeout" components/AIABTesting.tsx returns no matches (or only UI-related)
    - grep "fetch.*ab-testing" components/AIABTesting.tsx returns matches
  </verify>
  <done>AIABTesting uses real /api/ab-testing endpoints</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Both components have no mock data patterns
- [ ] Both components call real API endpoints
- [ ] `npm run type-check` passes
- [ ] No new TypeScript errors introduced
</verification>

<success_criteria>

- AIPersonaManager.tsx wired to /api/personas endpoints
- AIABTesting.tsx wired to /api/ab-testing/tests endpoints
- All mock patterns removed
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-deferred-components/12-02-SUMMARY.md` with:

```yaml
---
phase: 12-deferred-components
plan: 02
status: complete
subsystem: components
tags: [ai-components, api-wiring, mock-removal]
key-files: [components/AIPersonaManager.tsx, components/AIABTesting.tsx]
affects: [12-03-components]
---
```
</output>
