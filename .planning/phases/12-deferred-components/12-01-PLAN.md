---
phase: 12-deferred-components
plan: 01
type: execute
model: sonnet
---

<objective>
Wire AI content components (AIHashtagGenerator, SentimentAnalysis, AIWritingAssistant) to real API endpoints.

Purpose: Remove mock data patterns and connect components to production APIs that already exist.
Output: Three components using real API calls with proper loading/error states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-deferred-legacy-services/SRC-DEPENDENCIES.md

**Prior decisions:**
- Real APIs exist at app/api/ai-content/hashtags, app/api/analytics/sentiment, app/api/generate
- Components are standalone (not imported by dashboard pages)
- Pattern: Replace setTimeout/mock functions with fetch() to real endpoints

**Key files:**
- components/AIHashtagGenerator.tsx (line 66-71: mockGenerateHashtags)
- components/SentimentAnalysis.tsx (check for mock patterns)
- components/AIWritingAssistant.tsx (check for mock patterns)
- app/api/ai-content/hashtags/route.ts (real API endpoint)
- app/api/analytics/sentiment/route.ts (real API endpoint)
- app/api/generate/route.ts (real API endpoint)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AIHashtagGenerator to real API</name>
  <files>components/AIHashtagGenerator.tsx</files>
  <action>
    1. Read the component to locate all mock patterns (mockGenerateHashtags, setTimeout)
    2. Replace mock generation with fetch to `/api/ai-content/hashtags`:
       - POST body: { content, platform, count, includeNiche, includeTrending, style }
       - Response: { hashtags: [{ tag, relevance, popularity, trending, reach, competition, category }] }
    3. Add proper error handling with try/catch
    4. Use notify.error() for API failures with user-friendly messages
    5. Ensure loading state (setLoading) is properly managed
    6. Remove the mockGenerateHashtags function and extractKeywords helper if no longer needed

    Reference: app/api/ai-content/hashtags/route.ts for expected request/response format
  </action>
  <verify>
    - grep "mock" components/AIHashtagGenerator.tsx returns no matches
    - grep "setTimeout" components/AIHashtagGenerator.tsx returns no matches
    - grep "fetch.*ai-content/hashtags" components/AIHashtagGenerator.tsx returns 1 match
  </verify>
  <done>AIHashtagGenerator uses real /api/ai-content/hashtags endpoint</done>
</task>

<task type="auto">
  <name>Task 2: Wire SentimentAnalysis to real API</name>
  <files>components/SentimentAnalysis.tsx</files>
  <action>
    1. Read the component to identify mock patterns (Math.random, setTimeout, mock data arrays)
    2. Replace mock sentiment analysis with fetch to `/api/analytics/sentiment`:
       - POST body: { text } or { texts: string[] } for batch
       - Response: { sentiment, score, confidence, emotions, topics, entities }
    3. Add proper error handling with try/catch
    4. Handle empty/loading states gracefully
    5. Remove all mock data generation functions

    Reference: app/api/analytics/sentiment/route.ts for expected format
  </action>
  <verify>
    - grep "mock" components/SentimentAnalysis.tsx returns no matches (case insensitive)
    - grep "Math.random" components/SentimentAnalysis.tsx returns no matches
    - grep "fetch.*analytics/sentiment\|fetch.*ai-content/sentiment" components/SentimentAnalysis.tsx returns match
  </verify>
  <done>SentimentAnalysis uses real sentiment API endpoint</done>
</task>

<task type="auto">
  <name>Task 3: Wire AIWritingAssistant to real API</name>
  <files>components/AIWritingAssistant.tsx</files>
  <action>
    1. Read the component to identify mock patterns
    2. Replace mock content generation with fetch to `/api/generate`:
       - POST body: { prompt, type, platform, tone, length }
       - Response: { content, alternatives, suggestions }
    3. Add proper error handling with try/catch
    4. Ensure streaming responses are handled if applicable (check if API supports streaming)
    5. Remove mock generation functions

    If component uses different API patterns, wire to appropriate endpoints:
    - Content variations: /api/content/variations
    - Content optimization: /api/ai-content/optimize
  </action>
  <verify>
    - grep "mock\|Mock" components/AIWritingAssistant.tsx returns no matches
    - grep "setTimeout" components/AIWritingAssistant.tsx returns no matches (or only UI-related ones)
    - Component imports fetch or uses API call pattern
  </verify>
  <done>AIWritingAssistant uses real content generation API</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 components have no mock data patterns
- [ ] All 3 components call real API endpoints
- [ ] `npm run type-check` passes
- [ ] No new TypeScript errors introduced
</verification>

<success_criteria>

- AIHashtagGenerator.tsx wired to /api/ai-content/hashtags
- SentimentAnalysis.tsx wired to /api/analytics/sentiment
- AIWritingAssistant.tsx wired to /api/generate or appropriate endpoint
- All mock patterns removed
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-deferred-components/12-01-SUMMARY.md` with:

```yaml
---
phase: 12-deferred-components
plan: 01
status: complete
subsystem: components
tags: [ai-components, api-wiring, mock-removal]
key-files: [components/AIHashtagGenerator.tsx, components/SentimentAnalysis.tsx, components/AIWritingAssistant.tsx]
affects: [12-02-components, 12-03-components]
---
```

Include:
- Components wired (with before/after API patterns)
- API endpoints used
- Any issues encountered
- Ready for next plan
</output>
