---
phase: 49-roi-calculator
plan: 02
type: execute
---

<objective>
Create ROI dashboard UI with metrics, charts, investment tracking, and navigation.

Purpose: Let creators visualize their content ROI and track investments.
Output: ROI components, dashboard page, sidebar and command palette integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-roi-calculator/49-01-SUMMARY.md

**From 49-01:**
@hooks/useROI.ts (ROI data hook)
@app/api/roi/route.ts (ROI API)
@lib/roi/roi-service.ts (ROI service)

**UI patterns to follow:**
@app/dashboard/revenue/page.tsx (dashboard with form modal)
@components/revenue/RevenueOverview.tsx (stats cards)
@components/revenue/RevenueChart.tsx (Recharts)

**Established patterns:**
- Glassmorphic cards (bg-gray-900/50, border-white/10)
- Recharts for visualizations
- PageHeader with filters
- Loading skeletons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ROI visualization components</name>
  <files>components/roi/ROIOverview.tsx, components/roi/ROIChart.tsx, components/roi/PlatformROICards.tsx</files>
  <action>
**ROIOverview.tsx:**
- Large ROI percentage display (color-coded: red <0, yellow 0-50, green 50-100, cyan >100)
- Stats row: Total Revenue, Money Invested, Hours Invested, Net Profit
- ROI per hour metric
- Trend indicator vs previous period

**ROIChart.tsx:**
- Bar chart comparing Revenue vs Investment by platform
- Two bars per platform (revenue in green, investment in red)
- Uses Recharts BarChart
- Tooltip with values

**PlatformROICards.tsx:**
- Grid of cards, one per platform
- Each card shows: platform name, ROI %, revenue, investment, hours
- Color-coded ROI badge
- Sorted by ROI descending

All components:
- Handle loading with skeletons
- Handle empty state gracefully
- Glassmorphic styling
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>ROI visualization components display metrics and charts</done>
</task>

<task type="auto">
  <name>Task 2: Create investment management components</name>
  <files>components/roi/InvestmentList.tsx, components/roi/InvestmentForm.tsx</files>
  <action>
**InvestmentList.tsx:**
- Table/list of investments
- Columns: Date, Type (time/money badge), Category, Description, Amount
- Type badge: time=blue, money=green
- Category badge with colors
- Edit and delete buttons
- Empty state

**InvestmentForm.tsx:**
- Modal form for add/edit investment
- Fields:
  - Type (time/money toggle or select)
  - Category (select dropdown)
  - Amount (number input, label changes based on type: "Hours" vs "Amount")
  - Currency (select, only for money type)
  - Description (text input)
  - Platform (optional select)
  - Investment Date (date picker)
- Dynamic field visibility based on type
- Submit and cancel buttons
- Loading state during submission
  </action>
  <verify>Form validates and submits correctly</verify>
  <done>Investment management components allow CRUD operations</done>
</task>

<task type="auto">
  <name>Task 3: Create ROI dashboard page</name>
  <files>app/dashboard/roi/page.tsx, app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
**page.tsx - ROI Calculator Dashboard:**

Layout:
1. **PageHeader:** "ROI Calculator" with date range picker, platform filter
2. **Overview:** ROIOverview with main ROI metric and stats
3. **Charts:** ROIChart showing revenue vs investment
4. **Platform Cards:** PlatformROICards grid
5. **Investment List:** InvestmentList with add button
6. **Form Modal:** InvestmentForm (shown when adding/editing)

Use useROI hook for data.
Handle loading with skeletons, error with retry.

**layout.tsx update:**
Add to sidebarItems after "Revenue":
```typescript
{ icon: Calculator, label: 'ROI', href: '/dashboard/roi' },
```

**CommandPalette.tsx update:**
Add command:
```typescript
{
  id: 'roi-calculator',
  title: 'ROI Calculator',
  description: 'Calculate return on content investment',
  icon: Calculator,
  action: () => router.push('/dashboard/roi'),
  category: 'navigation',
  keywords: ['roi', 'return', 'investment', 'calculator', 'profit', 'hours', 'time']
}
```
  </action>
  <verify>Navigate to /dashboard/roi, see loading then data</verify>
  <done>ROI dashboard accessible via sidebar and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] ROI metrics display correctly
- [ ] Charts show revenue vs investment
- [ ] Investment list works
- [ ] Add/edit form works
- [ ] Navigation integrated (sidebar + command palette)
</verification>

<success_criteria>
- All tasks completed
- ROI data visualized clearly
- Investments can be managed from UI
- Dashboard is accessible and functional
- Phase 49 complete
</success_criteria>

<output>
After completion, create `.planning/phases/49-roi-calculator/49-02-SUMMARY.md`
</output>
