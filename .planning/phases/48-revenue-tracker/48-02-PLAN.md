---
phase: 48-revenue-tracker
plan: 02
type: execute
---

<objective>
Create revenue dashboard UI with charts, entry management, and navigation.

Purpose: Let creators visualize and manage their income across sources.
Output: Revenue components, dashboard page, sidebar and command palette integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-revenue-tracker/48-01-SUMMARY.md

**From 48-01:**
@hooks/useRevenue.ts (revenue data hook)
@app/api/revenue/route.ts (revenue API)
@lib/revenue/revenue-service.ts (revenue service)

**UI patterns to follow:**
@app/dashboard/analytics/benchmarks/page.tsx (dashboard layout)
@components/benchmarks/BenchmarkOverview.tsx (stats cards)
@components/performance/PatternCharts.tsx (Recharts usage)

**Established patterns:**
- Glassmorphic cards (bg-gray-900/50, border-white/10)
- Recharts for visualizations
- PageHeader with filters
- Loading skeletons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create revenue visualization components</name>
  <files>components/revenue/RevenueOverview.tsx, components/revenue/RevenueChart.tsx, components/revenue/RevenueBySource.tsx</files>
  <action>
**RevenueOverview.tsx:**
- Total revenue stat (large, prominent)
- Stats row: this month, last month, % change, entry count
- Trend indicator (up/down arrow with color)
- Currency display (format with commas, 2 decimals)

**RevenueChart.tsx:**
- Line chart showing revenue over time (monthly)
- Uses Recharts AreaChart with gradient fill
- Tooltip showing month and amount
- Responsive container

**RevenueBySource.tsx:**
- Pie chart showing breakdown by source
- Legend with source names and amounts
- Uses Recharts PieChart
- Color coding per source:
  - sponsorship: cyan
  - affiliate: emerald
  - ads: violet
  - tips: orange
  - merchandise: pink
  - other: gray

All components:
- Handle loading with skeletons
- Handle empty state gracefully
- Glassmorphic styling
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>Revenue visualization components display charts and stats</done>
</task>

<task type="auto">
  <name>Task 2: Create revenue entry management components</name>
  <files>components/revenue/RevenueEntryList.tsx, components/revenue/RevenueEntryForm.tsx</files>
  <action>
**RevenueEntryList.tsx:**
- Table/list of revenue entries
- Columns: Date, Source, Description, Platform, Amount
- Source shown as colored badge
- Edit and delete buttons per row
- Pagination or "load more"
- Empty state when no entries

**RevenueEntryForm.tsx:**
- Modal form for add/edit entry
- Fields:
  - Source (select dropdown)
  - Amount (number input)
  - Currency (select, default USD)
  - Description (text input)
  - Platform (optional select)
  - Brand Name (optional, for sponsorships)
  - Paid Date (date picker)
  - Period Start/End (optional date range)
- Submit and cancel buttons
- Loading state during submission
- Error handling
  </action>
  <verify>Form validates and submits correctly</verify>
  <done>Entry management components allow CRUD operations</done>
</task>

<task type="auto">
  <name>Task 3: Create revenue dashboard page</name>
  <files>app/dashboard/revenue/page.tsx, app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
**page.tsx - Revenue Dashboard:**

Layout:
1. **PageHeader:** "Revenue Tracker" with date range picker, source filter
2. **Overview:** RevenueOverview with total and trends
3. **Charts row:** RevenueChart (70%) + RevenueBySource (30%)
4. **Entry List:** RevenueEntryList with add button
5. **Form Modal:** RevenueEntryForm (shown when adding/editing)

Use useRevenue hook for data.
Handle loading with skeletons, error with retry.

**layout.tsx update:**
Add to sidebarItems after "Benchmarks":
```typescript
{ icon: DollarSign, label: 'Revenue', href: '/dashboard/revenue' },
```

**CommandPalette.tsx update:**
Add command:
```typescript
{
  id: 'revenue-tracker',
  title: 'Revenue Tracker',
  description: 'Track income from sponsorships, affiliates, and more',
  icon: DollarSign,
  action: () => router.push('/dashboard/revenue'),
  category: 'navigation',
  keywords: ['revenue', 'income', 'money', 'earnings', 'sponsorship', 'affiliate', 'monetization']
}
```
  </action>
  <verify>Navigate to /dashboard/revenue, see loading then data</verify>
  <done>Revenue dashboard accessible via sidebar and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] Charts render with correct colors
- [ ] Entry list shows data
- [ ] Add/edit form works
- [ ] Dashboard page loads with all sections
- [ ] Navigation integrated (sidebar + command palette)
</verification>

<success_criteria>
- All tasks completed
- Revenue data visualized clearly
- Entries can be managed from UI
- Dashboard is accessible and functional
- Phase 48 complete
</success_criteria>

<output>
After completion, create `.planning/phases/48-revenue-tracker/48-02-SUMMARY.md`
</output>
