# Phase 128 Plan 01 — Agency Task Model

**Linear:** SYN-972 / SYN-PM-104  
**Goal:** Wire dashboard Tasks to AT-\* agency catalog.

## Tasks

1. Add `lib/agency/agency-task-catalog.ts` (AT-001–032, CEO top 15)
2. Prisma `agency_task_id` column + migration SQL (human applies — no db push)
3. Extend `/api/tasks` create/list/update for `agencyTaskId`
4. UI: create dialog picker, badges, hook mapping
5. Unit tests for catalog + API validation

## Human gate

Apply `prisma/migrations/20260525_add_agency_task_id/migration.sql` via project DB procedure.
