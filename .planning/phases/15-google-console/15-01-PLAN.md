---
phase: 15-google-console
plan: 01
type: execute
model: sonnet
---

<objective>
Create Google Developer Console setup documentation and validation scripts for production OAuth.

Purpose: Document complete setup process for all Google APIs used by Synthex, including OAuth consent screen verification requirements for production use.
Output: Comprehensive setup guide in docs/, validation script, and environment variable checklist.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Google APIs used by Synthex:**
1. YouTube Data API v3 (lib/social/youtube-service.ts) - OAuth 2.0
2. YouTube Analytics API (same file) - OAuth 2.0
3. Google Indexing API (lib/google/indexing.ts) - Service Account
4. Google Gemini AI API (lib/ai/providers/google-provider.ts) - API Key
5. Google OAuth (NextAuth) - OAuth 2.0

**Environment variables for Google services:**
- GOOGLE_CLIENT_ID - OAuth client ID
- GOOGLE_CLIENT_SECRET - OAuth client secret
- GOOGLE_AI_API_KEY - Gemini API key
- GOOGLE_API_KEY - Maps/Places API key
- GOOGLE_PAGESPEED_API_KEY - PageSpeed Insights
- GOOGLE_INDEXING_SERVICE_ACCOUNT_JSON - Indexing API service account
- GOOGLE_CLOUD_PROJECT_ID - Cloud project ID
- NEXT_PUBLIC_GA_ID - Google Analytics

**Prior context:**
- Phase 14 completed all agent wiring
- All platform services are implemented and functional
- Need OAuth consent screen verification for production YouTube access
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Google Cloud Console setup guide</name>
  <files>docs/setup/google-cloud-console.md</files>
  <action>
    Create comprehensive documentation for Google Cloud Console setup.

    Document should cover:

    1. **Project Setup**
       - Creating a new Google Cloud project
       - Enabling required APIs:
         - YouTube Data API v3
         - YouTube Analytics API
         - Web Search Indexing API
         - Generative Language API (Gemini)
         - PageSpeed Insights API
         - Maps JavaScript API (optional)

    2. **OAuth 2.0 Configuration**
       - Creating OAuth 2.0 credentials (Web application type)
       - Authorized JavaScript origins:
         - http://localhost:3000 (development)
         - https://synthex.vercel.app (production)
       - Authorized redirect URIs:
         - http://localhost:3000/api/auth/callback/google
         - http://localhost:3000/api/auth/youtube/callback
         - https://synthex.vercel.app/api/auth/callback/google
         - https://synthex.vercel.app/api/auth/youtube/callback

    3. **OAuth Consent Screen Configuration**
       - User type: External
       - App information fields
       - Scopes required:
         - .../auth/userinfo.email
         - .../auth/userinfo.profile
         - .../auth/youtube.readonly
         - .../auth/youtube.upload
         - .../auth/youtube.force-ssl
       - Test users for development

    4. **Service Account Setup** (for Indexing API)
       - Creating service account
       - Generating JSON key
       - Adding to Search Console as owner

    5. **API Keys**
       - Creating API keys for Gemini, PageSpeed
       - Restriction best practices

    Format: Clear markdown with numbered steps, screenshots placeholders, and troubleshooting tips.
  </action>
  <verify>
    - File exists at docs/setup/google-cloud-console.md
    - Covers all required APIs
    - Includes OAuth consent screen steps
    - Includes service account setup
  </verify>
  <done>Comprehensive Google Cloud Console setup guide created</done>
</task>

<task type="auto">
  <name>Task 2: Create OAuth verification requirements guide</name>
  <files>docs/setup/google-oauth-verification.md</files>
  <action>
    Create documentation for Google OAuth consent screen verification process.

    Document should cover:

    1. **Why Verification is Required**
       - Unverified apps limited to 100 users
       - "Unverified app" warning screen
       - Sensitive scopes require verification

    2. **Verification Requirements**
       - App homepage with privacy policy link
       - OAuth scopes justification
       - Domain verification via DNS or HTML file
       - YouTube partner compliance (if uploading videos)

    3. **Sensitive Scopes Used**
       - youtube.upload - requires verification
       - youtube.force-ssl - requires verification
       - Justification template for each scope

    4. **Verification Process Timeline**
       - Initial submission
       - Google review (typically 4-6 weeks)
       - Common rejection reasons and fixes

    5. **Alternative: Limited Access Mode**
       - How to operate with test users only
       - Adding test users (max 100)
       - When this is acceptable (internal tools, beta)

    6. **Checklist for Submission**
       - [ ] Privacy policy page published
       - [ ] Terms of service page published
       - [ ] Domain ownership verified
       - [ ] All scopes justified
       - [ ] Test video demonstrating OAuth flow
  </action>
  <verify>
    - File exists at docs/setup/google-oauth-verification.md
    - Covers verification requirements
    - Includes scope justifications
    - Includes submission checklist
  </verify>
  <done>OAuth verification requirements guide created</done>
</task>

<task type="auto">
  <name>Task 3: Create environment variable validation script</name>
  <files>scripts/validate-google-config.ts</files>
  <action>
    Create a validation script that checks all Google-related environment variables and API connectivity.

    Script should:

    1. **Check Required Variables**
       - GOOGLE_CLIENT_ID - format validation
       - GOOGLE_CLIENT_SECRET - presence check
       - Warn about missing optional variables

    2. **Validate API Keys**
       - GOOGLE_AI_API_KEY - test with simple Gemini call
       - GOOGLE_PAGESPEED_API_KEY - test with PageSpeed API

    3. **Validate OAuth Configuration**
       - Verify redirect URIs match NEXT_PUBLIC_APP_URL
       - Check scope configuration

    4. **Validate Service Account**
       - GOOGLE_INDEXING_SERVICE_ACCOUNT_JSON - JSON parse validation
       - Verify required fields present

    5. **Output Report**
       - Summary table of all configs
       - Clear PASS/FAIL/WARN status
       - Actionable error messages

    Add npm script: "validate:google" in package.json
  </action>
  <verify>
    - File compiles without TypeScript errors
    - Script runs and produces output
    - npm script added to package.json
  </verify>
  <done>Google configuration validation script created</done>
</task>

<task type="auto">
  <name>Task 4: Create environment variable reference</name>
  <files>docs/setup/google-env-variables.md</files>
  <action>
    Create a quick reference document for all Google-related environment variables.

    Format as a table:

    | Variable | Required | Description | Where to Get |
    |----------|----------|-------------|--------------|
    | GOOGLE_CLIENT_ID | Yes (OAuth) | OAuth 2.0 client ID | Cloud Console > Credentials |
    | ... | ... | ... | ... |

    Include:
    - All Google env vars from .env.example
    - Security level (PUBLIC/SECRET/CRITICAL)
    - Format/validation rules
    - Dependencies between variables
    - Example values (sanitized)

    Also create a quick-start section:
    ```bash
    # Minimum required for YouTube integration
    GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
    GOOGLE_CLIENT_SECRET=GOCSPX-xxx

    # Minimum required for AI features
    GOOGLE_AI_API_KEY=AIza-xxx
    ```
  </action>
  <verify>
    - File exists at docs/setup/google-env-variables.md
    - All Google env vars documented
    - Includes security levels
    - Includes quick-start examples
  </verify>
  <done>Google environment variable reference created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 4 documentation files created
- [ ] Validation script compiles and runs
- [ ] npm script "validate:google" works
- [ ] Documentation is consistent with actual codebase
</verification>

<success_criteria>

- Google Cloud Console setup guide with all required APIs
- OAuth verification guide with scope justifications
- Working validation script for Google configs
- Environment variable quick reference
- All docs use correct paths matching actual code

</success_criteria>

<output>
After completion, create `.planning/phases/15-google-console/15-01-SUMMARY.md` with:

```yaml
---
phase: 15-google-console
plan: 01
status: complete
subsystem: documentation
tags: [google-cloud, oauth, setup-guide, validation]
key-files: [docs/setup/google-cloud-console.md, docs/setup/google-oauth-verification.md, scripts/validate-google-config.ts, docs/setup/google-env-variables.md]
affects: []
---
```

Include:
- Documents created with file paths
- Key sections in each document
- Phase 15 complete indicator (this is the only plan)
</output>
