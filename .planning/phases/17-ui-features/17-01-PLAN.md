---
phase: 17-ui-features
plan: 01
status: pending
subsystem: ui
tags: [navigation, sidebar, command-palette, discoverability]
key-files: [app/dashboard/layout.tsx, components/CommandPalette.tsx]
affects: [dashboard navigation, user dropdown]
---

# Plan 17-01: Add Missing Navigation Items

**Add hidden features to sidebar navigation and command palette for better discoverability**

## Objective

Several important dashboard pages exist but aren't accessible via navigation:
- `/dashboard/reports` — Report generation (important feature)
- `/dashboard/experiments` — A/B testing (valuable feature)
- `/dashboard/billing` — Subscription management

Make these features discoverable by adding them to navigation.

## Execution Context

```
@app/dashboard/layout.tsx          # Sidebar navigation definition
@components/CommandPalette.tsx     # Command palette commands
```

## Context

**Current sidebar navigation (20 items):**
Dashboard, Viral Patterns, Personas, Content, Sandbox, Schedule, Tasks, Analytics, Psychology, Competitors, Video, SEO Tools, GEO Analysis, Authors, Research, Visuals, Local SEO, Team, Integrations, Settings

**Hidden pages to add:**
1. Reports — should be in sidebar (between Analytics and Psychology makes sense)
2. Experiments — should be in sidebar (near Analytics or after Reports)
3. Billing — should be in user dropdown menu (not sidebar, since it's account-related)

**Command palette currently has 10 commands**, missing many navigation options.

## Tasks

### Task 1: Add Reports and Experiments to sidebar navigation
**File:** `app/dashboard/layout.tsx`

Add to `sidebarItems` array after Analytics:
```typescript
{ icon: BarChart3, label: 'Analytics', href: '/dashboard/analytics' },
{ icon: FileSpreadsheet, label: 'Reports', href: '/dashboard/reports' },
{ icon: Beaker, label: 'Experiments', href: '/dashboard/experiments' },
```

Import icons:
```typescript
import { FileSpreadsheet, Beaker } from '@/components/icons';
```

**Commit:** `feat(17-01): add Reports and Experiments to sidebar navigation`

### Task 2: Add Billing link to user dropdown menu
**File:** `app/dashboard/layout.tsx`

Add Billing menu item after Settings in the user dropdown:
```tsx
<DropdownMenuItem asChild>
  <Link href="/dashboard/billing" className="flex items-center">
    <CreditCard className="mr-2 h-4 w-4" />
    <span>Billing</span>
  </Link>
</DropdownMenuItem>
```

Import CreditCard icon if not already imported.

**Commit:** `feat(17-01): add Billing to user dropdown menu`

### Task 3: Expand CommandPalette with missing navigation commands
**File:** `components/CommandPalette.tsx`

Add these commands to the `commands` array:

**Navigation commands:**
- Reports (FileSpreadsheet icon, keywords: reports, generate, export, pdf)
- Experiments (Beaker icon, keywords: experiments, ab testing, test, variants)
- Billing (CreditCard icon, keywords: billing, subscription, payment, invoice)
- Team (Users icon, keywords: team, members, collaboration)
- Competitors (Target icon, keywords: competitors, competition, tracking)
- Personas (Brain icon, keywords: personas, brand voice, ai)
- Help (HelpCircle icon, keywords: help, support, docs, faq)

**Commit:** `refactor(17-01): expand CommandPalette with additional navigation commands`

## Verification

```bash
# Type-check
npm run type-check

# Visual verification
# 1. Start dev server: npm run dev
# 2. Navigate to /dashboard
# 3. Verify sidebar shows Reports and Experiments after Analytics
# 4. Click user avatar dropdown - verify Billing option present
# 5. Press Cmd+K - verify new commands appear and work
```

## Success Criteria

- [ ] Reports link visible in sidebar navigation
- [ ] Experiments link visible in sidebar navigation
- [ ] Billing link visible in user dropdown menu
- [ ] CommandPalette includes 7 new navigation commands
- [ ] All new links navigate to correct pages
- [ ] Type-check passes

## Output

- 3 files modified
- 2 new sidebar items (Reports, Experiments)
- 1 new dropdown item (Billing)
- 7 new command palette commands
