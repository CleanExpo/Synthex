---
phase: 109-ui-state-audit
plan: 02
type: execute
---

<objective>
Add missing loading.tsx and error.tsx for remaining 10 pages (v6.0 + nested routes), then audit inline state handling across all v5.0/v6.0 client components.

Purpose: Complete file-based state coverage for 100% of dashboard routes, then verify inline loading/empty/error handling quality in the newest pages.
Output: 20 new files (10 loading.tsx + 10 error.tsx) + audit report documenting inline state coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ui-state-audit/109-01-SUMMARY.md

**Reference patterns (same as Plan 01):**
@app/dashboard/analytics/loading.tsx
@app/dashboard/analytics/error.tsx

**Target pages (10 — v6.0 + nested routes):**
1. `app/dashboard/forecasting/` — Prophet + BayesNF forecasting
2. `app/dashboard/optimisation/` — Bayesian Optimisation surfaces
3. `app/dashboard/voice/` — Voice dashboard (server → client)
4. `app/dashboard/geo/optimiser/` — GEO Optimiser v2 (nested under /geo)
5. `app/dashboard/web-projects/` — Web Projects list
6. `app/dashboard/web-projects/[id]/` — Web Project detail (dynamic route)
7. `app/dashboard/settings/brand-profile/` — Brand Profile settings
8. `app/dashboard/settings/accounts/` — Connected accounts settings
9. `app/dashboard/ai-chat/[conversationId]/` — AI Chat conversation (dynamic route)
10. `app/dashboard/admin/bayesian-health/` — Admin BO health page

**Inline state audit targets (all 22 v5.0/v6.0 pages from Plan 01 + 02):**
Verify each page's client component handles:
- Loading: spinner, skeleton, or loading text while data fetches
- Empty: meaningful message when no data exists (not just blank)
- Error: catch block, error state, or toast notification on fetch failure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx + error.tsx for 10 remaining pages</name>
  <files>
    app/dashboard/forecasting/loading.tsx, app/dashboard/forecasting/error.tsx,
    app/dashboard/optimisation/loading.tsx, app/dashboard/optimisation/error.tsx,
    app/dashboard/voice/loading.tsx, app/dashboard/voice/error.tsx,
    app/dashboard/geo/optimiser/loading.tsx, app/dashboard/geo/optimiser/error.tsx,
    app/dashboard/web-projects/loading.tsx, app/dashboard/web-projects/error.tsx,
    app/dashboard/web-projects/[id]/loading.tsx, app/dashboard/web-projects/[id]/error.tsx,
    app/dashboard/settings/brand-profile/loading.tsx, app/dashboard/settings/brand-profile/error.tsx,
    app/dashboard/settings/accounts/loading.tsx, app/dashboard/settings/accounts/error.tsx,
    app/dashboard/ai-chat/[conversationId]/loading.tsx, app/dashboard/ai-chat/[conversationId]/error.tsx,
    app/dashboard/admin/bayesian-health/loading.tsx, app/dashboard/admin/bayesian-health/error.tsx
  </files>
  <action>
    Same pattern as Plan 01:
    - Read each page.tsx to understand layout
    - Create loading.tsx with glassmorphic skeleton matching the layout
    - Create error.tsx with DashboardError and page-specific title/description

    Page-specific error titles:
    - forecasting → "Forecasting Error" / "forecasting data"
    - optimisation → "Optimisation Error" / "optimisation surfaces"
    - voice → "Voice Dashboard Error" / "voice analysis data"
    - geo/optimiser → "GEO Optimiser Error" / "GEO optimisation data"
    - web-projects → "Web Projects Error" / "web project data"
    - web-projects/[id] → "Project Detail Error" / "project details"
    - settings/brand-profile → "Brand Profile Error" / "brand profile settings"
    - settings/accounts → "Connected Accounts Error" / "connected accounts"
    - ai-chat/[conversationId] → "Chat Error" / "conversation data"
    - admin/bayesian-health → "BO Health Error" / "Bayesian optimisation health data"

    For dynamic routes ([id], [conversationId]):
    - Keep skeletons simple — detail view with header + content area
    - Error boundaries work the same way regardless of dynamic params

    For nested routes (geo/optimiser, settings/*, admin/*):
    - These already have parent loading.tsx/error.tsx, but specific ones provide better UX
    - Keep skeletons focused on the specific page layout
  </action>
  <verify>
    `npx tsc --noEmit` passes with 0 new errors.
    All 20 new files exist in correct locations.
  </verify>
  <done>10 loading.tsx + 10 error.tsx files created for remaining pages, zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Audit inline state handling across all 22 v5.0/v6.0 client components</name>
  <files>Read-only audit — no files modified unless critical gaps found</files>
  <action>
    Read the primary client component for each of the 22 pages (from Plans 01 + 02).
    For each, document:
    1. **Loading**: What renders during data fetch? (spinner / skeleton / text / nothing)
    2. **Empty**: What renders when data is empty/null? (message / CTA / blank)
    3. **Error**: What happens on fetch failure? (error state / toast / silent fail)

    Grade each page: ✅ (all 3 handled) | ⚠️ (partial) | ❌ (missing critical state)

    For any page graded ❌:
    - Add the missing state handling inline (follow existing SWR patterns)
    - Use `Loader2` spinner for loading, inline empty state card, `toast.error()` for errors

    For pages graded ⚠️:
    - Document what's partial but do NOT fix (minor improvements, not critical)

    Produce audit table in the SUMMARY.md.
  </action>
  <verify>
    Audit table complete for all 22 pages.
    Any ❌-graded pages fixed with inline state handling.
    `npm run type-check` passes.
  </verify>
  <done>All 22 v5.0/v6.0 pages audited for inline state handling. Any critical gaps (❌) fixed. Audit table documented in SUMMARY.md.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes with 0 errors
- [ ] All 20 new files (10 loading + 10 error) exist
- [ ] Audit table covers all 22 v5.0/v6.0 pages
- [ ] Any ❌-graded pages have been fixed
- [ ] Total dashboard state coverage: ~93 pages all with loading.tsx + error.tsx
</verification>

<success_criteria>

- 10 loading.tsx + 10 error.tsx files created for remaining pages
- Comprehensive inline state audit for all 22 v5.0/v6.0 pages
- Any critical state handling gaps fixed
- Zero TypeScript errors
- Phase 109 complete — all dashboard pages have file-based + inline state coverage
</success_criteria>

<output>
After completion, create `.planning/phases/109-ui-state-audit/109-02-SUMMARY.md`

Update `.planning/STATE.md`:
- Phase: 110 of 113 — NOT STARTED
- Progress: ████░░░░░░ 33% (2/6 phases)
- Last activity: Phase 109 complete

Update `.planning/ROADMAP.md`:
- Mark Phase 109 as [x] with completion note
</output>
