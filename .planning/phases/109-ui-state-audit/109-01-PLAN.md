---
phase: 109-ui-state-audit
plan: 01
type: execute
---

<objective>
Add missing loading.tsx and error.tsx files for 12 v5.0 feature pages that lack file-based Suspense boundaries.

Purpose: Ensure consistent loading/error UX across all dashboard pages — v5.0 pages added in Phases 85-100 were built with inline SWR state handling but lack the file-based loading.tsx/error.tsx siblings that the rest of the dashboard uses.
Output: 24 new files (12 loading.tsx + 12 error.tsx) following established glassmorphic skeleton + DashboardError patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-ui-audit-states/55-01-SUMMARY.md
@.planning/phases/55-ui-audit-states/55-02-SUMMARY.md

**Reference patterns:**
@app/dashboard/analytics/loading.tsx — glassmorphic skeleton with Card, bg-white/5, animate-pulse
@app/dashboard/analytics/error.tsx — DashboardError pattern (title + description)
@app/dashboard/platforms/loading.tsx — grid skeleton (9-card)
@app/dashboard/seo/error.tsx — DashboardError for section pages

**Established conventions (from Phase 55-01):**
- `'use client'` directive on all loading/error files
- Loading skeletons use `animate-pulse`, `bg-white/5` fills, `bg-surface-base/80 border border-cyan-500/10` cards
- Error files use `DashboardError` from `@/components/dashboard`
- Skeleton shape should approximate the actual page layout (stats grid, tabs, charts, lists)

**Target pages (12 — v5.0 intelligence/authority features):**
1. `app/dashboard/authority/` — Authority Engine (tabbed: analysis + audit)
2. `app/dashboard/citation/` — Citation Dashboard (auto-refresh metrics)
3. `app/dashboard/eeat/` — E-E-A-T Score Builder (3 tabs: score, history, assets)
4. `app/dashboard/backlinks/` — Backlink Prospector (tabs: prospects, analysis, outreach)
5. `app/dashboard/sentinel/` — Algorithm Sentinel (tabs: health, alerts, algorithm)
6. `app/dashboard/prompts/` — Prompt Intelligence (tabs: tracking, gaps)
7. `app/dashboard/pr/` — PR Manager (tabs: journalists, pitches, coverage, releases)
8. `app/dashboard/awards/` — Awards & Directories (4 tabs)
9. `app/dashboard/brand/` — Brand Builder (tabs: identity, consistency, mentions, calendar)
10. `app/dashboard/quality/` — Content Quality Gate (tabs: audit, history)
11. `app/dashboard/brand-voice/` — Brand Voice (server → client delegation)
12. `app/dashboard/insights/` — AI Insights (server → client delegation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx for 12 v5.0 feature pages</name>
  <files>
    app/dashboard/authority/loading.tsx,
    app/dashboard/citation/loading.tsx,
    app/dashboard/eeat/loading.tsx,
    app/dashboard/backlinks/loading.tsx,
    app/dashboard/sentinel/loading.tsx,
    app/dashboard/prompts/loading.tsx,
    app/dashboard/pr/loading.tsx,
    app/dashboard/awards/loading.tsx,
    app/dashboard/brand/loading.tsx,
    app/dashboard/quality/loading.tsx,
    app/dashboard/brand-voice/loading.tsx,
    app/dashboard/insights/loading.tsx
  </files>
  <action>
    For each page, read the actual page.tsx (or its client component) to understand the layout, then create a matching skeleton.

    Common pattern for tabbed pages (authority, eeat, backlinks, sentinel, prompts, pr, awards, brand, quality):
    - Header placeholder (h-9 w-48 + h-5 w-80 description)
    - Tab bar placeholder (3-4 tab items as h-10 w-24 rectangles)
    - Card grid or list below tabs (2-3 skeleton cards)
    - Use `Card` from `@/components/ui/card` with `bg-surface-base/80 border border-cyan-500/10`

    For dashboard-metric pages (citation):
    - Stats grid (4 metric cards) + chart area + list section

    For server-wrapper pages (brand-voice, insights):
    - Simple skeleton matching the client component's primary layout

    All files must:
    - Start with `'use client';`
    - Export default function named `{PageName}Loading()`
    - Use `animate-pulse` on outer div
    - Use `bg-white/5` for skeleton fills
    - Use `bg-cyan-500/10` for primary action placeholders

    Do NOT over-engineer — keep skeletons simple (header + tabs/stats + content area). Approximate the layout, don't replicate every element.
  </action>
  <verify>
    `npx tsc --noEmit` passes with 0 new errors.
    All 12 loading.tsx files exist: `ls app/dashboard/{authority,citation,eeat,backlinks,sentinel,prompts,pr,awards,brand,quality,brand-voice,insights}/loading.tsx | wc -l` = 12
  </verify>
  <done>12 loading.tsx files created, all with 'use client' + glassmorphic skeleton pattern, zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create error.tsx for 12 v5.0 feature pages</name>
  <files>
    app/dashboard/authority/error.tsx,
    app/dashboard/citation/error.tsx,
    app/dashboard/eeat/error.tsx,
    app/dashboard/backlinks/error.tsx,
    app/dashboard/sentinel/error.tsx,
    app/dashboard/prompts/error.tsx,
    app/dashboard/pr/error.tsx,
    app/dashboard/awards/error.tsx,
    app/dashboard/brand/error.tsx,
    app/dashboard/quality/error.tsx,
    app/dashboard/brand-voice/error.tsx,
    app/dashboard/insights/error.tsx
  </files>
  <action>
    Create error.tsx for each page following the exact DashboardError pattern from `app/dashboard/analytics/error.tsx`.

    Each file:
    ```tsx
    'use client';

    import { DashboardError } from '@/components/dashboard';

    export default function {PageName}Error({
      error,
      reset,
    }: {
      error: Error & { digest?: string };
      reset: () => void;
    }) {
      return (
        <DashboardError
          error={error}
          reset={reset}
          title="{Page Display Name} Error"
          description="Failed to load {page description}. Please try again."
        />
      );
    }
    ```

    Page-specific titles and descriptions:
    - authority → "Authority Engine Error" / "authority analysis data"
    - citation → "Citation Dashboard Error" / "citation performance data"
    - eeat → "E-E-A-T Builder Error" / "E-E-A-T scoring data"
    - backlinks → "Backlink Prospector Error" / "backlink prospecting data"
    - sentinel → "Algorithm Sentinel Error" / "algorithm monitoring data"
    - prompts → "Prompt Intelligence Error" / "prompt tracking data"
    - pr → "PR Manager Error" / "PR and media data"
    - awards → "Awards & Directories Error" / "awards and directory data"
    - brand → "Brand Builder Error" / "brand identity data"
    - quality → "Content Quality Error" / "content quality data"
    - brand-voice → "Brand Voice Error" / "brand voice data"
    - insights → "Insights Error" / "AI insights data"
  </action>
  <verify>
    `npx tsc --noEmit` passes with 0 new errors.
    All 12 error.tsx files exist: `ls app/dashboard/{authority,citation,eeat,backlinks,sentinel,prompts,pr,awards,brand,quality,brand-voice,insights}/error.tsx | wc -l` = 12
  </verify>
  <done>12 error.tsx files created, all using DashboardError component with page-specific titles, zero TypeScript errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes with 0 errors
- [ ] All 24 new files (12 loading + 12 error) exist in correct directories
- [ ] Each loading.tsx starts with 'use client' and uses animate-pulse glassmorphic pattern
- [ ] Each error.tsx uses DashboardError from @/components/dashboard
- [ ] No existing files were modified
</verification>

<success_criteria>

- 12 loading.tsx files created for v5.0 feature pages
- 12 error.tsx files created for v5.0 feature pages
- All files follow established patterns (glassmorphic skeletons, DashboardError)
- Zero TypeScript errors introduced
- Consistent with existing dashboard state file conventions
</success_criteria>

<output>
After completion, create `.planning/phases/109-ui-state-audit/109-01-SUMMARY.md`
</output>
