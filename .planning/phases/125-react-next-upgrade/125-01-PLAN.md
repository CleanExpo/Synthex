---
phase: 125-react-next-upgrade
plan: 01
type: execute
domain: backend
---

<objective>
Bump core framework packages to their latest major versions: Next.js 15→16, React 18→19, and associated TypeScript types. Resolve any TypeScript errors introduced by the updated type definitions.

Purpose: Keep the framework stack current as the first step of the v11.0 tech foundation upgrade (SYN-407). React 19 ships ref-as-prop (forwardRef deprecated but not removed), improved server components, and better TypeScript types. Next.js 16 brings Turbopack improvements and Node >=20.9.0 requirements (we are on Node 22).

Output:

- `package.json` bumped: next ^16.2.0, react ^19.2.4, react-dom ^19.2.4, @types/react ^19, @types/react-dom ^19, eslint-config-next ^16.2.0, @next/bundle-analyzer ^16.2.0
- `npm run type-check` passes: 0 errors
- `npm test` runs to completion — record pass/fail baseline (some failures expected from @testing-library/react@14 incompatibility — will be fixed in Plan 02)
  </objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/124-openai-v6/124-01-SUMMARY.md
@package.json
@next.config.mjs

**Discovery findings (2026-03-20):**

Current installed: `react@18.3.1`, `next@15.5.12`
Latest available: `react@19.2.4`, `next@16.2.0`

Next.js 16 peer deps: `react: ^18.2.0 || ^19.0.0` — supports both React 18 and 19. Node requirement: >=20.9.0 (we run Node 22 ✅).

**next.config.mjs is clean** — no deprecated options for Next.js 16. `turbopack` is already top-level (not inside experimental), `serverExternalPackages` already top-level. No changes to next.config.mjs needed.

**Packages NOT being bumped in this plan** (handled in Plan 02):

- `react-day-picker` — requires calendar.tsx source rewrite, separate plan
- `cmdk` — verify on type-check, separate plan
- `@testing-library/react` — React 19 incompatible (v14 peer: ^18 only), separate plan

**Known React 19 type changes that may affect source:**

- `forwardRef` is deprecated (not removed) — will generate TS deprecation warnings, NOT errors. Do NOT fix these.
- `React.ReactElement` generic parameter made optional — unlikely to cause errors
- `ref` prop is now accepted natively on function components — may improve some types
- `useFormState` renamed to `useActionState` — only applies if server actions are used (grep confirms this is not used in this codebase: no `useFormState` in source)

**Constraint:** Do NOT change `@testing-library/react` in this plan — version 14 is incompatible with React 19 and `npm test` will show failures. Record the failing tests but do NOT fix them here. They are fixed in Plan 02.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump core framework packages in package.json + npm install</name>
  <files>package.json, package-lock.json</files>
  <action>
    Edit `package.json` to change these exact version strings in `dependencies`:

    ```
    "next": "^15.5.12"        → "next": "^16.2.0"
    "react": "^18.2.0"        → "react": "^19.2.4"
    "react-dom": "^18.2.0"    → "react-dom": "^19.2.4"
    ```

    And in `devDependencies`:

    ```
    "@next/bundle-analyzer": "^14.2.18"   → "@next/bundle-analyzer": "^16.2.0"
    "@types/react": "^18.2.46"            → "@types/react": "^19"
    "@types/react-dom": "^18.2.18"        → "@types/react-dom": "^19"
    "eslint-config-next": "^15.5.12"      → "eslint-config-next": "^16.2.0"
    ```

    Do NOT change any other package versions. These 7 entries only.

    Then run: `npm install` (allow up to 5 minutes — use 300000ms timeout)

    After install, verify:
    ```bash
    node -e "const r=require('./node_modules/react/package.json'); const n=require('./node_modules/next/package.json'); console.log('react:', r.version, 'next:', n.version)"
    ```
    Must print `react: 19.x.x  next: 16.x.x`

  </action>
  <verify>node -e "const r=require('./node_modules/react/package.json'); const n=require('./node_modules/next/package.json'); console.log('react:', r.version, 'next:', n.version)" — must print 19.x.x and 16.x.x</verify>
  <done>package.json shows all 7 bumped versions, node_modules has react 19.x and next 16.x installed</done>
</task>

<task type="auto">
  <name>Task 2: Run type-check, fix source errors, record test baseline</name>
  <files>Any source .ts/.tsx files that have actual TypeScript errors (not warnings)</files>
  <action>
    1. Run `npm run type-check` and capture the FULL output.

    2. Classify each error:
       - Errors in `node_modules/**` → IGNORE (not our code)
       - Errors about `forwardRef` being deprecated → IGNORE (deprecation warning, not error — React 19 deprecates but does not remove forwardRef)
       - Errors in `src/**` or `with-turbopack-app/**` → check if they were pre-existing (not caused by this upgrade); fix ONLY if introduced by this bump
       - Errors in `app/**`, `components/**`, `lib/**`, `hooks/**` → FIX

    3. Common React 19 type fixes if they appear:
       - If `React.ReactElement<any>` causes error: React 19 made the generic optional, update as needed
       - If `children: React.ReactNode` on props causes error: shouldn't happen but fix if needed
       - Do NOT attempt to refactor `forwardRef` usage to the new ref-as-prop pattern — too large a scope change

    4. STOP rule: If there are errors in more than 10 source files, do NOT fix them all blindly. Report the full list of affected files and stop for human review.

    5. After fixing (or if 0 errors): run `npm run type-check` again — must show 0 errors.

    6. Run `npm test` — let it complete fully. Record the output:
       - Total tests passed
       - Total tests failed (expect failures related to @testing-library/react compatibility)
       - DO NOT FIX the failing tests here — they will be addressed in Plan 02

  </action>
  <verify>npm run type-check exits 0 with 0 errors. npm test completes (failures from @testing-library@14 are expected and documented)</verify>
  <done>
    - `npm run type-check`: 0 errors
    - `npm test`: runs to completion, pass/fail counts recorded in commit message
    - No new test failures beyond @testing-library compatibility (confirm by checking which suites fail)
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "require('./node_modules/react/package.json').version"` prints 19.x.x
- [ ] `node -e "require('./node_modules/next/package.json').version"` prints 16.x.x
- [ ] `npm run type-check` exits 0 with 0 errors
- [ ] `npm test` has been run and results recorded
- [ ] Only package.json, package-lock.json, and (possibly) a small number of source files changed
</verification>

<success_criteria>

- next, react, react-dom, @types/react, @types/react-dom, eslint-config-next, @next/bundle-analyzer all bumped to target versions
- npm install completes without unresolvable peer conflicts
- Zero TypeScript errors in source code
- npm test baseline recorded (some failures expected — not a blocker for this plan)
  </success_criteria>

<output>
After completion, create `.planning/phases/125-react-next-upgrade/125-01-SUMMARY.md`:

---

phase: 125-react-next-upgrade
plan: 01
subsystem: dependencies
tags: [upgrade, react, nextjs, typescript]
key-decisions:

- forwardRef deprecation warnings accepted — not fixed (would require rewriting 20+ UI components)
- @testing-library/react NOT bumped in this plan — requires separate calendar/command migration
  key-files:
  modified: [package.json, package-lock.json]
  completed: YYYY-MM-DD

---

# Phase 125 Plan 01: Core Framework Bump Summary

**[Substantive one-liner: react X→Y, next X→Y, N type errors fixed]**

## Accomplishments

- [What was bumped]
- [Type errors found and fixed, or "0 type errors — clean upgrade"]
- npm test baseline: [X passed, Y failed — failures are @testing-library compat, fixed in Plan 02]

## Files Created/Modified

- `package.json` — 7 version bumps
- `package-lock.json` — regenerated
- [any source files fixed]

## Decisions Made

- [Any non-obvious choices]

## Issues Encountered

- [Errors and fixes, or "None"]

## Next Step

Ready for 125-02-PLAN.md — ecosystem packages (react-day-picker@9, cmdk@1, @testing-library/react@16) + full gate.
</output>
