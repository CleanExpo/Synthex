---
phase: 02-mock-data-api
plan: 02
type: execute
---

<objective>
Fix content generation mock persona and template fallbacks — the content/generate endpoint ignores the actual personaId and returns a hardcoded "Professional Voice" persona, and the content generators fall back to hardcoded template arrays instead of proper error responses.

Purpose: Content generation is a core feature. When a user selects a persona, the system should use their actual persona from the database. When AI generation fails, the system should return an error — not silently swap in fake content that the user might publish.
Output: Persona fetched from database, mock AI content fallbacks replaced with proper error handling, in-memory persona storage replaced with database queries.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mock-data-api/02-CONTEXT.md
@app/api/content/generate/route.ts
@lib/services/content-generator.ts
@lib/ai/content-generator.ts
@lib/ai-persona-learning.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded persona with database query in content generate endpoint</name>
  <files>app/api/content/generate/route.ts, lib/ai-persona-learning.ts</files>
  <action>
  **content/generate/route.ts (lines ~54-66):**
  Replace the hardcoded persona object `{ id: personaId, name: 'Professional Voice', attributes: { tone: 'Professional', style: 'Formal', ... } }` with a real Prisma query.

  Query the persona from the database: `prisma.persona.findUnique({ where: { id: personaId } })` (check actual Prisma model name — may be `Persona`, `AiPersona`, or `ContentPersona` in the schema).

  If personaId is provided but not found in database, return 404: `{ error: "Persona not found", message: "The specified persona does not exist. Create one first or omit personaId to use default settings." }`

  If personaId is not provided (optional), proceed without persona context (no fake default).

  **lib/ai-persona-learning.ts (lines ~172, 180-188):**
  Replace the in-memory `Map<string, PersonaProfile>` and `localStorage.getItem('ai_personas')` with Prisma database queries. This file uses browser-side localStorage which won't work in a serverless environment anyway.

  - Replace `this.personas = new Map()` with database-backed methods
  - Replace `localStorage.getItem/setItem` calls with Prisma queries
  - If the file is used server-side only, remove all localStorage references
  - If the file is used client-side, it's out of scope for this plan (API routes only) — add a TODO comment noting it needs migration to API calls

  Add proper error handling around all database queries.
  </action>
  <verify>
  - Grep content/generate/route.ts for `Professional Voice` — zero matches
  - Grep content/generate/route.ts for `mock persona` or `// In production` — zero matches
  - Grep lib/ai-persona-learning.ts for `localStorage` — zero matches (or TODO comment if client-side file)
  - `npm run type-check` passes
  </verify>
  <done>
  - Hardcoded persona replaced with Prisma query in content/generate endpoint
  - localStorage persona storage replaced with database queries (or marked with TODO if client-side)
  - Persona not found returns proper 404 with hint message
  - Missing personaId proceeds without persona (no fake default)
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace mock AI content fallbacks with proper error responses</name>
  <files>lib/services/content-generator.ts, lib/ai/content-generator.ts</files>
  <action>
  **lib/services/content-generator.ts:**
  - Remove `generateMockAIContent()` method (lines ~530-539) with its hardcoded template array entirely
  - Replace the fallback at lines ~416-430 (when `!this.apiKey`): instead of calling generateMockAIContent(), throw an error or return `{ error: "AI API key not configured", message: "Content generation requires an API key. Configure OpenRouter or Anthropic API key in settings." }`
  - Replace mock variations generation (lines ~381-409) that does simple string transformations (question prefix, emoji swap, reorder): return `{ error: "AI unavailable for variations", message: "Content variations require AI. Configure an API key to enable this feature." }` instead of faking it
  - Remove the `selectRandom()` helper if only used by mock methods

  **lib/ai/content-generator.ts:**
  - Remove or replace `generateFromTemplate()` method (lines ~276-309) with its hardcoded template arrays per platform
  - Remove all hardcoded template strings (`"🚀 {topic} is changing the game!"`, etc.)
  - Remove generic placeholder replacements (`{topic}` → `'innovative strategies'`)
  - When AI call fails, return a clear error: `{ error: "Content generation failed", message: "AI service is temporarily unavailable. Please try again." }` — do NOT silently substitute templates
  - Remove Math.random() template selection (line ~297)

  The principle: if AI is unavailable, tell the user. Never silently publish fake generated content.
  </action>
  <verify>
  - Grep lib/services/content-generator.ts for `generateMockAIContent` — zero matches
  - Grep lib/services/content-generator.ts for `mockResponses` — zero matches
  - Grep lib/ai/content-generator.ts for `generateFromTemplate` — zero matches
  - Grep both files for hardcoded template strings like `changing the game` or `innovative strategies` — zero matches
  - `npm run type-check` passes
  </verify>
  <done>
  - generateMockAIContent() removed entirely
  - generateFromTemplate() removed entirely
  - All hardcoded content templates removed from both files
  - AI unavailable returns proper error responses, never fake content
  - Mock variation generation replaced with error response
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "generateMock" lib/services/content-generator.ts lib/ai/content-generator.ts` returns zero matches
- [ ] `grep -r "Professional Voice" app/api/content/` returns zero matches
- [ ] `grep -r "generateFromTemplate" lib/ai/content-generator.ts` returns zero matches
- [ ] `npm run type-check` passes without errors
- [ ] Content generation with missing API key returns error, not fake content
</verification>

<success_criteria>

- Hardcoded persona replaced with real database query
- All mock AI content generation functions removed
- All hardcoded template arrays removed from content generators
- AI unavailable → clear error response, never fake content
- In-memory/localStorage persona storage replaced with database or marked for migration
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-data-api/02-02-SUMMARY.md`
</output>
