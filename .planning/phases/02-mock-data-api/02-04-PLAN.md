---
phase: 02-mock-data-api
plan: 04
type: execute
---

<objective>
Fix monitoring metrics, SEO audit history, integration status, and persona training stubs — these endpoints use Math.random() for system metrics, hardcoded audit history arrays, in-memory Maps for connection/job state, and random hook/CTA selection.

Purpose: Monitoring should show real system health (or admit it's not configured). SEO audit history should come from the database. Integration connections and training jobs need persistent storage, not in-memory Maps that reset on every serverless cold start.
Output: All four endpoint groups return real data from database/proper sources or honest empty states.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mock-data-api/02-CONTEXT.md
@app/api/monitoring/metrics/route.ts
@app/api/seo/audit/route.ts
@app/api/integrations/[integrationId]/status/route.ts
@app/api/personas/[id]/train/route.ts
@app/api/ai-content/optimize/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace monitoring mock metrics and SEO audit hardcoded history</name>
  <files>app/api/monitoring/metrics/route.ts, app/api/seo/audit/route.ts</files>
  <action>
  **monitoring/metrics/route.ts (lines ~30-51):**
  Remove all Math.random() calls that generate fake system metrics (response time, DB connections, query time, API latency, throughput, error count, blocked attempts).

  Replace with real metrics where available:
  - Database: Use `prisma.$metrics` if available, or query `pg_stat_activity` for connection count, or simply return `{ status: "healthy" }` from a basic `prisma.$queryRaw('SELECT 1')`
  - API latency: Track actual request timing with `Date.now()` at start/end of request
  - For metrics that require external monitoring (Datadog, Prometheus), return: `{ data: null, message: "System metrics require monitoring integration. Basic health check: operational." }` — do NOT fabricate numbers

  The key principle: real metrics or honest "not configured" — never random numbers pretending to be real.

  **seo/audit/route.ts (lines ~421-436):**
  Replace the `mockHistory` hardcoded array with a real Prisma query. Check the schema for `SeoAudit`, `AuditResult`, `AuditHistory`, or similar model.

  If the model exists: query audit records for the user, ordered by date descending.
  If no model exists: return `{ data: [], message: "No SEO audits have been run yet. Run your first audit to start building history." }`

  Also fix:
  - Line ~327: Replace `// TODO: Track and check actual usage` with real usage tracking query or remove the TODO and return actual count
  - Line ~360: Replace `// TODO: Store audit result in database for history` — implement the actual storage using Prisma create, or add a clear code comment explaining what's needed
  - Line ~366: Replace hardcoded usage count `1` with actual count from database
  </action>
  <verify>
  - Grep monitoring/metrics/route.ts for `Math.random` — zero matches
  - Grep seo/audit/route.ts for `mockHistory` — zero matches
  - Grep seo/audit/route.ts for `TODO.*Track.*actual` — zero matches (fixed or replaced with implementation)
  - `npm run type-check` passes
  </verify>
  <done>
  - All Math.random() system metrics removed from monitoring endpoint
  - Monitoring returns real health data or honest "not configured" message
  - SEO audit hardcoded history replaced with database query or proper empty state
  - SEO audit TODOs addressed (implemented or replaced with clear plan)
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace in-memory Maps in integration status and persona training</name>
  <files>app/api/integrations/[integrationId]/status/route.ts, app/api/personas/[id]/train/route.ts, app/api/ai-content/optimize/route.ts</files>
  <action>
  **integrations/[integrationId]/status/route.ts (line ~5, lines ~54-63):**
  Remove the module-level `const integrationConnections = new Map()` — this resets on every Vercel cold start, making it useless in serverless.

  Replace with Prisma queries:
  - Check schema for `Integration`, `UserIntegration`, `ConnectedAccount`, or `SocialAccount` model
  - Query the user's integration connection status from the database
  - If no integration model exists, return: `{ connected: false, message: "Integration status tracking is not yet configured." }`
  - Replace `getPermissionsForIntegration()` hardcoded permissions with actual OAuth scopes stored during the connection flow, or keep as configuration constants (hardcoded permission definitions are config, not mock data — but label them clearly as defaults)

  **personas/[id]/train/route.ts (lines ~42-48, ~244-251):**
  Remove the module-level `const trainingJobs = new Map()` and the `setInterval()` cleanup — both are useless in serverless (reset on cold start, interval never runs).

  Replace with Prisma queries:
  - Check schema for `TrainingJob`, `PersonaTraining`, or similar model
  - If model exists: create/query/update training jobs in database
  - If no model exists: return `{ status: "not_started", message: "Persona training requires a training jobs table. Feature not yet available." }`
  - For long-running training: acknowledge the request and return a job ID that can be polled, stored in database

  **ai-content/optimize/route.ts (lines ~46-93, ~369, ~388, ~553):**
  The hardcoded HOOK_STARTERS, CTA_TEMPLATES, and platform hashtag arrays are content configuration, not mock data — they're reasonable defaults for a content optimization tool. However, the Math.random() selection should be replaced with deterministic selection:
  - Replace `Math.random()` selection with index-based rotation (use post count or hash of content as seed)
  - Or better: if the user has historical performance data, select hooks/CTAs based on past engagement
  - If no performance data available, rotate through options deterministically rather than randomly
  - The hardcoded arrays themselves can stay as defaults, but add a comment: `// Default optimization templates - can be overridden per-user in database`
  </action>
  <verify>
  - Grep integrations/[integrationId]/status/route.ts for `new Map()` at module level — zero matches
  - Grep personas/[id]/train/route.ts for `new Map` at module level — zero matches
  - Grep personas/[id]/train/route.ts for `setInterval` — zero matches
  - Grep ai-content/optimize/route.ts for `Math.random` — zero matches
  - `npm run type-check` passes
  </verify>
  <done>
  - In-memory Map removed from integration status endpoint, replaced with database query
  - In-memory Map and setInterval removed from persona training endpoint
  - Training jobs stored in database (or proper "not available" response)
  - Math.random() selection in optimize endpoint replaced with deterministic approach
  - All endpoints work correctly in serverless environment (no cold-start data loss)
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "Math.random" app/api/monitoring/ app/api/seo/audit/ app/api/integrations/ app/api/ai-content/optimize/` returns zero matches
- [ ] `grep -r "new Map()" app/api/integrations/ app/api/personas/` at module level — zero matches
- [ ] `grep -r "mockHistory" app/api/seo/` returns zero matches
- [ ] `npm run type-check` passes without errors
</verification>

<success_criteria>

- Monitoring metrics endpoint returns real data or honest "not configured" state
- SEO audit history comes from database, not hardcoded array
- Integration status stored in database, not in-memory Map
- Training jobs stored in database, not in-memory Map
- No serverless cold-start data loss in any endpoint
- Optimization endpoint uses deterministic selection, not Math.random()
- All endpoints include proper error handling
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-data-api/02-04-SUMMARY.md`
</output>
