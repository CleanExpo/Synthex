---
phase: 02-mock-data-api
plan: 03
type: execute
---

<objective>
Fix content library and trending endpoints — the library endpoints return empty arrays/404 stubs instead of querying the database, and the trending endpoint uses Math.random() for trend changes with hardcoded default topics.

Purpose: Content library is a user-facing feature that should show saved content. Trending topics should reflect real data or proper empty states. Both currently return fake/empty data that makes the feature appear broken.
Output: Library endpoints perform real CRUD with Prisma, trending endpoint queries real data or returns proper empty state.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mock-data-api/02-CONTEXT.md
@app/api/library/content/route.ts
@app/api/library/content/[contentId]/route.ts
@app/api/trending/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement real CRUD for content library endpoints</name>
  <files>app/api/library/content/route.ts, app/api/library/content/[contentId]/route.ts</files>
  <action>
  **library/content/route.ts (GET list endpoint):**
  Replace the hardcoded empty array placeholder (line ~62-63 `const data: unknown[] = []`) with a real Prisma query. Check the schema for the correct model name — likely `ContentLibrary`, `SavedContent`, `Content`, or similar.

  Query pattern:
  ```
  const data = await prisma.[model].findMany({
    where: { userId: session.user.id },
    orderBy: { createdAt: 'desc' },
    take: limit,
    skip: offset,
  })
  ```

  If the model doesn't exist in the schema, check for any content-related model (Post, Content, Draft, etc.). If genuinely no model exists for content library, return: `{ data: [], message: "Content library is not yet configured. Save content from the editor to see it here.", total: 0 }`

  **library/content/[contentId]/route.ts (GET/PUT/DELETE single item):**
  Replace all stubbed operations (lines ~67-234):
  - GET: Query by id AND userId (IDOR protection): `prisma.[model].findFirst({ where: { id: contentId, userId: session.user.id } })`
  - PUT: Update with ownership check, return updated record
  - DELETE: Delete with ownership check, return success message

  Replace hardcoded 404 responses with real "not found" checks — return 404 only when the record genuinely doesn't exist or doesn't belong to the user.

  Add proper error handling: try/catch around Prisma queries, 500 on database errors with logged details.
  </action>
  <verify>
  - Grep library/content/route.ts for `placeholder` — zero matches
  - Grep library/content/[contentId]/route.ts for stubbed/placeholder patterns — zero matches
  - `npm run type-check` passes
  </verify>
  <done>
  - List endpoint queries real data from database with pagination
  - Single-item endpoint performs real GET/PUT/DELETE with ownership checks
  - No placeholder arrays or stubbed 404s remain
  - Proper empty states with hint messages when no content exists
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace trending endpoint mock data with real queries</name>
  <files>app/api/trending/route.ts</files>
  <action>
  Replace `calculateChange()` function (lines ~106-109) that uses Math.random() to simulate trend changes. Instead:
  - If there's a database model for trending data (check schema for `TrendingTopic`, `Trend`, `Hashtag`, etc.), query it directly
  - If trend data is stored as part of another model (e.g., Post hashtags), aggregate from real data
  - If no trending data exists in database, return proper empty state: `{ data: [], message: "No trending data available yet. Trending topics will appear as content is published and tracked." }`

  Replace `getDefaultTrendingTopics()` function (lines ~118-129) that returns hardcoded hashtag arrays. Remove the entire function. When there's no data, return the empty state above — never return fake "default" trends.

  Remove all Math.random() calls from the file.

  If the endpoint genuinely depends on external trending APIs (Twitter Trends, etc.) that aren't configured, return: `{ data: [], message: "Trending data requires social platform connections. Connect your accounts in Settings to see trends." }`
  </action>
  <verify>
  - Grep trending/route.ts for `Math.random` — zero matches
  - Grep trending/route.ts for `getDefaultTrendingTopics` — zero matches
  - Grep trending/route.ts for hardcoded hashtag strings — zero matches
  - `npm run type-check` passes
  </verify>
  <done>
  - Math.random() trend simulation removed
  - Hardcoded default topics function removed
  - Endpoint returns real data or proper empty state with hint message
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "Math.random" app/api/library/ app/api/trending/` returns zero matches
- [ ] `grep -r "placeholder" app/api/library/` returns zero matches (case-insensitive)
- [ ] `grep -r "getDefaultTrending" app/api/trending/` returns zero matches
- [ ] `npm run type-check` passes without errors
</verification>

<success_criteria>

- Content library endpoints perform real CRUD operations with Prisma
- Trending endpoint returns real data or proper empty state
- Zero Math.random() calls remain in these endpoints
- Zero hardcoded arrays remain
- All empty states include hint messages per 02-CONTEXT.md pattern
- IDOR protection on content library (userId ownership checks)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-data-api/02-03-SUMMARY.md`
</output>
