---
phase: 02-mock-data-api
plan: 01
type: execute
---

<objective>
Fix competitor tracking mock data — the cron runs every 30 minutes and is actively polluting the database with fake metrics and posts generated by Math.random().

Purpose: Stop the bleeding. The competitor tracking cron creates CompetitorSnapshot and CompetitorPost records with completely fabricated data every 30 minutes. Every hour this stays live, the database gets more fake data that will need cleanup.
Output: All three competitor tracking endpoints use real Prisma queries; generateMockMetrics() and generateMockPost() functions removed entirely.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mock-data-api/02-CONTEXT.md
@app/api/competitors/track/execute/route.ts
@app/api/competitors/track/[id]/snapshot/route.ts
@app/api/competitors/track/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove generateMockMetrics() and generateMockPost() from cron execute route</name>
  <files>app/api/competitors/track/execute/route.ts</files>
  <action>
  Remove the `generateMockMetrics()` function (lines ~135-168) and `generateMockPost()` function (lines ~170-199) entirely.

  Replace calls to these functions with real Prisma queries:
  - For metrics: Query the most recent `CompetitorSnapshot` for the competitor. If no snapshot exists yet (competitor was just added), create a snapshot record with null/zero metrics and a `dataSource: 'pending'` flag. Do NOT fabricate numbers.
  - For posts: Query `CompetitorPost` records for the competitor filtered by platform. If none exist, return an empty array.
  - When the endpoint has no real data to return, use the empty-state pattern: `{ data: [], message: "No competitor data collected yet. Data will appear after the next tracking cycle." }`

  Remove all hardcoded arrays: `topHashtags`, `contentTemplates`, `contentTypes`, `postingTimes` arrays.
  Remove all Math.random() calls.

  Add proper error handling: wrap Prisma queries in try/catch, return 500 with `{ error: "Failed to execute competitor tracking" }` on database errors. Log the actual error with console.error for debugging.

  Keep the cron authentication check and rate limiting logic intact — only replace the data generation layer.
  </action>
  <verify>
  - Grep the file for `Math.random` — zero matches
  - Grep the file for `generateMock` — zero matches
  - Grep the file for hardcoded hashtag arrays like `marketing.*business.*growth` — zero matches
  - `npm run type-check` passes (no TypeScript errors)
  </verify>
  <done>
  - generateMockMetrics() and generateMockPost() functions deleted
  - All Math.random() calls removed from file
  - All hardcoded content arrays removed
  - Cron endpoint returns real data from Prisma or proper empty states
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove generateMockMetrics() from snapshot route and fix initial snapshot stub</name>
  <files>app/api/competitors/track/[id]/snapshot/route.ts, app/api/competitors/track/route.ts</files>
  <action>
  **Snapshot route (track/[id]/snapshot/route.ts):**
  Remove the duplicate `generateMockMetrics()` function (lines ~78-111). Replace its call with a real Prisma query to fetch the latest `CompetitorSnapshot` for this competitor ID. If no snapshot exists, return `{ data: null, message: "No snapshot data available yet. Tracking will collect data on the next cycle." }` with a 200 status (not an error — absence of data is a valid state).

  Remove all Math.random() calls and hardcoded arrays (hashtags, content types, posting times).

  Keep the milestone detection logic (lines ~408) but make the thresholds come from the endpoint's own logic (the hardcoded milestone array `[1000, 5000, 10000, ...]` is acceptable as configuration, not mock data).

  **Track route (track/route.ts):**
  Fix the initial snapshot creation stub (lines ~248-257). Instead of creating an empty snapshot record that silently catches errors, either:
  - Don't create an initial snapshot at all (let the cron populate it on next cycle), OR
  - Create the record with explicit `dataSource: 'initial'` and all metrics set to 0/null

  Remove the `.catch(() => {})` silent error swallowing — if the snapshot creation fails, log the error with `console.error('Failed to create initial snapshot:', error)` but don't block the competitor creation response.

  Add proper error handling to both files: try/catch around Prisma queries, meaningful error responses.
  </action>
  <verify>
  - Grep both files for `Math.random` — zero matches
  - Grep both files for `generateMock` — zero matches
  - Grep track/route.ts for `catch\(\)` or `catch(() =>` with empty body — zero matches
  - `npm run type-check` passes
  </verify>
  <done>
  - generateMockMetrics() removed from snapshot route
  - All Math.random() and hardcoded arrays removed from both files
  - Silent error swallowing replaced with proper error logging
  - Both endpoints return real Prisma data or meaningful empty states
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "generateMock" app/api/competitors/` returns zero matches
- [ ] `grep -r "Math.random" app/api/competitors/` returns zero matches
- [ ] `npm run type-check` passes without errors
- [ ] All three competitor routes handle the "no data yet" case with proper empty states and hint messages
</verification>

<success_criteria>

- All generateMockMetrics() and generateMockPost() functions removed from competitor tracking
- Zero Math.random() calls remain in app/api/competitors/
- Zero hardcoded content arrays remain
- All endpoints return real Prisma data or proper empty-state responses with hint messages
- Silent error swallowing replaced with proper error logging
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-data-api/02-01-SUMMARY.md`
</output>
