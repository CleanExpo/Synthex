---
phase: 02-mock-data-api
plan: 05
type: execute
---

<objective>
Full endpoint audit — grep the entire codebase for any remaining mock patterns (Math.random in data generation, hardcoded arrays as data sources, TODO comments about mocks, in-memory stores pretending to be databases) and fix or document every instance.

Purpose: Catch-all sweep to ensure zero mock data remains in any API route or supporting service. Plans 01-04 handled the known patterns; this plan catches anything missed and handles edge cases in lib/ services.
Output: Zero mock data patterns remain in app/api/ or lib/ directories. Audit report documenting every pattern found and resolved.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mock-data-api/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining mock patterns in lib/ services</name>
  <files>lib/ai/agents/strategic-marketing/psychology-testing.ts, lib/analytics/anomaly-detector.ts, src/services/ai/content-variations.ts, lib/ai-persona-learning.ts</files>
  <action>
  Fix known remaining mock patterns in lib/ and src/ services:

  **lib/ai/agents/strategic-marketing/psychology-testing.ts:**
  - Lines ~94-96: Replace `simulateUserInteractions()` that adds random variance to metrics. If this is used for A/B testing, it needs real engagement data. If the feature isn't ready, replace with: `throw new Error('Psychology testing requires real engagement data. Feature not yet available.')`
  - Line ~597: Replace `Math.random() > 0.3` mock criterion evaluation with actual evaluation logic, or throw an error if the evaluation logic isn't implemented yet

  **lib/analytics/anomaly-detector.ts:**
  - Lines ~401, ~803: Replace `Math.random().toString(36)` UUID generation with `crypto.randomUUID()` (available in Node 19+/Next.js edge runtime). Import from `crypto` module. This is a correctness fix — Math.random() UUIDs can collide.

  **src/services/ai/content-variations.ts:**
  - Lines ~621-646: Replace simple string manipulation fallback variations (question prefix, emoji prefix, truncation) with proper error response when AI is unavailable: `{ error: "Variations require AI", message: "Content variation generation requires an AI API key." }`
  - Line ~673: Replace Math.random() ID generation with `crypto.randomUUID()`

  **lib/ai-persona-learning.ts (if not fully addressed in 02-02):**
  - Lines ~402-415: Replace mock experiment results with `Math.random() > 0.5` success/failure. Either connect to real experiment tracking or return: `{ error: "Experiment tracking not configured", message: "A/B testing requires analytics integration." }`
  </action>
  <verify>
  - Grep all four files for `Math.random` — zero matches (except any that are genuinely appropriate, like crypto seeding)
  - Grep for `simulateUserInteractions` — zero matches or properly marked as unimplemented
  - `npm run type-check` passes
  </verify>
  <done>
  - psychology-testing.ts mock simulations replaced with real data or proper errors
  - anomaly-detector.ts uses crypto.randomUUID() instead of Math.random() for IDs
  - content-variations.ts string manipulation fallbacks replaced with error responses
  - persona-learning.ts mock experiments addressed
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Full codebase grep audit and cleanup</name>
  <files>app/api/, lib/, src/</files>
  <action>
  Run comprehensive grep searches across all API routes and services to catch any patterns missed by plans 01-04 and task 1:

  Search patterns (in app/api/, lib/, src/):
  1. `Math.random()` — flag every instance. Acceptable uses: crypto seeding, shuffle algorithms with comment. Unacceptable: data generation, metric simulation, ID generation
  2. `generateMock|mockData|mockResponse|fakeDat|MOCK_` — should be zero
  3. `TODO.*(real|mock|replace|fake|stub|placeholder|hardcoded)` — should be zero or documented
  4. `new Map()` at module level in API routes — should be zero (serverless incompatible)
  5. `localStorage|sessionStorage` in lib/ files — should be zero (server-side code)
  6. Hardcoded arrays used as data sources (look for arrays of objects with realistic-looking data that should come from DB)

  For each pattern found:
  - If it's a genuine mock/stub: fix it (replace with Prisma query or proper error)
  - If it's acceptable (config constants, type definitions): add a comment explaining why it's not mock data
  - If it needs a database model that doesn't exist: return empty state with hint message

  Document every finding in the SUMMARY.md with:
  - File path
  - Pattern found
  - Action taken (fixed / acceptable / deferred to later phase)

  Special attention to src/ directory — it contains legacy service files that may have been overlooked. Check:
  - src/services/advanced-analytics.js — known Math.random() predictions
  - src/services/ai-content.controller.js — known mock scores
  - Any other src/ files with mock patterns
  </action>
  <verify>
  - `grep -r "Math.random" app/api/ --include="*.ts" --include="*.js" -l` returns zero files (or only files with documented acceptable uses)
  - `grep -r "generateMock\|mockData\|MOCK_" app/api/ lib/ --include="*.ts" --include="*.js" -l` returns zero files
  - `grep -rn "TODO.*(real\|mock\|replace\|fake\|stub)" app/api/ lib/ --include="*.ts" --include="*.js"` returns zero matches
  - `npm run type-check` passes
  </verify>
  <done>
  - Full audit of app/api/, lib/, src/ complete
  - Every mock pattern either fixed or documented with justification
  - Zero unaddressed Math.random() data generation in API routes
  - Zero TODO comments about replacing mocks remain
  - Zero in-memory Maps at module level in API routes
  - Comprehensive audit findings documented in SUMMARY.md
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "Math.random" app/api/ --include="*.ts" --include="*.js"` returns zero matches in data generation context
- [ ] `grep -rn "generateMock\|mockData\|MOCK_\|fakeDat" app/api/ lib/ src/ --include="*.ts" --include="*.js"` returns zero matches
- [ ] `grep -rn "TODO.*(real\|mock\|replace\|fake\|stub\|placeholder\|hardcoded)" app/api/ lib/ --include="*.ts" --include="*.js"` returns zero matches
- [ ] `npm run type-check` passes without errors
- [ ] `npm run build` succeeds (full build validation — first time in Phase 2)
</verification>

<success_criteria>

- Zero mock data generation patterns remain in app/api/
- Zero mock-related TODO comments remain in app/api/ or lib/
- Zero in-memory Maps used as data stores in API routes
- All remaining lib/ mock patterns fixed or properly error-handled
- UUID generation uses crypto.randomUUID() not Math.random()
- Full build passes
- Comprehensive audit report in SUMMARY.md documents every finding
- Phase 2 objective achieved: every API route returns real data or proper error
</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-data-api/02-05-SUMMARY.md`

The SUMMARY for this final plan should include a complete audit table:

| File | Pattern | Action | Status |
|------|---------|--------|--------|
| [path] | [what was found] | [what was done] | Fixed/Acceptable/Deferred |

This becomes the definitive record that Phase 2 is complete.
</output>
