---
phase: 127-contact-form
plan: 01
type: standard
---

<objective>
Wire the existing contact form UI at /contact to send a real email to phil@carsi.com.au via Resend SDK.
The form had a complete UI but used a fake setTimeout to simulate submission — no email was ever sent.
</objective>

<context>
@app/contact/page.tsx
@app/api/demo/analyze/route.ts
@lib/email/billing-emails.ts
@lib/rate-limit/presets.ts
</context>

## Task 1: Create /api/contact route

**Files:**

- Create: `app/api/contact/route.ts`

Public POST endpoint. No auth required (visitors don't have accounts).

- Zod validation: name (1–100), email, subject (optional, max 200), message (1–5000)
- `writeDefault` rate limiter (30 req/min) — spam protection
- Resend SDK lazy singleton (same pattern as `lib/email/billing-emails.ts`)
- Send to `phil@carsi.com.au`, `replyTo` set to sender email
- Subject: `[Synthex Contact] {subject} — from {name}`

## Task 2: Wire contact page form

**Files:**

- Modify: `app/contact/page.tsx`

Replace fake `handleSubmit` (setTimeout simulation) with real `fetch('/api/contact')`.
Add error state JSX alongside existing success block.

## Task 3: Update ROUTE_REFERENCE.md

**Files:**

- Modify: `.planning/ROUTE_REFERENCE.md`

Add `POST /api/contact — public` to Recent Changes log.

## Verification

- `npm run type-check` → 0 errors
- `npm run lint` → 0 warnings
- Submit form at /contact → email arrives at phil@carsi.com.au
- Submit blank fields → validation error, no email sent
