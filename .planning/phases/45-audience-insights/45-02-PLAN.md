---
phase: 45-audience-insights
plan: 02
type: execute
---

<objective>
Create UI components and dashboard page for audience insights visualization.

Purpose: Let creators visually explore their audience demographics and optimal posting times.
Output: Demographics charts, best times heatmap, and audience dashboard page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-audience-insights/45-01-SUMMARY.md

**From 45-01:**
@hooks/useAudienceInsights.ts (audience data hook)
@app/api/audience/insights/route.ts (audience API)

**UI patterns to follow:**
@app/dashboard/unified/page.tsx (stats row, insights grid)
@components/unified/PlatformComparisonChart.tsx (Recharts patterns)
@app/dashboard/analytics/page.tsx (analytics page structure)

**Established patterns:**
- Glassmorphic cards (bg-gray-900/50, border-white/10)
- Recharts for visualizations (AreaChart, BarChart, PieChart)
- PageHeader with filters
- Loading skeletons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demographics visualization components</name>
  <files>components/audience/DemographicsCharts.tsx, components/audience/AgeDistributionChart.tsx, components/audience/GenderChart.tsx, components/audience/LocationMap.tsx</files>
  <action>
Create demographic visualization components using Recharts:

**DemographicsCharts.tsx** (container component):
- Props: demographics: AudienceDemographics, isLoading?: boolean
- Grid layout with 3 charts: Age, Gender, Location
- Glassmorphic card wrapper

**AgeDistributionChart.tsx:**
- Horizontal BarChart showing age ranges
- Color gradient from young (cyan) to older (purple)
- Show percentage and count on hover

**GenderChart.tsx:**
- PieChart or RadialBarChart for gender distribution
- Colors: Male (#3B82F6), Female (#EC4899), Other (#8B5CF6)
- Center label showing total audience

**LocationMap.tsx:**
- Horizontal bar chart showing top 10 locations
- Flag emoji or country code prefix
- Percentage bars with counts

All components:
- Handle loading state with skeletons
- Handle empty state gracefully
- Use Recharts ResponsiveContainer
- Custom tooltip with glassmorphic styling
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>Demographics components display age, gender, and location data</done>
</task>

<task type="auto">
  <name>Task 2: Create best times heatmap component</name>
  <files>components/audience/BestTimesHeatmap.tsx</files>
  <action>
Create a 7×24 heatmap showing optimal posting times:

```typescript
interface BestTimesHeatmapProps {
  data: Array<{ day: number; hour: number; engagement: number }>;
  isLoading?: boolean;
}
```

**Layout:**
- Y-axis: Days (Mon-Sun)
- X-axis: Hours (12am-11pm, show every 3 hours)
- Cells: Color intensity based on engagement score
- Color scale: Low (gray) → Medium (cyan) → High (green)

**Features:**
- Hover tooltip: "Monday at 2PM: High engagement (85%)"
- Click cell to see details
- Highlight current day/hour
- Legend showing color scale

**Implementation:**
- Use CSS Grid for layout (not canvas for accessibility)
- Each cell is a div with background color
- Calculate color from engagement value (0-100 scale)
- Glassmorphic container card

**Helper:**
```typescript
function getEngagementColor(value: number): string {
  // 0-33: gray shades
  // 34-66: cyan shades
  // 67-100: green shades
}
```
  </action>
  <verify>Heatmap renders 7×24 grid with correct colors</verify>
  <done>Best times heatmap shows weekly engagement patterns</done>
</task>

<task type="auto">
  <name>Task 3: Create audience dashboard page with navigation</name>
  <files>app/dashboard/audience/page.tsx, app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
**page.tsx - Audience Insights Dashboard:**

Layout:
1. **PageHeader:** "Audience Insights" with platform filter, period selector
2. **Stats row:** Total followers, growth %, avg engagement, top location
3. **Demographics section:** Grid of Age, Gender, Location charts
4. **Behavior section:** Best times heatmap, active hours chart
5. **Growth section:** Follower trend line chart

Use useAudienceInsights hook for data.
Handle loading with skeletons, error with retry.

**layout.tsx update:**
Add to sidebarItems after "Unified View":
```typescript
{ icon: Users, label: 'Audience', href: '/dashboard/audience' },
```

**CommandPalette.tsx update:**
Add command:
```typescript
{
  id: 'audience-insights',
  title: 'Audience Insights',
  description: 'View follower demographics and behavior',
  icon: Users,
  action: () => router.push('/dashboard/audience'),
  category: 'navigation',
  keywords: ['audience', 'demographics', 'followers', 'insights', 'age', 'gender', 'location']
}
```
  </action>
  <verify>Navigate to /dashboard/audience, see loading then data</verify>
  <done>Audience dashboard accessible via sidebar and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] Demographics charts render correctly
- [ ] Heatmap shows 7×24 grid
- [ ] Dashboard page loads with all sections
- [ ] Navigation integrated (sidebar + command palette)
</verification>

<success_criteria>
- All tasks completed
- Charts visualize audience data
- Heatmap shows engagement patterns
- Dashboard is accessible and functional
- Phase 45 complete
</success_criteria>

<output>
After completion, create `.planning/phases/45-audience-insights/45-02-SUMMARY.md`
</output>
