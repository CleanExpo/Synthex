---
phase: 16-ui-polish
plan: 03
type: execute
model: sonnet
---

<objective>
Create consistent page headers and improve empty states across dashboard.

Purpose: Consistent page structure improves navigation and user confidence.
Output: Reusable PageHeader component, improved empty states on key pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-polish/16-01-SUMMARY.md
@.planning/phases/16-ui-polish/16-02-SUMMARY.md

**Existing patterns:**
@components/error-states/api-error.tsx - EmptyState component exists
@components/personas/empty-state.tsx - Example feature-specific empty state

**Pattern from v1.0:**
- Dashboard empty state pattern: inline EmptyState with icon, message, CTA button
- Glassmorphic cards with gradient backgrounds

**Current inconsistencies:**
- Some pages have headers with actions, others don't
- Empty states vary in styling and placement
- Some pages lack clear CTAs when empty
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable PageHeader component</name>
  <files>components/dashboard/page-header.tsx</files>
  <action>
    Create a consistent page header component for dashboard pages.

    components/dashboard/page-header.tsx:
    ```tsx
    'use client';

    import { ReactNode } from 'react';
    import { cn } from '@/lib/utils';

    interface PageHeaderProps {
      title: string;
      description?: string;
      actions?: ReactNode;
      className?: string;
    }

    export function PageHeader({ title, description, actions, className }: PageHeaderProps) {
      return (
        <div className={cn('flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6', className)}>
          <div>
            <h1 className="text-2xl sm:text-3xl font-bold text-white">{title}</h1>
            {description && (
              <p className="text-sm sm:text-base text-slate-400 mt-1">{description}</p>
            )}
          </div>
          {actions && (
            <div className="flex items-center gap-2 sm:gap-3">
              {actions}
            </div>
          )}
        </div>
      );
    }
    ```

    Add export to components/dashboard/index.ts.

    This enables consistent headers like:
    ```tsx
    <PageHeader
      title="Analytics"
      description="Track your social media performance"
      actions={<Button>Export</Button>}
    />
    ```
  </action>
  <verify>
    - Component exists with proper TypeScript props
    - Exported from barrel file
    - Responsive (flex-col on mobile, flex-row on desktop)
    - npm run type-check passes
  </verify>
  <done>PageHeader component created with responsive design</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable EmptyState component for dashboard</name>
  <files>components/dashboard/empty-state.tsx</files>
  <action>
    Create a glassmorphic empty state component matching the design system.

    The existing EmptyState in error-states/ uses light theme colors.
    Create a dashboard-specific version with dark theme styling.

    components/dashboard/empty-state.tsx:
    ```tsx
    'use client';

    import { ReactNode } from 'react';
    import { Card, CardContent } from '@/components/ui/card';
    import { Button } from '@/components/ui/button';
    import { cn } from '@/lib/utils';
    import { LucideIcon } from 'lucide-react';

    interface DashboardEmptyStateProps {
      icon: LucideIcon;
      title: string;
      description: string;
      action?: {
        label: string;
        onClick: () => void;
        icon?: LucideIcon;
      };
      secondaryAction?: {
        label: string;
        onClick: () => void;
      };
      className?: string;
    }

    export function DashboardEmptyState({
      icon: Icon,
      title,
      description,
      action,
      secondaryAction,
      className,
    }: DashboardEmptyStateProps) {
      return (
        <Card className={cn('bg-white/[0.02] backdrop-blur-xl border-white/[0.06]', className)}>
          <CardContent className="py-16 text-center">
            <div className="mx-auto mb-4 h-16 w-16 rounded-full bg-cyan-500/10 flex items-center justify-center">
              <Icon className="h-8 w-8 text-cyan-400" />
            </div>
            <h3 className="text-xl font-semibold text-white mb-2">{title}</h3>
            <p className="text-slate-400 mb-6 max-w-md mx-auto">{description}</p>
            <div className="flex items-center justify-center gap-3">
              {action && (
                <Button onClick={action.onClick} className="gradient-primary text-white">
                  {action.icon && <action.icon className="mr-2 h-4 w-4" />}
                  {action.label}
                </Button>
              )}
              {secondaryAction && (
                <Button variant="outline" onClick={secondaryAction.onClick}>
                  {secondaryAction.label}
                </Button>
              )}
            </div>
          </CardContent>
        </Card>
      );
    }
    ```

    Add export to components/dashboard/index.ts.
  </action>
  <verify>
    - Component matches glassmorphic design system
    - Supports primary and secondary actions
    - Exported from barrel file
    - npm run type-check passes
  </verify>
  <done>DashboardEmptyState component created with proper styling</done>
</task>

<task type="auto">
  <name>Task 3: Update dashboard barrel exports</name>
  <files>components/dashboard/index.ts</files>
  <action>
    Ensure all new dashboard components are properly exported.

    Read current components/dashboard/index.ts and add:
    - export { PageHeader } from './page-header';
    - export { DashboardEmptyState } from './empty-state';
    - export { DashboardError } from './error-fallback';

    Verify existing exports are preserved.
    The barrel file should export all reusable dashboard components.
  </action>
  <verify>
    - All new components exported
    - Existing exports preserved
    - Import works: `import { PageHeader, DashboardEmptyState, DashboardError } from '@/components/dashboard'`
    - npm run type-check passes
  </verify>
  <done>Dashboard component barrel exports updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PageHeader component exists and exports
- [ ] DashboardEmptyState component exists and exports
- [ ] Dashboard barrel file exports all new components
- [ ] npm run type-check passes
- [ ] Components match glassmorphic design system
</verification>

<success_criteria>

- PageHeader component with responsive layout
- DashboardEmptyState component matching design system
- All components exported from barrel file
- No TypeScript errors
- Phase 16 complete

</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-polish/16-03-SUMMARY.md` with:

```yaml
---
phase: 16-ui-polish
plan: 03
status: complete
subsystem: ui
tags: [page-header, empty-states, components, ux]
key-files: [components/dashboard/page-header.tsx, components/dashboard/empty-state.tsx, components/dashboard/index.ts]
affects: []
---
```

Include:
- Components created
- Usage examples
- Phase 16 complete indicator
</output>
