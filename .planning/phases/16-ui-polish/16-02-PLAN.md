---
phase: 16-ui-polish
plan: 02
type: execute
model: sonnet
---

<objective>
Add error boundary coverage and improve empty states across dashboard pages.

Purpose: Graceful error handling and meaningful empty states improve user experience and reduce confusion.
Output: error.tsx files for dashboard routes + improved empty states using existing components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-polish/16-01-SUMMARY.md

**Existing error infrastructure:**
@components/ui/error-boundary.tsx - Full ErrorBoundary with retry, report, home buttons
@components/error-states/api-error.tsx - APIErrorCard, EmptyState, DataStateWrapper

**Pattern from v1.0:**
- Dashboard empty state pattern: inline EmptyState with icon, message, CTA button
- EmptyState component exists in error-states/api-error.tsx

**Pages needing error.tsx:**
Dashboard routes should have error boundaries to prevent full-page crashes.
Next.js error.tsx provides route-level error handling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared DashboardError component</name>
  <files>components/dashboard/error-fallback.tsx, components/dashboard/index.ts</files>
  <action>
    Create a reusable error fallback component for dashboard pages.

    components/dashboard/error-fallback.tsx:
    ```tsx
    'use client';

    import { useEffect } from 'react';
    import { AlertTriangle, RefreshCw, Home } from '@/components/icons';
    import { Button } from '@/components/ui/button';
    import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

    interface DashboardErrorProps {
      error: Error & { digest?: string };
      reset: () => void;
      title?: string;
      description?: string;
    }

    export function DashboardError({
      error,
      reset,
      title = 'Something went wrong',
      description = 'An error occurred while loading this page.',
    }: DashboardErrorProps) {
      useEffect(() => {
        console.error('[DashboardError]', error);
      }, [error]);

      return (
        <div className="min-h-[400px] flex items-center justify-center p-4">
          <Card className="max-w-lg w-full bg-gradient-to-br from-red-500/10 to-rose-500/10 backdrop-blur-xl border border-red-500/20">
            <CardHeader className="text-center pb-4">
              <div className="mx-auto mb-4 h-12 w-12 rounded-full bg-red-500/20 flex items-center justify-center">
                <AlertTriangle className="h-6 w-6 text-red-400" />
              </div>
              <CardTitle className="text-lg text-red-100">{title}</CardTitle>
              <CardDescription className="text-sm text-red-200/70 mt-2">
                {description}
              </CardDescription>
            </CardHeader>
            <CardContent className="space-y-4">
              <details className="group">
                <summary className="cursor-pointer text-xs text-red-200/60 hover:text-red-200/80">
                  Technical details
                </summary>
                <div className="mt-2 p-2 rounded bg-black/20 text-xs text-red-200/80 break-all">
                  {error.message}
                </div>
              </details>
              <div className="flex gap-2">
                <Button
                  onClick={reset}
                  className="flex-1 bg-red-500/30 hover:bg-red-500/40 text-red-100 border border-red-500/40"
                >
                  <RefreshCw className="h-4 w-4 mr-2" />
                  Try Again
                </Button>
                <Button
                  variant="outline"
                  onClick={() => window.location.href = '/dashboard'}
                  className="bg-white/5 border-white/10 text-red-200/80"
                >
                  <Home className="h-4 w-4 mr-2" />
                  Dashboard
                </Button>
              </div>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    Add export to components/dashboard/index.ts barrel file.
  </action>
  <verify>
    - File exists with proper TypeScript types
    - Exported from barrel file
    - npm run type-check passes
  </verify>
  <done>DashboardError component created and exported</done>
</task>

<task type="auto">
  <name>Task 2: Create error.tsx for high-priority dashboard routes</name>
  <files>app/dashboard/analytics/error.tsx, app/dashboard/content/error.tsx, app/dashboard/schedule/error.tsx, app/dashboard/competitors/error.tsx, app/dashboard/personas/error.tsx, app/dashboard/reports/error.tsx</files>
  <action>
    Create Next.js error.tsx files for 6 high-priority routes.

    Pattern for each error.tsx:
    ```tsx
    'use client';

    import { DashboardError } from '@/components/dashboard';

    export default function AnalyticsError({
      error,
      reset,
    }: {
      error: Error & { digest?: string };
      reset: () => void;
    }) {
      return (
        <DashboardError
          error={error}
          reset={reset}
          title="Analytics Error"
          description="Failed to load analytics data. Please try again."
        />
      );
    }
    ```

    Customize title and description for each route:
    - analytics: "Analytics Error" / "Failed to load analytics data"
    - content: "Content Error" / "Failed to load content generator"
    - schedule: "Schedule Error" / "Failed to load content calendar"
    - competitors: "Competitors Error" / "Failed to load competitor data"
    - personas: "Personas Error" / "Failed to load personas"
    - reports: "Reports Error" / "Failed to load reports"
  </action>
  <verify>
    - All 6 error.tsx files exist
    - Each uses DashboardError component
    - npm run type-check passes
  </verify>
  <done>6 error.tsx files created with consistent error handling</done>
</task>

<task type="auto">
  <name>Task 3: Create error.tsx for remaining dashboard routes</name>
  <files>app/dashboard/integrations/error.tsx, app/dashboard/settings/error.tsx, app/dashboard/team/error.tsx, app/dashboard/tasks/error.tsx, app/dashboard/error.tsx</files>
  <action>
    Create error.tsx for remaining high-value routes plus the dashboard root.

    Routes to cover:
    - app/dashboard/error.tsx (root dashboard) - "Dashboard Error"
    - integrations/error.tsx - "Integrations Error"
    - settings/error.tsx - "Settings Error"
    - team/error.tsx - "Team Error"
    - tasks/error.tsx - "Tasks Error"

    Use same pattern as Task 2, customizing title and description.

    The root dashboard error.tsx catches errors from any dashboard route
    that doesn't have its own error boundary.
  </action>
  <verify>
    - All 5 error.tsx files exist
    - Root dashboard has error.tsx
    - npm run type-check passes
  </verify>
  <done>5 more error.tsx files created, including dashboard root</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] DashboardError component exists and exports
- [ ] 11 error.tsx files exist in dashboard routes
- [ ] npm run type-check passes
- [ ] Error pages render correctly (manual test)
</verification>

<success_criteria>

- Shared DashboardError component with retry and home buttons
- 11 dashboard routes have error.tsx files
- Consistent error UI matching existing design system
- No TypeScript errors

</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-polish/16-02-SUMMARY.md` with:

```yaml
---
phase: 16-ui-polish
plan: 02
status: complete
subsystem: ui
tags: [error-handling, error-boundaries, ux]
key-files: [components/dashboard/error-fallback.tsx, app/dashboard/error.tsx]
affects: []
---
```

Include:
- DashboardError component details
- List of error.tsx files created
- Any issues encountered
</output>
