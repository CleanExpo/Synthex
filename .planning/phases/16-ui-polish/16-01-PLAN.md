---
phase: 16-ui-polish
plan: 01
type: execute
model: sonnet
---

<objective>
Create consistent loading states across high-priority dashboard pages.

Purpose: Improve perceived performance and user experience with proper loading skeletons.
Output: loading.tsx files for 10 high-priority dashboard routes using existing skeleton components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Existing skeleton infrastructure:**
@components/skeletons/dashboard-skeletons.tsx - DashboardSkeleton, AnalyticsSkeleton, etc.
@app/dashboard/seo/loading.tsx - Example loading.tsx pattern

**Pattern from v1.0:**
- Dashboard empty state pattern: inline with icon, message, CTA button
- Skeleton components already exist and are comprehensive

**High-priority pages (by user traffic):**
1. /dashboard (main) - has inline skeleton
2. /dashboard/analytics - uses AnalyticsSkeleton but no loading.tsx
3. /dashboard/content
4. /dashboard/schedule
5. /dashboard/competitors
6. /dashboard/personas
7. /dashboard/reports
8. /dashboard/integrations
9. /dashboard/settings
10. /dashboard/team
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx for core dashboard routes</name>
  <files>app/dashboard/analytics/loading.tsx, app/dashboard/content/loading.tsx, app/dashboard/schedule/loading.tsx, app/dashboard/competitors/loading.tsx, app/dashboard/personas/loading.tsx</files>
  <action>
    Create Next.js loading.tsx files for the 5 highest-traffic dashboard routes.

    Pattern to follow (from seo/loading.tsx):
    - Import Card, CardContent from @/components/ui/card
    - Export default function with route-specific name (e.g., AnalyticsLoading)
    - Use animate-pulse class on wrapper div
    - Create skeleton shapes matching the page layout

    For each page:
    - analytics/loading.tsx: Use AnalyticsSkeleton from components/skeletons
    - content/loading.tsx: Header + GenerationSettings skeleton + content area
    - schedule/loading.tsx: Calendar header + grid skeleton
    - competitors/loading.tsx: Use DashboardSkeleton from components/skeletons
    - personas/loading.tsx: Two-column layout skeleton (list + detail)

    Keep skeletons simple - match approximate page structure with bg-white/5 boxes.
  </action>
  <verify>
    - Files exist and export default functions
    - npm run type-check passes
    - Manually verify loading appears when navigating to routes
  </verify>
  <done>5 loading.tsx files created with appropriate skeletons</done>
</task>

<task type="auto">
  <name>Task 2: Create loading.tsx for secondary dashboard routes</name>
  <files>app/dashboard/reports/loading.tsx, app/dashboard/integrations/loading.tsx, app/dashboard/settings/loading.tsx, app/dashboard/team/loading.tsx, app/dashboard/tasks/loading.tsx</files>
  <action>
    Create loading.tsx files for 5 more dashboard routes.

    For each page:
    - reports/loading.tsx: Table skeleton + header
    - integrations/loading.tsx: Grid of platform cards skeleton
    - settings/loading.tsx: Sidebar + form area skeleton
    - team/loading.tsx: Team members grid skeleton
    - tasks/loading.tsx: Kanban board skeleton

    Use existing skeleton components where applicable:
    - TableSkeleton for tables
    - StatsGridSkeleton for cards
    - KanbanBoardSkeleton for tasks

    Import from '@/components/skeletons' barrel export.
  </action>
  <verify>
    - All 5 files exist and export default functions
    - npm run type-check passes
    - No duplicate skeleton definitions (use shared components)
  </verify>
  <done>5 more loading.tsx files created, covering top 10 dashboard routes</done>
</task>

<task type="auto">
  <name>Task 3: Add loading.tsx exports to skeletons barrel</name>
  <files>components/skeletons/index.ts</files>
  <action>
    Ensure components/skeletons/index.ts exists and exports all skeleton components for easy import.

    If index.ts doesn't exist, create it:
    ```ts
    export * from './dashboard-skeletons';
    export { default as DashboardSkeleton } from './DashboardSkeleton';
    ```

    If it exists, verify all exports are present.

    This enables clean imports: `import { DashboardSkeleton, AnalyticsSkeleton } from '@/components/skeletons'`
  </action>
  <verify>
    - index.ts exists with proper exports
    - Import works: `import { DashboardSkeleton } from '@/components/skeletons'`
    - npm run type-check passes
  </verify>
  <done>Skeletons barrel export configured for clean imports</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 10 loading.tsx files exist in dashboard subdirectories
- [ ] All loading.tsx files export default functions
- [ ] npm run type-check passes
- [ ] Skeletons barrel export works
</verification>

<success_criteria>

- 10 high-priority dashboard routes have loading.tsx files
- Consistent skeleton patterns across all loading states
- Clean imports from @/components/skeletons
- No TypeScript errors

</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-polish/16-01-SUMMARY.md` with:

```yaml
---
phase: 16-ui-polish
plan: 01
status: complete
subsystem: ui
tags: [loading-states, skeletons, ux]
key-files: [app/dashboard/analytics/loading.tsx, app/dashboard/content/loading.tsx, components/skeletons/index.ts]
affects: []
---
```

Include:
- List of loading.tsx files created
- Skeleton components used
- Any issues encountered
</output>
