---
phase: 01-foundation
plan: 01
type: execute
model: sonnet
model_rationale: "Plan 01-01 (40% context) implements file archival and bulk deletion. Complexity: 2.1 → Sonnet. Pattern exists (git rm), standard operations."
---

<objective>
Archive the legacy Synthex/ Express app and remove all root-level debris files (288 .md docs, 37 Python scripts, 30+ batch/PowerShell scripts, broken path files).

Purpose: Clean the repository root so subsequent phases work against the real Next.js 15 codebase without confusion from legacy artifacts.
Output: Clean git history with legacy code archived, hundreds of debris files removed from tracking.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

**Current state:**
- `Synthex/` contains legacy Express 4 app (4 tracked files: index.ts, openrouter.ts, 2 HTML files)
- Root has 288 tracked .md documentation debris files (deployment guides, status reports, completion reports)
- Root has 37 tracked .py files (platform scripts, test scripts, AI engines — all legacy/unused)
- Root has 30+ tracked .bat/.ps1 scripts at root level (deploy, setup, launch scripts)
- Some files have broken `D:Synthex` prefix paths in filenames
- `.gitignore` already covers most patterns but tracked files persist

**Constraints:**
- Windows 11 (PowerShell) — use `git rm` not filesystem delete
- Must not remove: CLAUDE.md, README.md, CONTRIBUTING.md, LICENSE, package.json, vercel.json, next.config.mjs, tsconfig.json, or any file in app/, src/, lib/, prisma/, public/, scripts/, .claude/, .planning/, docs/, components/, middleware*, config/
- Must not remove active config: eslint.config.js, postcss.config.cjs, tailwind.config.cjs, jest.config.*, playwright.config.*
- The `Synthex/` directory should be deleted entirely (only 4 files, all legacy Express code)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive legacy Synthex/ Express app and remove root Python scripts</name>
  <files>Synthex/*, *.py (root level)</files>
  <action>
1. Remove the entire `Synthex/` directory from git tracking:
   ```
   git rm -r Synthex/
   ```
   This directory contains only 4 tracked files (index.ts, openrouter.ts, 2 HTML sandbox files) — all legacy Express code superseded by Next.js.

2. Remove all root-level Python scripts from git tracking (37 files):
   ```
   git rm advanced_speech_interface.py ai_reasoning_engine.py automated_content_system.py content_scheduler.py content_transformation_engine.py cpu_manager.py enhanced_platform_optimizer.py execute-workflow.py openrouter_config.py openrouter_workflow.py orchestrator.py performance_tracker.py platform_automation.py platform_content_adapter.py platform_mastery_system.py production_health_check.py run_content_generator.py shoot_the_breeze.py simple_entry_system.py smart_content_generator.py social_media_auth_system.py speech_recognition_engine.py subscription_pricing_engine.py test-cpu-manager.py test_complete_system_integration.py test_content_generation_system.py test_platform_system.py test_platform_system_safe.py test_system_simple.py test_unified_system.py trend_discovery_system.py unicode_utils.py unified_platform_orchestrator.py veo3_integration.py viral_content_analyzer.py
   ```
   Also remove `scripts/generate_imagen.py` and `scripts/generate_nano_banana.py` if they exist.

3. Remove `requirements.txt` (Python dependency file — no longer needed).

4. Add to `.gitignore`:
   ```
   # Legacy (archived)
   Synthex/
   *.py
   !scripts/**/*.py
   !.claude/**/*.py
   ```
  </action>
  <verify>
  - `git status` shows Synthex/ and .py files staged for deletion
  - `ls Synthex/` should fail or show empty
  - `git ls-files "*.py"` returns only files under `.claude/` and `scripts/` (allowed paths)
  </verify>
  <done>All legacy Synthex/ Express files and root Python scripts removed from git tracking. .gitignore updated to prevent re-addition.</done>
</task>

<task type="auto">
  <name>Task 2: Remove root documentation debris and batch scripts</name>
  <files>*.md (root debris), *.bat, *.ps1 (root level)</files>
  <action>
1. Remove all root-level .md documentation debris. Keep ONLY these .md files:
   - `CLAUDE.md` (project instructions)
   - `README.md` (repo readme)
   - `CONTRIBUTING.md` (contribution guide)
   - `PROGRESS.md`, `PROGRESS_SUMMARY.md`, `RAPID_PROGRESS_SUMMARY.md`, `TODO-PROGRESS.md` (progress trackers — to be cleaned in later phase)
   - `IMPLEMENTATION-ROADMAP.md`, `PHASE4-UIUX-ROADMAP.md`, `SYNTHEX-NEXTJS-ROADMAP.md` (active roadmaps)
   - Any .md under subdirectories: `docs/`, `.claude/`, `.planning/`, `agents/`, `tools/`, `supabase/`, `paperbanana/`

   Remove everything else — deployment guides, status reports, completion reports, fix guides, setup guides, etc. Use:
   ```
   git rm 100_PERCENT_COMPLETE.md API_DOCUMENTATION.md API-SETUP-GUIDE.md APPLICATION-GUIDE.md ...
   ```
   (Full list: all .md files at root level except the keep list above)

2. Remove all root-level .bat and .ps1 scripts:
   ```
   git rm deploy.bat execute-complete-build.bat fix-path-permanent.bat launch-build-agent.bat launch-synthex.bat reset-deployment.bat run-comprehensive-test.bat setup.bat setup-claude-bypass.bat start-app.bat start-dev.bat
   git rm check-deployment.ps1 check-deployment-status.ps1 deploy-to-vercel.ps1 fix-path-permanent.ps1 fix-terminal-freezing.ps1 generate-secrets.ps1 health-check.ps1 launch-build-agent.ps1 launch-synthex.ps1 migrate-to-nextjs.ps1 setup.ps1 setup-claude-bypass.ps1 setup-deploy-hook.ps1 start-app.ps1 start-complete.ps1 start-monitoring.ps1 start-safe.ps1 test-and-monitor.ps1
   ```

3. Remove miscellaneous root debris:
   - `build.js`, `build-css.js`, `check-redis-config.js`, `cleanup-duplicates.js`, `fix-tailwind-cdn.js` (legacy build scripts)
   - `deploy.js`, `check-deployment.js`, `seed-prod.js`, `setup-production.js`, `setup-redis-env.js`, `setup-sentry.js` (legacy setup scripts)
   - `validate-all-env.js`, `verify-env.js`, `verify-production-ready.js` (superseded by scripts/ dir)
   - `controlled-autonomous-build.ts`, `start-autonomous-build.ts` (legacy)
   - `Makefile`, `Dockerfile`, `Dockerfile.staging`, `docker-compose*.yml` (Docker artifacts — not used with Vercel)
   - `env-example.txt`, `test_env.example` (superseded by .env.example)
   - `server.log`, `deploy.log`, `build.log`, `full-build.log`, `deployment.log` (log files)
   - `monitoring-dashboard.html` (standalone HTML)
   - `synthex-starter-template.html` (template)
   - `tiktok-implementation.js`, `PlatformAutomationEngine.js` (legacy root JS)
   - `mcp.config.json`, `mcp.deployment.json`, `mcp.development.json`, `mcp.marketing.json`, `mcp_config.json` (legacy MCP configs)
   - `babel.config.cjs.disabled`, `webpack.config.js` (unused build configs)
   - `synthex.manifest.json`, `synthex.system-integration.json` (legacy manifests)
   - Various `vercel.*.json` files (except `vercel.json`): `vercel.fixed.json`, `vercel.staging.json`, `vercel-nextjs.json`, `vercel-static.json`, `vercel-build-fix.config.js`
   - `audit-after.json`, `ENVIRONMENT_ANALYSIS_REPORT.json`, `INTEGRATION-STATUS.json`, `testing-issues.json`, `production-validation-report.json` (one-off reports)
   - `synthex-design-system.css`, `hello.md`, `auto-start.md`, `workflow-status.md`
   - Any files with broken `D:Synthex` prefix in names

4. Update `.gitignore` to add:
   ```
   # Root-level script debris
   *.bat
   !scripts/**/*.bat
   *.ps1
   !scripts/**/*.ps1
   !.claude/**/*.ps1

   # Legacy configs
   mcp.*.json
   mcp_config.json
   vercel.*.json
   !vercel.json

   # Legacy manifests
   synthex.*.json

   # One-off reports
   *-report.json
   *-status.json
   ```
  </action>
  <verify>
  - `git ls-files "*.md" | grep -vE "^(node_modules|\.claude|\.planning|tools|agents|docs|supabase|paperbanana)/" | wc -l` returns < 10
  - `git ls-files "*.bat" "*.ps1" | grep -vE "^(scripts|\.claude)/" | wc -l` returns 0
  - Root directory listing is dramatically cleaner
  - `git status` shows all removals staged
  </verify>
  <done>Root directory cleaned of 250+ debris files. Only essential project files remain at root level. .gitignore updated to prevent debris re-accumulation.</done>
</task>

<task type="auto">
  <name>Task 3: Commit cleanup and verify clean state</name>
  <files>N/A (git operations only)</files>
  <action>
1. Stage all changes and create a single commit:
   ```
   git add -A
   git commit -m "chore(01-01): archive legacy Express app and remove root debris

   - Removed Synthex/ legacy Express 4 app (4 files)
   - Removed 37 root-level Python scripts (unused legacy)
   - Removed 250+ root-level .md documentation debris
   - Removed 30+ root-level .bat/.ps1 scripts
   - Removed legacy configs, manifests, build scripts
   - Updated .gitignore to prevent debris re-accumulation"
   ```

2. Verify clean state:
   - Count remaining files at root level
   - Confirm app/, src/, lib/, prisma/, public/ are intact
   - Confirm .claude/, .planning/, scripts/ are intact
   - Run `git status` to confirm clean working tree
  </action>
  <verify>
  - `git log --oneline -1` shows the cleanup commit
  - `git status` shows clean working tree
  - `git ls-files "app/" | wc -l` returns same count as before (no accidental deletion)
  </verify>
  <done>Single cleanup commit created. Repository root contains only essential project files. No application code affected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Synthex/` directory no longer tracked
- [ ] No .py files at root level tracked (only .claude/ and scripts/ subdirs)
- [ ] Root .md count < 10 (down from 288)
- [ ] No .bat/.ps1 at root level tracked
- [ ] All app code intact: app/, src/, lib/, prisma/, public/
- [ ] `git status` clean
- [ ] Single commit with clear message
</verification>

<success_criteria>

- Legacy Synthex/ Express app removed from tracking
- 250+ debris files removed from root
- .gitignore updated to prevent re-accumulation
- Zero application code affected
- Clean git commit history
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
