---
phase: 01-foundation
plan: 02
type: execute
model: sonnet
model_rationale: "Plan 01-02 (35% context) rewrites CLAUDE.md and consolidates env files. Complexity: 2.5 → Sonnet. Requires reading codebase to describe architecture accurately, but follows known patterns."
---

<objective>
Rewrite CLAUDE.md to reflect the actual Next.js 15 architecture and consolidate 30+ environment files down to essentials.

Purpose: CLAUDE.md is the primary project instructions file — if it's wrong, every subsequent phase gets wrong context. Env file sprawl creates confusion about which variables are real.
Output: Accurate CLAUDE.md, consolidated env files, validated .env.example.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@package.json
@vercel.json
@next.config.mjs
@tsconfig.json

**Current state (after Plan 01-01):**
- Root is cleaned of debris
- CLAUDE.md still incorrectly describes Express 4 architecture
- 30+ .env files exist, most stale or duplicate

**Real architecture (from package.json and app/ structure):**
- Next.js 15 App Router (not Express)
- Prisma ORM with PostgreSQL (Supabase)
- Vercel deployment (serverless, no rootDirectory — deploys from repo root)
- Node 22.x (per engines field)
- Auth: NextAuth.js + JWT + Google/GitHub OAuth
- Payments: Stripe
- AI: OpenRouter, Anthropic SDK, Google AI SDK, OpenAI
- Caching: Upstash Redis, ioredis
- Job queues: BullMQ
- Monitoring: Sentry, PostHog
- UI: Radix UI, Tailwind CSS, Framer Motion, Recharts, Lucide icons
- Testing: Jest, Playwright, Storybook
- State: Zustand, TanStack Query, SWR

**Env files to audit:**
- `.env` — primary local
- `.env.local` — Next.js local overrides
- `.env.development` — development mode
- `.env.staging` — staging
- `.env.production` — production
- `.env.production.template` — production template
- `.env.example` — example for onboarding
- `.env.test` — test environment
- `.env.docker` — Docker (not used with Vercel)
- `.env.docker.example` — Docker example (not used)
- `.env.stripe.example` — Stripe-specific
- `.env.orchestration.example` — orchestration-specific
- `.env.local.example` — local example
- `.env.production.example` — production example
- `.env.production.new` — stale
- `.env.production.local` — stale
- `.env.vercel` — Vercel-specific
- `.env.vercel-check` — one-off check
- `.env.audit.json` — audit report
- `.env.local.cleaned` — cleanup artifact
- `.env.local.backup.*` — backup
- `.env.improved`, `.env.final`, `.env.clean` — cleanup iterations
- `config/.env.production` — misplaced
- `Synthex/.env.txt` — legacy (removed in 01-01)

**Keep:** `.env`, `.env.local`, `.env.example`, `.env.test`
**Remove everything else** — consolidate into `.env.example` as the single source of truth for required variables.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md to reflect actual Next.js 15 architecture</name>
  <files>CLAUDE.md</files>
  <action>
Read the actual codebase structure to build an accurate CLAUDE.md:
1. Scan `app/` directory structure (API routes, dashboard pages, auth flows)
2. Read `prisma/schema.prisma` to list actual models
3. Read `next.config.mjs` for build configuration
4. Read `lib/` directory for service structure
5. Read `middleware.ts` for middleware setup
6. Check `scripts/` for valid utility scripts

Rewrite CLAUDE.md with:

**Project Identity:**
- Name: Synthex
- Stack: Next.js 15 (App Router), TypeScript 5, Prisma ORM, PostgreSQL (Supabase)
- Deployment: Vercel (serverless), deploys from repo root
- Node: 22.x
- OS: Windows 11 (PowerShell)

**Directory Structure:** Accurate tree reflecting:
- `app/` — Next.js App Router (pages, API routes, layouts)
- `lib/` — Shared utilities, services, auth, AI, social, stripe
- `src/` — Source modules (if different from lib/)
- `components/` — React components
- `prisma/` — Schema and migrations
- `public/` — Static assets
- `scripts/` — Utility scripts
- `middleware.ts` — Next.js middleware
- `.claude/` — Claude Code integration (skills, agents, hooks)
- `.planning/` — GSD planning system

**Commands:** Accurate npm scripts from package.json (dev, build, test, db commands)

**Data Models:** Actual Prisma models (read from schema)

**Architecture Notes:** Actual patterns:
- Next.js App Router API routes (not Express)
- NextAuth.js authentication (not Passport.js)
- Prisma ORM (Supabase PostgreSQL)
- Stripe billing integration
- AI via OpenRouter + direct SDKs
- Redis caching (Upstash)
- Sentry + PostHog monitoring

Remove all references to Express, Passport.js, `api/vercel.js` adapter, and legacy `Synthex/` subdirectory.

Keep the Claude Code Integration section mostly as-is (still accurate).
  </action>
  <verify>
  - CLAUDE.md mentions "Next.js 15" not "Express 4"
  - CLAUDE.md directory structure matches actual filesystem
  - CLAUDE.md commands match package.json scripts
  - No references to legacy Synthex/ directory
  </verify>
  <done>CLAUDE.md accurately describes the real Next.js 15 architecture. All references to legacy Express app removed. Directory structure, commands, and architecture notes match reality.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate environment files and update .gitignore</name>
  <files>.env.*, config/.env.*, .gitignore</files>
  <action>
1. Read `.env.example` to understand the canonical variable list.

2. Remove stale/duplicate env files from git tracking:
   ```
   git rm .env.staging .env.production .env.production.template .env.production.example .env.production.new .env.production.local .env.docker .env.docker.example .env.stripe.example .env.orchestration.example .env.local.example .env.vercel .env.vercel-check .env.audit.json .env.local.cleaned .env.improved .env.final .env.clean config/.env.production
   ```
   Also remove any `.env.local.backup.*` files.

3. Ensure `.env.example` contains ALL required variables with placeholder values, organized by section:
   - Database (SUPABASE_URL, DATABASE_URL, DIRECT_URL)
   - Auth (NEXTAUTH_SECRET, NEXTAUTH_URL, GOOGLE_*, GITHUB_*)
   - AI (OPENROUTER_API_KEY, ANTHROPIC_API_KEY, GOOGLE_AI_*)
   - Payments (STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET)
   - Email (SENDGRID_API_KEY, RESEND_API_KEY)
   - Redis (UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN)
   - Monitoring (SENTRY_DSN, NEXT_PUBLIC_POSTHOG_KEY)
   - Social (TWITTER_*, LINKEDIN_*, INSTAGRAM_*, FACEBOOK_*)
   - App (NEXT_PUBLIC_APP_URL, NODE_ENV)

4. Verify `.gitignore` env section covers all patterns (already mostly correct from Plan 01-01).

5. Final env file list should be:
   - `.env` — local development (gitignored)
   - `.env.local` — Next.js local overrides (gitignored)
   - `.env.example` — checked in, source of truth for required vars
   - `.env.test` — checked in, test environment defaults
  </action>
  <verify>
  - `git ls-files ".env*" | grep -v node_modules` returns only `.env.example` and `.env.test`
  - `.env.example` contains all required variable sections
  - No env files with actual secrets are tracked
  </verify>
  <done>Environment files consolidated from 30+ to 4 (2 tracked: .env.example, .env.test; 2 gitignored: .env, .env.local). .env.example is comprehensive and organized.</done>
</task>

<task type="auto">
  <name>Task 3: Commit and update project state</name>
  <files>.planning/STATE.md</files>
  <action>
1. Stage and commit all changes:
   ```
   git add -A
   git commit -m "docs(01-02): rewrite CLAUDE.md for Next.js 15 and consolidate env files

   - Rewrote CLAUDE.md to reflect actual Next.js 15 App Router architecture
   - Removed all references to legacy Express app
   - Consolidated 30+ env files to 4 (2 tracked, 2 gitignored)
   - Updated .env.example as single source of truth for required variables"
   ```

2. Update `.planning/STATE.md`:
   - Current Position: Phase 1, Plan 02 complete
   - Add decision: "CLAUDE.md now reflects Next.js 15 architecture"
   - Update progress bar
  </action>
  <verify>
  - `git log --oneline -1` shows the commit
  - `git status` clean
  - STATE.md reflects completed plans
  </verify>
  <done>Phase 1 Plan 02 committed. STATE.md updated with progress.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CLAUDE.md accurately describes Next.js 15 architecture
- [ ] CLAUDE.md directory structure matches `ls` output
- [ ] Only `.env.example` and `.env.test` tracked
- [ ] No secrets in tracked env files
- [ ] `git status` clean
- [ ] STATE.md updated
</verification>

<success_criteria>

- CLAUDE.md is accurate and complete for the real architecture
- Environment files reduced from 30+ to 4
- .env.example is comprehensive and organized
- All changes committed with clear messages
- STATE.md reflects Phase 1 completion
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
