---
phase: 25-third-party-integrations
plan: 02
type: execute
model: haiku
model_rationale: "Plan 02 (25% context) implements UI components + navigation updates. Complexity score: 0.7 (standard UI, isolated, exact patterns exist). Routing: Haiku."
---

<objective>
Add third-party integration UI to the integrations dashboard page with connect/disconnect flows and navigation updates.

Purpose: Give users a visual interface to manage Canva, Buffer, and Zapier connections alongside existing social platform integrations.
Output: Third-party integration components, updated integrations page, command palette entry.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-third-party-integrations/25-01-SUMMARY.md

# Key existing files:
@app/dashboard/integrations/page.tsx
@components/CommandPalette.tsx
@app/dashboard/layout.tsx
@hooks/use-third-party-integrations.ts
@lib/integrations/types.ts

# Established patterns:
**Tech stack available:** React, Tailwind CSS, Radix UI, Lucide icons, glassmorphic Card variants
**Established patterns:** Card variant="glass", Badge for status, Button with gradient-primary, toast notifications via react-hot-toast, Loader2 spinner for loading states
**Constraining decisions:**
- Glassmorphic styling: bg-white/5, border-white/10, bg-[#0f172a]/80
- Icons from @/components/icons barrel export (Lucide)
- Command palette uses CommandItem interface with id, title, icon, action, category, keywords
- Sidebar items in app/dashboard/layout.tsx sidebarItems array (integrations already listed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create third-party integration UI components</name>
  <files>components/integrations/types.ts, components/integrations/third-party-card.tsx, components/integrations/connect-dialog.tsx, components/integrations/index.ts</files>
  <action>
Create UI components for third-party integration management:

**components/integrations/types.ts:**
- ThirdPartyCardProps: { provider, name, description, icon, category, connected, loading, onConnect, onDisconnect, onConfigure }
- ConnectDialogProps: { open, onOpenChange, provider, requiredFields, onSubmit }

**components/integrations/third-party-card.tsx:**
- 'use client' component
- Render a Card variant="glass" matching existing integration card pattern from app/dashboard/integrations/page.tsx
- Show provider icon (use Palette for Canva, Clock for Buffer, Zap for Zapier from Lucide), name, description, category badge
- Connected state: show "Connected" Badge (emerald), Refresh + Disconnect buttons
- Disconnected state: show "Connect" button with gradient-primary
- Loading state: Loader2 spinner on active button
- Add category badge: "Design" (cyan), "Scheduling" (amber), "Automation" (purple)

**components/integrations/connect-dialog.tsx:**
- 'use client' component
- Use Dialog from @/components/ui/dialog (Radix primitive)
- Render dynamic form fields based on provider's requiredFields:
  - Canva: OAuth flow CTA (button that would redirect to OAuth)
  - Buffer: OAuth flow CTA
  - Zapier: API key input + webhook URL input
- Input fields use Radix Input styling (bg-white/5, border-white/10)
- Submit button with gradient-primary
- Error display if connection fails

**components/integrations/index.ts:**
- Export ThirdPartyCard, ConnectDialog, and types

Follow exact Lucide icon import pattern: import from '@/components/icons'. Check that Palette, Clock, Zap exist in the barrel export — if not, add them.
  </action>
  <verify>npm run type-check passes; the 4 new files in components/integrations/ exist and compile</verify>
  <done>4 files created, ThirdPartyCard renders provider info with connect/disconnect, ConnectDialog handles credential input per provider, all use glassmorphic styling</done>
</task>

<task type="auto">
  <name>Task 2: Update integrations page and command palette</name>
  <files>app/dashboard/integrations/page.tsx, components/CommandPalette.tsx</files>
  <action>
**app/dashboard/integrations/page.tsx:**
- Import useThirdPartyIntegrations hook from hooks/use-third-party-integrations
- Import ThirdPartyCard and ConnectDialog from components/integrations
- Add a "Third-Party Tools" section BELOW the existing social platforms grid:
  - Section header: "Third-Party Tools" with subtitle "Connect design, scheduling, and automation tools"
  - Grid of ThirdPartyCard components (Canva, Buffer, Zapier) — same md:grid-cols-2 lg:grid-cols-3 layout
  - ConnectDialog for the selected provider
- Wire connect/disconnect/refresh actions to the hook
- Keep ALL existing social platform code unchanged — only add below it
- Add a visual separator (border-t border-white/10 with mt-10 pt-10) between social and third-party sections

**components/CommandPalette.tsx:**
- Add integrations entry to commands array in the navigation category:
  ```
  {
    id: 'integrations',
    title: 'Integrations',
    description: 'Manage platform and third-party connections',
    icon: Zap,
    action: () => router.push('/dashboard/integrations'),
    category: 'navigation',
    keywords: ['integrations', 'connect', 'canva', 'buffer', 'zapier', 'third-party', 'tools']
  }
  ```
- Insert after the 'team' entry (before the Actions section)
- Verify Zap icon is imported from @/components/icons

Do NOT modify the sidebar — "Integrations" already exists in sidebarItems.
  </action>
  <verify>npm run type-check passes; grep "Third-Party Tools" app/dashboard/integrations/page.tsx returns a match; grep "integrations" components/CommandPalette.tsx returns the new entry</verify>
  <done>Integrations page shows third-party section below social platforms with connect/disconnect flows; command palette has integrations entry with Canva/Buffer/Zapier keywords; no TypeScript errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes with no new errors
- [ ] Integrations page renders both social platforms and third-party tools sections
- [ ] Command palette includes integrations navigation entry
- [ ] Third-party cards show correct provider info and category badges
- [ ] Connect dialog renders per-provider form fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Integrations page has third-party section with Canva, Buffer, Zapier cards
- Connect/disconnect flows wired to useThirdPartyIntegrations hook
- Command palette searchable by "canva", "buffer", "zapier", "integrations"
- Phase 25 complete
</success_criteria>

<output>
After completion, create `.planning/phases/25-third-party-integrations/25-02-SUMMARY.md`
</output>
