---
phase: 03-mock-data-dashboard
plan: 01
type: execute
---

<objective>
Remove mock data arrays from dashboard config files and replace silent mock fallbacks in dashboard pages with proper error/empty states.

Purpose: Dashboard pages currently silently fall back to hardcoded mock data when API calls fail, making it impossible to detect broken integrations. Users see fake data and think the system works. After this plan, failures show error states and empty data shows empty states with CTAs.
Output: All mock arrays removed from 7 config files. 8 dashboard pages show proper error/empty states instead of fake data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mock-data-api/02-01-SUMMARY.md
@.planning/phases/02-mock-data-api/02-05-SUMMARY.md

**Established patterns from Phase 2:**
- Empty state: `{ data: [], message: "hint" }` for API responses
- Error state: Show error message with retry option
- 501 for unimplemented features with "not yet configured" message
- null for unconfigured metrics (not 0, not Math.random())

**Key files to reference:**
- components/error-states.tsx — existing APIErrorCard component
- components/skeletons.tsx — existing DashboardSkeleton component
- components/personas/index.ts — exports PersonaEmptyState (existing empty state pattern)

**Constraining decisions:**
- [02-01]: Empty state pattern established
- [02-02]: AI unavailable returns error, never substitutes fake content
- [02-05]: Legacy src/ services deferred, client-side animations acceptable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove mock arrays from config files and fix page fallbacks (schedule, team, personas, admin)</name>
  <files>
    components/schedule/schedule-config.ts,
    components/team/team-config.ts,
    components/personas/personas-config.ts,
    components/admin/admin-config.ts,
    app/dashboard/schedule/page.tsx,
    app/dashboard/team/page.tsx,
    app/dashboard/personas/page.tsx,
    app/dashboard/admin/page.tsx
  </files>
  <action>
  **Config file cleanup — remove mock arrays:**

  1. **components/schedule/schedule-config.ts:** Remove `mockScheduledPosts` array (lines 19-64). Keep `platformIcons`, `bestTimes`, and any other config constants.

  2. **components/team/team-config.ts:** Remove `mockTeamMembers` array (lines 8-53) and `mockActivityLog` array (lines 55+). Keep `getRolePermissions()` and other helper functions.

  3. **components/personas/personas-config.ts:** Remove `mockPersonas` array (lines 9-67). Keep `contentTypes` and other config constants.

  4. **components/admin/admin-config.ts:** Remove `mockUsers` array (lines 8-23). Keep `roleOptions`.

  **Page fallback replacement — error/empty states:**

  5. **app/dashboard/schedule/page.tsx:** Remove import of `mockScheduledPosts`. Replace the silent fallback in catch block (line ~88-92) with:
     - `setError('Failed to load scheduled posts')` + `setIsLoading(false)`
     - Remove the `await new Promise(resolve => setTimeout(resolve, 400))` fake delay + `setPosts(mockScheduledPosts)` on empty API response
     - When posts array is empty after successful fetch, show empty state with CTA: "No posts scheduled yet. Create your first post to get started."
     - Use existing `APIErrorCard` component for error display with retry callback

  6. **app/dashboard/team/page.tsx:** Remove import of `mockTeamMembers`. Replace silent fallback (line ~98-101) with `setError(...)`. Show empty state when no team members: "No team members yet. Invite your first team member."

  7. **app/dashboard/personas/page.tsx:** Remove import of `mockPersonas`. Replace silent fallback (line ~86-89 and ~115-118) with `setError(...)`. The page already has `PersonaEmptyState` component — use it when personas array is empty.

  8. **app/dashboard/admin/page.tsx:** Remove import of `mockUsers` from `@/components/admin`. Replace the catch block fallback (lines ~51-52) that sets `setUsers(mockUsers)` with `setError('Failed to fetch users')`. Show empty state when no users: "No users found."

  **Pattern for all pages:** Each page already has `isLoading`, `error` state variables, `DashboardSkeleton`, and `APIErrorCard` imported. Wire them up:
  - Loading: show DashboardSkeleton (already done)
  - Error: show APIErrorCard with retry
  - Empty: show empty state component with CTA
  - Data: show normal content (already done)
  </action>
  <verify>
  - Grep schedule-config.ts for `mockScheduledPosts` — zero matches
  - Grep team-config.ts for `mockTeamMembers` — zero matches
  - Grep personas-config.ts for `mockPersonas` — zero matches
  - Grep admin-config.ts for `mockUsers` — zero matches
  - Grep app/dashboard/ for `mockScheduledPosts|mockTeamMembers|mockPersonas|mockUsers` — zero matches
  - `npx tsc --noEmit` passes
  </verify>
  <done>
  - 4 config files cleaned of mock arrays
  - 4 dashboard pages show error states on API failure (not fake data)
  - 4 dashboard pages show empty states with CTAs when data is empty
  - No silent mock fallbacks remain in schedule, team, personas, or admin pages
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove analytics and dashboard config mocks, fix settings/billing/main dashboard pages</name>
  <files>
    components/analytics/analytics-config.ts,
    components/dashboard/dashboard-config.ts,
    components/settings/settings-config.ts,
    app/dashboard/analytics/page.tsx,
    app/dashboard/settings/page.tsx,
    app/dashboard/billing/page.tsx,
    app/dashboard/page.tsx
  </files>
  <action>
  **Config file cleanup — remove mock arrays:**

  1. **components/analytics/analytics-config.ts:** Remove all 5 mock arrays: `engagementData`, `platformDistribution`, `contentPerformance`, `growthData`, `topPosts` (lines 24-88). Keep `platformColors`, `timeRangeOptions`. Fix `transformPlatformData()` and `transformChartData()` to return empty arrays when API data is empty/undefined (instead of returning mock arrays as fallback).

  2. **components/dashboard/dashboard-config.ts:** Remove `mockTeamMembers`, `mockScheduledPosts`, `mockAIGenerations`, `platformBreakdown` arrays (lines 29-61). Keep `formatTimeAgo()`, `aiQuickActions` (these are UI config, not mock data).

  3. **components/settings/settings-config.ts:** Remove `mockApiKeys` (lines 56-59) and `mockInvoices` (lines 61-65). Keep `defaultNotifications`, `defaultPrivacy`, `defaultAdvanced`, `initialPlatforms`, `platformConnections`, `defaultBillingInfo` — these are configuration defaults, not mock data.

  **Page fallback replacement:**

  4. **app/dashboard/analytics/page.tsx:** Remove hardcoded fallback numbers (lines ~105-110) where `reach: 2400000`, `engagement: 184500`, etc. Replace with `0` or `null` when API data unavailable. Show "No analytics data yet" message when all metrics are empty. Fix silent catch blocks to set error state.

  5. **app/dashboard/settings/page.tsx:** Remove import of `mockApiKeys`. Replace `Math.random().toString(36)` in `handleCreateApiKey()` with proper API call to create API key endpoint, or if no endpoint exists, show error: "API key management not yet available." Fix silent billing/invoice fetch failures to show error state.

  6. **app/dashboard/billing/page.tsx:** Fix silent billing/invoice fetch failures (lines ~154-156, ~173-175) to set error state instead of silently keeping defaults.

  7. **app/dashboard/page.tsx:** Remove hardcoded trending topics fallback `['#AI', '#SocialMedia', '#Marketing', '#Growth']` (line ~69-75). Replace with empty array — the trending API now returns proper empty state (fixed in 02-03). Use real API response or show "No trending topics yet."
  </action>
  <verify>
  - Grep analytics-config.ts for `engagementData|platformDistribution|contentPerformance|growthData|topPosts` — zero matches (as exports)
  - Grep dashboard-config.ts for `mockTeamMembers|mockScheduledPosts|mockAIGenerations` — zero matches
  - Grep settings-config.ts for `mockApiKeys|mockInvoices` — zero matches
  - Grep app/dashboard/ for `Math.random` — zero matches in data generation context
  - `npx tsc --noEmit` passes
  </verify>
  <done>
  - 3 config files cleaned of mock arrays
  - Analytics page shows proper zeros/empty state instead of hardcoded numbers
  - Settings page doesn't generate mock API keys
  - Billing page shows error on fetch failure
  - Dashboard main page uses real trending data or empty state
  - Transform functions return empty arrays (not mock data) on missing API data
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "mockPersonas\|mockTeamMembers\|mockScheduledPosts\|mockUsers\|mockApiKeys\|mockInvoices\|mockAIGenerations\|mockActivityLog" components/ app/dashboard/ --include="*.ts" --include="*.tsx"` returns zero matches
- [ ] `grep -rn "engagementData\|platformDistribution\|contentPerformance\|growthData\|topPosts" components/analytics/analytics-config.ts` returns zero matches (as exported mock arrays)
- [ ] `grep -rn "Math.random" app/dashboard/ --include="*.tsx"` returns zero matches in data generation context
- [ ] `npx tsc --noEmit` passes without errors
</verification>

<success_criteria>

- Zero mock data arrays remain in config files (7 config files cleaned)
- Zero silent fallbacks to mock data in dashboard pages
- All dashboard pages show proper error states on API failure
- All dashboard pages show empty states with CTAs on empty data
- Analytics transform functions return empty arrays (not mock data) on missing API
- Settings page doesn't generate fake API keys with Math.random()
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-mock-data-dashboard/03-01-SUMMARY.md`
</output>