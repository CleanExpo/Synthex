---
phase: 03-mock-data-dashboard
plan: 02
type: execute
---

<objective>
Fix remaining dashboard pages with mock fallbacks (experiments, tasks), remove inline Math.random() from MetricsTable, and perform full audit sweep of all dashboard components.

Purpose: After 03-01 cleaned 7 config files and 8 dashboard pages, this plan catches the remaining pages that define mock data inline or import from dedicated mock-data files. Also audits all dashboard-visible components for Math.random() data generation.
Output: Zero mock fallbacks in any dashboard page. MetricsTable shows real data or dashes. Full audit table documenting remaining mock components (deferred to later phases).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mock-data-dashboard/03-01-SUMMARY.md

**Established patterns from Phase 2 and 03-01:**
- Empty state: show message with CTA (e.g., "No posts scheduled yet. Create your first post.")
- Error state: use APIErrorCard with retry callback
- Loading: use DashboardSkeleton (already wired in most pages)
- Data: show normal content

**Key files to reference:**
- components/error-states.tsx — APIErrorCard component
- components/skeletons.tsx — DashboardSkeleton, TasksSkeleton
- lib/ab-testing.ts — ABTestingService (uses real Supabase queries, returns [] on error)

**Constraining decisions:**
- [02-05]: Client-side animations (Math.random for positions/colors) are acceptable
- [02-05]: Legacy src/ services deferred
- SentimentAnalysis.tsx, AIHashtagGenerator.tsx, AIPersonaManager.tsx, PredictiveAnalytics.tsx, RealTimeAnalytics.tsx, ROICalculator.tsx — NOT imported by any dashboard page. These are standalone feature components that need backing APIs from Phase 5+. Defer, don't fix.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix experiments, tasks, and MetricsTable mock fallbacks</name>
  <files>
    app/dashboard/experiments/page.tsx,
    app/dashboard/tasks/page.tsx,
    components/tasks/mock-data.ts,
    components/analytics/metrics-table.tsx
  </files>
  <action>
  **Page fallback replacement:**

  1. **app/dashboard/experiments/page.tsx:** Remove the inline `mockExperiments` array (lines 73-117) and the silent fallback `const displayExperiments = experiments.length > 0 ? experiments : mockExperiments` (line 120). Replace with:
     - When `experiments` is empty after successful fetch, show empty state: "No experiments yet. Create your first A/B test to get started." with a CTA button that triggers `setShowNewExperimentForm(true)`.
     - The page already has proper error handling via `toast.error('Failed to load experiments')` in the catch block — verify it also sets an `error` state for displaying `APIErrorCard`.

  2. **app/dashboard/tasks/page.tsx:** Remove import of `mockTasks` from `@/components/tasks`. Replace ALL mock fallback patterns:
     - Line ~90-96: catch block and empty response both fall back to `setTasks(mockTasks)` — replace with `setError('Failed to load tasks')` in catch, and show empty state when API returns empty array.
     - Line ~115: `setTasks(apiTasks.length > 0 ? apiTasks : mockTasks)` — replace with `setTasks(apiTasks)` (real empty is real empty).
     - Line ~120-123: retry handler falls back to mockTasks — replace with `setError('Failed to load tasks')`.
     - Remove the `await new Promise(resolve => setTimeout(resolve, 500))` fake delay before mock fallback.
     - When tasks array is empty after successful fetch, show empty state: "No tasks yet. Create your first task to get started."
     - Page already imports `TasksSkeleton` and `APIErrorCard` — wire them to error state.

  3. **components/tasks/mock-data.ts:** Delete this file entirely — it only contains the `mockTasks` array. Update `components/tasks/index.ts` to remove the re-export of `mockTasks`.

  4. **components/analytics/metrics-table.tsx:** The component renders `Math.random()` directly in JSX (lines 53-66), generating different fake numbers on every render. Replace with:
     - Accept a `data` prop: `{ platform: string; followers: number; posts: number; engagement: number; reach: number; growth: number }[]`
     - If no data prop provided or array is empty, show dashes ("—") in each cell instead of random numbers.
     - Update the analytics page import to pass real data or let it show dashes.
  </action>
  <verify>
  - Grep experiments/page.tsx for `mockExperiments` — zero matches
  - Grep tasks/page.tsx for `mockTasks` — zero matches
  - Verify components/tasks/mock-data.ts is deleted
  - Grep metrics-table.tsx for `Math.random` — zero matches
  - `npx tsc --noEmit` passes
  </verify>
  <done>
  - Experiments page shows empty state instead of inline mock data
  - Tasks page shows error/empty states instead of falling back to mock tasks
  - mock-data.ts deleted from components/tasks/
  - MetricsTable accepts data via props, shows dashes when empty
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Full dashboard and component audit sweep</name>
  <files>
    app/dashboard/**/*.tsx,
    components/**/*.tsx,
    components/**/*.ts
  </files>
  <action>
  **Comprehensive audit of all remaining mock patterns:**

  1. **Grep all dashboard pages** for: `mock|Mock|MOCK|Math\.random|setTimeout.*set[A-Z].*fake|generateMock|hardcoded|fallback.*\[`
     - Every match must be classified as: Fixed (by 03-01 or Task 1), Acceptable (UI animation, loading delay), or Needs Fix.
     - Fix any remaining "Needs Fix" patterns found.

  2. **Grep all components/** for: `generateMock|mockData|MOCK_|Math\.random` in .ts/.tsx files
     - Classify each match:
       - **Fixed**: Already addressed in 03-01 or Task 1
       - **Acceptable**: Client-side animations (visuals/*, skeletons/*, marketing/*), UI position randomization
       - **Deferred**: Standalone feature components not used in dashboard pages (SentimentAnalysis, AIHashtagGenerator, AIPersonaManager, AIABTesting, PredictiveAnalytics, RealTimeAnalytics, ROICalculator) — document in summary for Phase 5+

  3. **Verify all config file barrel exports** (components/*/index.ts) no longer export mock arrays that were removed in 03-01.

  4. **Create full audit table** documenting every mock pattern found, its classification, and status. Include in SUMMARY.md.

  5. **Final verification:**
     - `grep -rn "mockPersonas\|mockTeamMembers\|mockScheduledPosts\|mockUsers\|mockApiKeys\|mockInvoices\|mockAIGenerations\|mockActivityLog\|mockExperiments\|mockTasks" components/ app/dashboard/ --include="*.ts" --include="*.tsx"` returns zero matches
     - `grep -rn "Math.random" app/dashboard/ --include="*.tsx"` returns zero matches
     - `npx tsc --noEmit` passes
  </action>
  <verify>
  - Zero named mock imports remain in any dashboard page
  - Zero Math.random() calls remain in any dashboard page (app/dashboard/**/*.tsx)
  - All barrel exports (components/*/index.ts) clean of mock re-exports
  - Full audit table created
  - `npx tsc --noEmit` passes
  </verify>
  <done>
  - Complete audit table of all mock patterns in components/ and dashboard/
  - Zero mock imports or Math.random() data generation in any dashboard page
  - Deferred components documented with rationale
  - Phase 3 objective achieved: dashboard pages never show fake data
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "mockPersonas\|mockTeamMembers\|mockScheduledPosts\|mockUsers\|mockApiKeys\|mockInvoices\|mockAIGenerations\|mockActivityLog\|mockExperiments\|mockTasks" app/dashboard/ --include="*.ts" --include="*.tsx"` returns zero matches
- [ ] `grep -rn "Math.random" app/dashboard/ --include="*.tsx"` returns zero matches
- [ ] `npx tsc --noEmit` passes without errors
- [ ] Full audit table in SUMMARY.md
</verification>

<success_criteria>

- Zero mock fallbacks in experiments, tasks pages
- MetricsTable shows real data or dashes (not random numbers)
- components/tasks/mock-data.ts deleted
- Full audit table documenting every mock pattern across components/ and app/dashboard/
- Standalone mock components (SentimentAnalysis, etc.) documented as deferred
- Zero Math.random() in any dashboard page
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-mock-data-dashboard/03-02-SUMMARY.md`
</output>
