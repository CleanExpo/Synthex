---
phase: 43-link-in-bio
plan: 02
type: execute
---

<objective>
Create Link in Bio dashboard editor with themes, preview, and navigation.

Purpose: Enable users to visually design and manage their bio pages.
Output: Dashboard page with editor, theme picker, live preview, and navigation integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-link-in-bio/43-01-SUMMARY.md

**Existing patterns:**
@app/dashboard/listening/page.tsx (two-column dashboard)
@app/dashboard/reports/builder/page.tsx (drag-drop builder)
@components/dashboard/page-header.tsx

**Dashboard patterns:**
- PageHeader with actions
- Two-column layout (editor + preview)
- Modal dialogs for forms
- Glassmorphic styling

**Established conventions:**
- Sidebar navigation integration
- Command palette commands
- Responsive design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard bio page list</name>
  <files>app/dashboard/bio/page.tsx</files>
  <action>
Create bio pages listing dashboard:

**Route:** /dashboard/bio

**Layout:**
- PageHeader: "Link in Bio" with "Create Page" CTA
- Stats row: Total pages, Total views, Total clicks
- Pages grid (cards showing each bio page)

**Page card:**
- Avatar thumbnail
- Page title and slug
- Status badge (Published/Draft)
- Stats: Views, Clicks
- Actions: Edit, Preview, Copy URL, Delete

**Empty state:**
- "Create your first Link in Bio page"
- CTA to create page

**Create page modal:**
- Title input (required)
- Auto-generate slug preview
- Create button

Styling: Glassmorphic cards (bg-gray-900/50, border-white/10)
  </action>
  <verify>Page renders at /dashboard/bio with create functionality</verify>
  <done>Bio pages list dashboard with create and manage actions</done>
</task>

<task type="auto">
  <name>Task 2: Create bio page editor</name>
  <files>app/dashboard/bio/[pageId]/page.tsx</files>
  <action>
Create full-featured bio page editor:

**Route:** /dashboard/bio/[pageId]

**Layout:** Two-column
- Left (2/3): Editor panel
- Right (1/3): Live preview (mobile frame)

**Editor sections:**

1. **Profile section:**
   - Avatar upload/URL input
   - Cover image upload/URL input
   - Title input
   - Bio textarea

2. **Links section:**
   - Draggable link list
   - Add link button
   - Each link item:
     - Title input
     - URL input
     - Icon picker (emoji, lucide, or image URL)
     - Visibility toggle
     - Highlight toggle
     - Delete button
   - Drag handle for reordering

3. **Social links section:**
   - Platform icons grid (Twitter, Instagram, YouTube, TikTok, etc.)
   - Click to add/edit URL for each platform
   - Toggle visibility per platform

4. **Theme section:**
   - Theme preset selector (cards with preview)
   - Custom colors (primary, background, text)
   - Button style selector (rounded, pill, square)
   - Branding toggle

**Header actions:**
- Save button (disabled if no changes)
- Publish/Unpublish toggle
- Preview in new tab button
- Copy public URL button

**Auto-save:** Debounced save on changes (optional)
  </action>
  <verify>Editor loads page data, saves changes</verify>
  <done>Bio page editor with all sections and live preview</done>
</task>

<task type="auto">
  <name>Task 3: Create live preview component</name>
  <files>components/bio/BioPagePreview.tsx</files>
  <action>
Create real-time preview component:

```typescript
interface BioPagePreviewProps {
  page: {
    title: string;
    bio: string | null;
    avatarUrl: string | null;
    coverUrl: string | null;
    theme: string;
    primaryColor: string;
    backgroundColor: string;
    textColor: string;
    buttonStyle: string;
    socialLinks: Array<{ platform: string; url: string }>;
    showBranding: boolean;
  };
  links: Array<{
    id: string;
    title: string;
    url: string;
    iconType: string | null;
    iconValue: string | null;
    isVisible: boolean;
    isHighlighted: boolean;
  }>;
}
```

**Features:**
- Mobile device frame (iPhone-style bezel)
- Scrollable content area
- Real-time updates as user edits
- Theme colors applied dynamically
- Clickable links (open in new tab)
- Accurate rendering of final public page

**Styling:**
- Fixed height (600px mobile viewport)
- Centered in preview panel
- Drop shadow for depth
- Rounded corners for device frame
  </action>
  <verify>Preview updates in real-time with changes</verify>
  <done>Live preview component renders page accurately</done>
</task>

<task type="auto">
  <name>Task 4: Create theme presets</name>
  <files>lib/bio/themes.ts, components/bio/ThemePicker.tsx</files>
  <action>
Create theme system:

**lib/bio/themes.ts:**
```typescript
export interface BioTheme {
  id: string;
  name: string;
  primaryColor: string;
  backgroundColor: string;
  textColor: string;
  buttonStyle: 'rounded' | 'pill' | 'square';
  preview: string; // CSS gradient for preview card
}

export const BIO_THEMES: BioTheme[] = [
  {
    id: 'default',
    name: 'Synthex',
    primaryColor: '#06b6d4',
    backgroundColor: '#0f172a',
    textColor: '#ffffff',
    buttonStyle: 'rounded',
    preview: 'linear-gradient(135deg, #0f172a 0%, #1e293b 100%)',
  },
  {
    id: 'minimal',
    name: 'Minimal',
    primaryColor: '#000000',
    backgroundColor: '#ffffff',
    textColor: '#000000',
    buttonStyle: 'square',
    preview: 'linear-gradient(135deg, #ffffff 0%, #f1f5f9 100%)',
  },
  {
    id: 'gradient',
    name: 'Gradient',
    primaryColor: '#8b5cf6',
    backgroundColor: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
    textColor: '#ffffff',
    buttonStyle: 'pill',
    preview: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
  },
  {
    id: 'dark',
    name: 'Midnight',
    primaryColor: '#f59e0b',
    backgroundColor: '#000000',
    textColor: '#ffffff',
    buttonStyle: 'rounded',
    preview: 'linear-gradient(135deg, #000000 0%, #1a1a2e 100%)',
  },
  {
    id: 'neon',
    name: 'Neon',
    primaryColor: '#22d3ee',
    backgroundColor: '#0c0a09',
    textColor: '#22d3ee',
    buttonStyle: 'pill',
    preview: 'linear-gradient(135deg, #0c0a09 0%, #1c1917 100%)',
  },
  {
    id: 'forest',
    name: 'Forest',
    primaryColor: '#22c55e',
    backgroundColor: '#14532d',
    textColor: '#ffffff',
    buttonStyle: 'rounded',
    preview: 'linear-gradient(135deg, #14532d 0%, #166534 100%)',
  },
];
```

**components/bio/ThemePicker.tsx:**
- Grid of theme cards
- Visual preview of each theme
- Current selection highlighted
- Click to apply theme colors
- Custom color inputs below presets
  </action>
  <verify>Theme picker displays all presets, applies on selection</verify>
  <done>Theme system with 6 presets and custom colors</done>
</task>

<task type="auto">
  <name>Task 5: Add navigation and command palette</name>
  <files>app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
1. In layout.tsx sidebarItems, add after "Listening":
   - Icon: Link2 (chain link icon)
   - Label: "Link in Bio"
   - Path: /dashboard/bio

2. In CommandPalette.tsx commands, add:
   - "Link in Bio Pages" → /dashboard/bio
   - "Create Bio Page" → /dashboard/bio?action=create

3. Ensure icon imports are correct (Link2 from lucide)
  </action>
  <verify>Sidebar shows Link in Bio, Command palette finds commands</verify>
  <done>Link in Bio accessible via navigation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] Pages list renders at /dashboard/bio
- [ ] Editor loads and saves page data
- [ ] Live preview updates in real-time
- [ ] Theme presets apply correctly
- [ ] Navigation includes Link in Bio
- [ ] Command palette has bio commands
</verification>

<success_criteria>
- All tasks completed
- Dashboard editor functional
- Live preview accurate
- Theme system works
- Navigation integrated
- Phase 43 complete
</success_criteria>

<output>
After completion, create `.planning/phases/43-link-in-bio/43-02-SUMMARY.md` with:
- Phase completion status
- Files created/modified
- Theme presets available
- Known limitations
</output>
