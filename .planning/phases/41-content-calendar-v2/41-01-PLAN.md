---
phase: 41-content-calendar-v2
plan: 01
type: execute
---

<objective>
Create Content Calendar dashboard page with useCalendar hook and team filtering.

Purpose: Surface the existing calendar infrastructure in the dashboard with team collaboration support.
Output: Working /dashboard/calendar page with week view, team filtering, and navigation integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Existing calendar infrastructure:**
@components/calendar/WeekView.tsx
@components/calendar/CalendarTypes.ts
@src/services/content/calendar-service.ts
@app/api/content/calendar/route.ts

**Established patterns:**
- Dashboard pages: app/dashboard/*/page.tsx with PageHeader + content
- Hooks: hooks/use*.ts with SWR/fetch patterns
- Navigation: components/dashboard/sidebar.tsx, lib/command-palette-commands.ts
- Glassmorphic UI: bg-gray-900/50, border-white/10, text-cyan-400 accents

**Tech stack:**
- SWR for data fetching (or fetch + useState pattern)
- CalendarService already handles all business logic
- WeekView component ready for use

**Decisions:**
- CalendarPost.userId field exists for team filtering
- API already supports organizationId scoping
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCalendar hook with team filtering</name>
  <files>hooks/useCalendar.ts</files>
  <action>
Create hook that:
1. Fetches calendar view from /api/content/calendar with date range params
2. Supports team member filtering via userId query param
3. Provides methods: schedulePost, reschedulePost, cancelPost
4. Manages loading/error states
5. Returns posts, conflicts, suggestions, stats, isLoading, error

Pattern to follow (from existing hooks):
```typescript
export function useCalendar(options: {
  organizationId: string;
  startDate?: Date;
  endDate?: Date;
  userId?: string; // For team filtering
})
```

Use fetch + useState pattern (not SWR) to match existing calendar components.
Include optimistic updates for reschedule (update local state, then API call).
  </action>
  <verify>npm run type-check passes, hook file exists with correct exports</verify>
  <done>useCalendar hook exports getCalendarView, schedulePost, reschedulePost with team filtering support</done>
</task>

<task type="auto">
  <name>Task 2: Create /dashboard/calendar page</name>
  <files>app/dashboard/calendar/page.tsx</files>
  <action>
Create calendar dashboard page:
1. Import WeekView from @/components/calendar/WeekView
2. Use useCalendar hook to fetch data
3. Add PageHeader with title "Content Calendar"
4. Add team member filter dropdown (fetch users from org)
5. Wire up onPostClick (open detail modal), onPostReschedule, onPostCreate
6. Include PostDetailModal for viewing/editing posts
7. Add "Schedule Post" CTA button that opens quick scheduler

Layout structure:
- PageHeader: "Content Calendar" with team filter + "Schedule Post" button
- Main content: WeekView component with full calendar data
- Modal: PostDetailModal for post details

Styling: Follow glassmorphic pattern with bg-gray-900/50, border-white/10.
Handle loading state with skeleton, error state with retry button.
  </action>
  <verify>npm run build passes, page renders at /dashboard/calendar</verify>
  <done>Calendar page displays week view with posts, supports drag-drop rescheduling, team filtering dropdown works</done>
</task>

<task type="auto">
  <name>Task 3: Add calendar to navigation</name>
  <files>components/dashboard/sidebar.tsx, lib/command-palette-commands.ts</files>
  <action>
1. In sidebar.tsx: Add calendar link to navigation
   - Icon: CalendarIcon from lucide-react
   - Label: "Calendar"
   - Path: /dashboard/calendar
   - Position: After "Content" section (logical grouping)

2. In command-palette-commands.ts: Add calendar commands
   - "Go to Calendar" → /dashboard/calendar
   - "Schedule Post" → /dashboard/calendar?action=schedule (opens with modal)

Follow existing patterns in both files.
  </action>
  <verify>Sidebar shows Calendar link, Command palette finds "calendar" commands</verify>
  <done>Calendar accessible via sidebar navigation and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] `npm run build` passes
- [ ] /dashboard/calendar page loads without errors
- [ ] WeekView displays with navigation arrows
- [ ] Team filter dropdown appears (even if empty org)
- [ ] Sidebar shows Calendar navigation item
- [ ] Command palette includes calendar commands
</verification>

<success_criteria>
- All tasks completed
- Calendar dashboard page functional
- Hook handles API communication
- Team filtering UI present
- Navigation integrated
</success_criteria>

<output>
After completion, create `.planning/phases/41-content-calendar-v2/41-01-SUMMARY.md`
</output>
