---
phase: 41-content-calendar-v2
plan: 02
type: execute
---

<objective>
Add MonthView component and approval status integration to Content Calendar.

Purpose: Complete the calendar v2 feature set with month view and team workflow visibility.
Output: Month view calendar, approval badges on posts, view switcher controls.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-content-calendar-v2/41-01-SUMMARY.md

**Calendar components:**
@components/calendar/WeekView.tsx
@components/calendar/DraggablePostCard.tsx
@components/calendar/CalendarTypes.ts

**Approval system (from Phase 27):**
@prisma/schema.prisma (ApprovalRequest model)
@app/api/approvals/route.ts

**Established patterns:**
- View components follow WeekView structure with @dnd-kit
- Approval status: 'pending', 'in_review', 'approved', 'rejected', 'revision_requested'
- Glassmorphic UI: bg-gray-900/50, border-white/10
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MonthView component</name>
  <files>components/calendar/MonthView.tsx, components/calendar/index.ts</files>
  <action>
Create MonthView calendar component:
1. Display 4-6 week grid (based on month start day)
2. Each day cell shows up to 3 posts with "+X more" overflow
3. Drag-and-drop support via @dnd-kit (same pattern as WeekView)
4. Month navigation (prev/next arrows, month/year header)
5. Today highlight with cyan accent
6. Platform color indicators on posts

Props (extend CalendarViewProps from CalendarTypes.ts):
- posts: ScheduledPost[]
- currentDate: Date
- onPostClick, onPostReschedule, onPostCreate
- onMonthChange: (direction: 'prev' | 'next') => void

Use existing DndContext pattern from WeekView.
Export from components/calendar/index.ts.

Styling: Glassmorphic with bg-gray-900/50, cells have hover state, optimal days highlighted.
  </action>
  <verify>npm run type-check passes, MonthView renders 6-week grid</verify>
  <done>MonthView component displays month grid with drag-drop posts and navigation</done>
</task>

<task type="auto">
  <name>Task 2: Add approval status to post cards</name>
  <files>components/calendar/DraggablePostCard.tsx, hooks/useCalendar.ts, app/api/content/calendar/route.ts</files>
  <action>
Integrate approval status display:

1. Extend ScheduledPost type in CalendarTypes.ts:
   - Add approvalStatus?: 'pending' | 'in_review' | 'approved' | 'rejected' | 'revision_requested'
   - Add approvalId?: string

2. Update API route (GET) to join ApprovalRequest:
   - Query ApprovalRequest where contentId = CalendarPost.id AND contentType = 'post'
   - Include status in response

3. Update DraggablePostCard.tsx:
   - Add status badge (colored dot or icon)
   - pending: yellow, in_review: blue, approved: green, rejected: red, revision_requested: orange
   - Show tooltip with status text on hover
   - Position: top-right corner of card

4. Update useCalendar hook to handle new approvalStatus field

Badge styling: Small colored dot with 'w-2 h-2 rounded-full' + status color.
Approved posts get subtle green border/glow.
  </action>
  <verify>Post cards show approval status badges, colors match status</verify>
  <done>Calendar posts display approval status visually, API returns approval data</done>
</task>

<task type="auto">
  <name>Task 3: Add view switcher and complete page integration</name>
  <files>app/dashboard/calendar/page.tsx</files>
  <action>
Add view mode controls to calendar page:

1. View switcher buttons (Week | Month):
   - Tab-style buttons: bg-white/10 inactive, bg-cyan-500/20 active
   - Store view mode in useState or URL param
   - Render WeekView or MonthView based on selection

2. Team filter enhancement:
   - Fetch team members from org
   - "All Members" default option
   - User avatars in dropdown if available

3. Quick stats bar (above calendar):
   - Total scheduled this week/month
   - Pending approvals count
   - Conflicts count (with warning icon)

4. Ensure smooth transitions between views:
   - Keep current date when switching
   - Maintain filter state

Layout: View switcher in PageHeader area, stats below header, calendar fills remaining space.
  </action>
  <verify>View switcher toggles between Week and Month views, stats display correctly</verify>
  <done>Calendar page has view switcher, team filter works, approval status visible on all posts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] `npm run build` passes
- [ ] MonthView renders 6-week grid correctly
- [ ] Drag-drop works in both Week and Month views
- [ ] Approval badges show on posts with active approvals
- [ ] View switcher toggles between Week/Month
- [ ] Stats bar shows scheduled/approval/conflict counts
</verification>

<success_criteria>
- All tasks completed
- MonthView component functional with drag-drop
- Approval status visible on calendar posts
- View switcher works
- Phase 41 complete
</success_criteria>

<output>
After completion, create `.planning/phases/41-content-calendar-v2/41-02-SUMMARY.md` with:
- Phase completion status
- Files created/modified
- Any deviations or decisions
</output>
