---
phase: 46-content-performance-ai
plan: 02
type: execute
---

<objective>
Create UI components and dashboard page for content performance visualization.

Purpose: Let creators understand what content works and why through visual insights.
Output: Performance charts, insights cards, and content performance dashboard.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-content-performance-ai/46-01-SUMMARY.md

**From 46-01:**
@hooks/useContentPerformance.ts (performance data hook)
@app/api/content/performance/route.ts (performance API)
@lib/ai/content-performance-analyzer.ts (analyzer service)

**UI patterns to follow:**
@app/dashboard/audience/page.tsx (dashboard layout, stats row)
@components/audience/BestTimesHeatmap.tsx (heatmap pattern)
@components/unified/PlatformComparisonChart.tsx (Recharts patterns)

**Established patterns:**
- Glassmorphic cards (bg-gray-900/50, border-white/10)
- Recharts for visualizations
- PageHeader with filters
- Loading skeletons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create performance visualization components</name>
  <files>components/performance/PerformanceOverview.tsx, components/performance/TopPostsGrid.tsx, components/performance/PatternCharts.tsx</files>
  <action>
Create performance visualization components:

**PerformanceOverview.tsx** (stats row):
- Props: summary from ContentPerformanceAnalysis
- 4 stat cards: Total Posts, Avg Engagement, Top Performer %, Trend
- Glassmorphic styling

**TopPostsGrid.tsx:**
- Props: topPerforming, lowPerforming posts
- Two columns: "Top Performers" and "Needs Improvement"
- Each post shows: platform icon, content preview, metrics, date
- Hover to expand content

**PatternCharts.tsx:**
- Props: patterns from ContentPerformanceAnalysis
- 4 mini charts in grid:
  1. Best Days (horizontal bar)
  2. Best Hours (line chart)
  3. Best Length (range indicator)
  4. Top Hashtags (horizontal bar)
- Each chart in glassmorphic card

All components:
- Handle loading with skeletons
- Handle empty state gracefully
  </action>
  <verify>Components render without TypeScript errors</verify>
  <done>Performance components display summary, posts, and patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create AI insights component</name>
  <files>components/performance/AIInsightsPanel.tsx</files>
  <action>
Create component to display AI-generated insights:

```typescript
interface AIInsightsPanelProps {
  insights: PerformanceInsight[];
  isLoading?: boolean;
}
```

**Layout:**
- Header: "AI Insights" with sparkle icon
- List of insight cards, each showing:
  - Impact badge (high/medium/low with colors)
  - Type icon (topic/format/timing/hashtag/length)
  - Title (bold)
  - Description
  - Recommendation (highlighted)
  - Evidence bullets (collapsible)

**Styling:**
- High impact: cyan border/glow
- Medium impact: violet border
- Low impact: gray border
- Icons per type:
  - topic: Lightbulb
  - format: FileText
  - timing: Clock
  - hashtag: Hash
  - length: AlignLeft

**States:**
- Loading: Pulsing skeleton cards
- Empty: "Not enough data for AI insights"
- Error: Retry button
  </action>
  <verify>Insights render with proper styling</verify>
  <done>AI insights panel displays actionable recommendations</done>
</task>

<task type="auto">
  <name>Task 3: Create content performance dashboard page</name>
  <files>app/dashboard/content/performance/page.tsx, app/dashboard/layout.tsx, components/CommandPalette.tsx</files>
  <action>
**page.tsx - Content Performance Dashboard:**

Layout:
1. **PageHeader:** "Content Performance" with platform filter, period selector
2. **Stats row:** PerformanceOverview component
3. **AI Insights:** AIInsightsPanel (full width)
4. **Patterns section:** PatternCharts grid
5. **Posts section:** TopPostsGrid (two columns)

Use useContentPerformance hook for data.
Handle loading with skeletons, error with retry.

**layout.tsx update:**
Add to sidebarItems after "Optimizer":
```typescript
{ icon: BarChart3, label: 'Performance', href: '/dashboard/content/performance' },
```

**CommandPalette.tsx update:**
Add command:
```typescript
{
  id: 'content-performance',
  title: 'Content Performance',
  description: 'AI analysis of what content works',
  icon: BarChart3,
  action: () => router.push('/dashboard/content/performance'),
  category: 'navigation',
  keywords: ['performance', 'analytics', 'ai', 'insights', 'what works', 'patterns', 'engagement']
}
```
  </action>
  <verify>Navigate to /dashboard/content/performance, see loading then data</verify>
  <done>Performance dashboard accessible via sidebar and command palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` passes
- [ ] Performance overview displays correctly
- [ ] AI insights render with proper styling
- [ ] Pattern charts visualize data
- [ ] Dashboard page loads with all sections
- [ ] Navigation integrated (sidebar + command palette)
</verification>

<success_criteria>
- All tasks completed
- Charts visualize performance patterns
- AI insights show actionable recommendations
- Dashboard is accessible and functional
- Phase 46 complete
</success_criteria>

<output>
After completion, create `.planning/phases/46-content-performance-ai/46-02-SUMMARY.md`
</output>
