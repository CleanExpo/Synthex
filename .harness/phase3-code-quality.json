```json
{
  "scores": {
    "completeness": 6,
    "correctness": 6,
    "codeQuality": 5,
    "documentation": 7
  },
  "issues": [
    {
      "severity": "high",
      "file": "tsconfig.build.json",
      "description": "All strict TypeScript checks disabled (strictNullChecks, noImplicitAny, strictFunctionTypes, etc.). This is a blanket escape hatch that masks real type errors in production builds. The tsconfig.json has strict:true but tsconfig.build.json nullifies every strict flag — build output cannot be trusted for type safety."
    },
    {
      "severity": "high",
      "file": ".env.example",
      "description": "FIELD_ENCRYPTION_KEY default is 64 zero characters ('000...000'). If a developer copies this file without changing it, all encrypted database fields (OAuth tokens, API keys) use a known-zero key. Should be commented out or use a clear CHANGEME sentinel that fails startup validation."
    },
    {
      "severity": "high",
      "file": ".env.example",
      "description": "ENCRYPTION_KEY default is also 64 zeros. Same issue as FIELD_ENCRYPTION_KEY — two separate encryption keys share the same trivially-guessable default. lib/security/env-validator.ts (referenced in the file header) should reject zero-filled keys at startup."
    },
    {
      "severity": "high",
      "file": "k8s/Dockerfile",
      "description": "Build ARG SUPABASE_SERVICE_ROLE_KEY has default 'placeholder-service-role-key-build-only' baked into the image layer. Comment states '17 service files create Supabase admin clients at module scope' — this is an architectural defect (module-scope side effects) being papered over with a Dockerfile hack. The root cause must be fixed: admin clients must be lazily initialised, not created at import time."
    },
    {
      "severity": "high",
      "file": "components/approval-workflow/index.tsx",
      "description": "getMockRequests() returns hardcoded mock data. The component reads from this function on mount (line: setRequests(getMockRequests())) with no real API call for approval workflows. The system ships with a critical business feature (content approvals) running entirely on mock data."
    },
    {
      "severity": "high",
      "file": "components/competitor-analysis/index.tsx",
      "description": "eslint-disable-line comment on line in fetchConnections useEffect suppresses the missing-dependency lint warning rather than fixing the logic. formData.platform is used inside the effect but excluded from deps — this is a stale-closure bug that will cause the platform reset logic to fire with stale values after re-renders."
    },
    {
      "severity": "medium",
      "file": "package.json",
      "description": "build script uses '--webpack' flag while dev uses '--turbopack'. Mixed bundlers across dev/prod create environment parity issues. Additionally, build:vercel pins prisma@7.5.0 explicitly via npx while the main build does not — version skew risk if the lockfile drifts."
    },
    {
      "severity": "medium",
      "file": "package.json",
      "description": "postinstall is a no-op echo. Prisma generate is deferred to build time, meaning a fresh 'npm install' leaves the Prisma client absent. Developers running 'npm install' then immediately importing from '@prisma/client' will hit runtime errors without a subsequent build step."
    },
    {
      "severity": "medium",
      "file": "deployment/Dockerfile",
      "description": "Base image is node:20-alpine but k8s/Dockerfile uses node:22-alpine. engines field in package.json requires 22.x. The deployment/Dockerfile will run on an unsupported Node version, violating the declared engine constraint and diverging from the k8s production target."
    },
    {
      "severity": "medium",
      "file": "synthex-bayesian-service/Dockerfile",
      "description": "CMD runs only 2 uvicorn workers hardcoded. No consideration for the --workers flag being inadequate for variable load, and no graceful shutdown signal handling specified. For a forecasting service receiving concurrent BO/Prophet/BayesNF requests this will bottleneck under load."
    },
    {
      "severity": "medium",
      "file": "components/ai-content-studio/index.tsx",
      "description": "fetchBusinesses catch block silently swallows all errors (comment: 'Silently fail — personal account mode'). Network failures, 500 errors, and auth failures are all treated identically — user gets no feedback and the businesses list remains empty with no indication of why."
    },
    {
      "severity": "medium",
      "file": "components/command-palette/index.tsx",
      "description": "buildCommands(router.push) is called inside useMemo with router as dependency but router.push is extracted outside the memo — if router identity changes (Next.js App Router can recreate the router object), the memo won't invalidate correctly. Should use router directly or useCallback-wrap the push function."
    },
    {
      "severity": "medium",
      "file": "synthex-bayesian-service/app/main.py",
      "description": "CORS allow_origins includes 'https://*.synthex.app' and 'https://*.vercel.app' wildcards. FastAPI's CORSMiddleware does not support wildcard subdomains in origin matching — these entries are silently ignored, meaning subdomains are NOT actually allowed. The wildcard syntax only works with allow_origins=['*']."
    },
    {
      "severity": "medium",
      "file": "components/approval-workflow/index.tsx",
      "description": "approveRequest and rejectRequest mutate req object properties directly inside prev.map() (e.g. req.approvers[approverIndex].status = 'approved'). This is direct state mutation — React state must be treated as immutable. The spread/copy is missing, causing potential stale render bugs."
    },
    {
      "severity": "medium",
      "file": "tsconfig.json",
      "description": "exclude list is 40+ entries including specific legacy paths (src/server.ts, src/routes/**, src/middleware/**, src/config/security.config.ts, src/services/performance.ts). These excluded paths suggest dead code that was never cleaned up post-migration (confirmed by .claude/archived/2026-03-22/legacy-src/README.md). The migration map and tsconfig exclusions should be reconciled — either delete the dead paths or document why they persist."
    },
    {
      "severity": "medium",
      "file": ".claude/archived/2026-03-22/legacy-src/agents/env-management/index.ts",
      "description": "EnvManagement.generateTemplate() calls orchestrator.getAgent('discovery') as any and then calls discovery.generateEnvTemplate(). The as any cast bypasses type safety on a method call that may not exist on the returned agent type. The archived code reflects a broader pattern of as any casts used to work around missing interface definitions."
    },
    {
      "severity": "low",
      "file": "package.json",
      "description": "109 npm scripts defined. Many are duplicates with platform variants (ralph, ralph:windows, ralph:windows:init; setup, setup:windows; verify, verify:windows). No discoverability mechanism — developers must grep to find relevant scripts. Consider a script registry or reducing to a core set with a platform-detection wrapper."
    },
    {
      "severity": "low",
      "file": "components/icons/index.ts",
      "description": "Multiple icons are mapped to the same Heroicons source (e.g., ChartBarIcon exports as Activity, BarChart, BarChart2, BarChart3; SparklesIcon exports as Sparkles, Wand2, Diamond; CommandLineIcon exports as Command, CommandLine, Keyboard). Semantic meaning is lost — a Keyboard icon and a Command icon should not be the same glyph."
    },
    {
      "severity": "low",
      "file": "board-cron/remotion/package.json",
      "description": "Separate package.json with its own remotion@4.0.242 dependency while root package.json has remotion@^4.0.435 and @remotion/player@^4.0.435. Version skew between the cron subpackage and root. If the root is updated, board-cron/remotion stays behind unless updated separately."
    },
    {
      "severity": "low",
      "file": "deployment/README.md",
      "description": "Quick Start section instructs 'npm run build' then 'cd public && python -m http.server 8000'. This serves the static /public directory, not the Next.js build output (.next/). The README gives incorrect local development instructions that would serve assets only, not the application."
    }
  ],
  "missingTests": [
    "Approval workflow component — zero test coverage on state mutation logic (approveRequest, rejectRequest, requestRevision). The direct-mutation bug in state would be caught by a simple render test.",
    "AI Content Studio — no tests for handleGenerate, business fetching, or connection-fetch logic. The silent-fail pattern in catch blocks makes these untestable without explicit error path tests.",
    "Competitor Analysis — the SWR data transformation pipeline (transformTrackingCompetitor, transformIntelligenceCompetitor) has no unit tests. Type assertions via 'as Record<string, unknown>' throughout with no schema validation.",
    "Command Palette — no keyboard navigation tests (ArrowDown/ArrowUp/Enter/Escape). Critical UX path with zero coverage.",
    "Bayesian service routers (app/routers/optimise, forecast, predict) — no router-level tests visible. Only pytest config in pyproject.toml with testpaths=['tests'] but no test files provided.",
    "Environment variable validation (lib/security/env-validator.ts) — referenced in .env.example as source of truth but no tests confirming zero-filled FIELD_ENCRYPTION_KEY or ENCRYPTION_KEY are rejected at startup.",
    "Auth middleware — CLAUDE.md states auth lives in lib/auth/ and is always checked first, but no auth unit/integration tests are referenced in the provided context.",
    "Stripe webhook handler — no tests for webhook signature verification or subscription lifecycle events.",
    "OAuth state parameter CSRF validation — OAUTH_STATE_SECRET is marked CRITICAL but no tests confirming state validation rejects tampered parameters.",
    "Database migration rollback — db:rollback script exists but no integration tests confirming rollback restores prior state correctly."
  ],
  "securityConcerns": [
    "FIELD_ENCRYPTION_KEY and ENCRYPTION_KEY both default to 64 zero characters in .env.example. If deployed without rotation, all AES-GCM encrypted fields (OAuth tokens, stored API keys) use a universally known key. The env validator must hard-reject all-zero keys.",
    "k8s/Dockerfile bakes SUPABASE_SERVICE_ROLE_KEY=placeholder-service-role-key-build-only as a build ARG with ENV promotion. Even placeholder values embedded in image layers appear in docker history and layer metadata. The correct fix is lazy Supabase admin client initialisation, not build-time placeholders.",
    "NEXT_PUBLIC_CRON_SECRET exists in .env.example — CRON_SECRET is a server-side secret and should never have a NEXT_PUBLIC_ variant. Exposing cron authentication to the browser bundle defeats the purpose of the secret.",
    "deployment/Dockerfile copies dist/ directory without verifying its integrity. No checksum or signature verification on the build artifact before it enters the container image.",
    "CORS wildcard subdomain entries in synthex-bayesian-service/app/main.py ('https://*.synthex.app', 'https://*.vercel.app') are silently ignored by FastAPI's CORSMiddleware. If these subdomains need access, the actual CORS policy is more restrictive than intended — a security-by-misconfiguration false sense.",
    "JWT_SECRET in .env.example has comment 'min 32 chars' but no programmatic enforcement shown. The example value 'your-base64-encoded-secret-key-at-least-32-chars==' is 47 chars but weak by entropy. No startup check enforcing minimum entropy is referenced.",
    "ADMIN_API_KEY and INTERNAL_API_KEY both have empty defaults in .env.example with no startup rejection. If deployed with empty strings, any request with an empty Authorization header passes key validation depending on implementation.",
    "LINEAR_API_KEY is stored in .env and used by .claude/task-runner.sh which runs Claude Code headlessly. If the task runner has file-system write access and the Linear key is compromised, an attacker could inject malicious Linear issues that get executed autonomously.",
    ".env.example contains actual Stripe price IDs (price_1TCO8GGib5mMf28d0W7pYdtY, etc.) committed to the repository. These are not secrets but their presence in a public repo reveals production Stripe product structure.",
    "DEMO_USER_EMAIL and DEMO_USER_PASSWORD exist as env vars for a demo account on production (synthex.social). Demo credentials in environment variables shared across deployments is a credential sprawl risk — demo accounts should use ephemeral credentials or a dedicated identity provider."
  ]
}
```