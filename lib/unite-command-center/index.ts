/**
 * Thin stub tree. The command-module implementation now lives in the shared package
 * `@unite-group/control-module` (git-tag-pinned). Prisma-backed packet helpers live
 * in `./intake/command-packet.service` and must stay server-only.
 */
export * from '@unite-group/control-module';
