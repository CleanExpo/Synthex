package-lock.json
.next/
.next2/
.next_alt/
node_modules/

# Generated by Next.js and regenerated verbatim by every `next build`. It carries
# "This file should not be edited" in its own body. lint-staged was running
# `prettier --write` over it, rewriting Next's double quotes to single quotes; the
# next build wrote them back, so a TRACKED file changed during verification and
# `pr_release_gate.py` refused with "repo changed while verification was running".
# Prettier and Next.js were fighting over one line, forever.
next-env.d.ts
