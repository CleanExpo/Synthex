From: Ship Audit Bot
Date: 2025-08-13
Subject: Add environment variable validation

--- /dev/null
+++ b/scripts/validate-env.js
@@ -0,0 +1,50 @@
+#!/usr/bin/env node
+
+/**
+ * Environment variable validation script
+ * Run before build and deployment
+ */
+
+const required = [
+  'NEXT_PUBLIC_SUPABASE_URL',
+  'NEXT_PUBLIC_SUPABASE_ANON_KEY',
+  'DATABASE_URL',
+  'JWT_SECRET',
+  'OPENROUTER_API_KEY'
+];
+
+const optional = [
+  'NEXT_PUBLIC_APP_URL',
+  'SENTRY_DSN',
+  'EMAIL_PROVIDER',
+  'SMTP_HOST'
+];
+
+console.log('Validating environment variables...\n');
+
+let hasErrors = false;
+const missing = [];
+
+for (const key of required) {
+  if (!process.env[key]) {
+    console.error(`❌ Missing required: ${key}`);
+    missing.push(key);
+    hasErrors = true;
+  } else {
+    console.log(`✅ Found: ${key}`);
+  }
+}
+
+console.log('\nOptional variables:');
+for (const key of optional) {
+  if (process.env[key]) {
+    console.log(`✅ Found: ${key}`);
+  } else {
+    console.log(`⚠️  Missing (optional): ${key}`);
+  }
+}
+
+if (hasErrors) {
+  console.error('\n❌ Environment validation failed!');
+  console.error('Missing required variables:', missing.join(', '));
+  process.exit(1);
+}
+
+console.log('\n✅ Environment validation passed!');
+
--- a/package.json
+++ b/package.json
@@ -5,7 +5,8 @@
   "private": true,
   "scripts": {
     "dev": "next dev",
-    "build": "next build",
+    "prebuild": "node scripts/validate-env.js",
+    "build": "npm run prebuild && next build",
     "vercel-build": "next build",
     "start": "next start",

Risk Level: LOW
Rollback: Remove prebuild script from package.json
Impact: Validates required environment variables before build