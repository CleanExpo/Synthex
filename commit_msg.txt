fix(133): lazy-init all module-level Supabase clients to fix build

Root cause: 27+ files created Supabase clients at module level via createClient().
During next build Collecting page data phase, env vars are not available,
causing Error: supabaseUrl is required crash on /api/analytics/anomalies.

Changes:
- Convert all module-level createClient() calls to lazy getSupabase() pattern
- Fix lib/analytics/anomaly-detector.ts (primary build crasher)
- Fix lib/ml/posting-time-predictor.ts (lazy getter in class)
- Fix lib/supabase.ts (Proxy-based lazy clients)
- Fix 9 social post routes, 5 lib modules, 9 API routes
- Add missing env vars to jest.setup.js (SERVICE_ROLE_KEY, CRON_SECRET, etc.)
- Add missing cron jobs to vercel.json (daily-post, analytics-sync)
- Create .claude/PROGRESS.md for autonomous agent tracking

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>