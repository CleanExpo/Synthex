# Session Handoff — Synthex Agent Contract v1 + main-green repair

**Status: READY** (all work merged; `main` CI-green) · **2026-07-04 07:20**

## 1. Summary

- **Attempted:** ship Synthex "SHIPIT" (agent-drivable by Margot/Pi/Phill); then repair main's red CI; then land the blocked #596.
- **Completed:** Agent Contract v1 published (#604, merged); main's 4 red CI checks repaired (#605, merged, `main` green); #596 conflict resolved + green (merged `aae05de6`). Board clean, no open PRs.
- **Partial / owner-gated:** two SHIPIT owner actions remain (Synthex-side; not code) — SYN-1067 (apply MCP keys) and SYN-1066 (image-provider secret).
- **Not touched:** Margot's Hermes runtime (owned in-flight).

## 2. Where it started

- **Requests (in order):** "pull Synthex in, get it SHIPIT-ready for Margot/Pi/Me" → handoff → PR/merge #604 → "fix main red" (ultracode) → "get main green" → "merge #605" → "resolve #596".
- **Repo:** `CleanExpo/Synthex`, prod `synthex.social`, Vercel `prj_gbQmHn6quoHgG3AswRrDoUlYaF40`.
- **Constraints:** internal-tool identity (SSOT); Margot off-limits; prod deploys = human gate; prod secret writes classifier-gated.

## 3. Decisions locked + what shipped

**Locked:** Synthex = internal tool (Stripe/launch out of scope). SHIPIT = Agent Contract v1 (MCP studio tools; publish deferred). The "control-module" local test failure is a **stale-node_modules red herring** — CI's `npm ci` resolves the git-pinned private package; verify against CI, not local full-suite.

**Shipped (merged to `main`):**

- **#604** `8c16905` — Agent Contract v1 spec + `docs/agent-contract-v1.md`.
- **#605** `fef2a17b` — repaired 4 red checks: rate-limit regression (`video/generate`), reverted SYN-1031 feature (`AgentRunDetail`), module-scope `JWT_SECRET` build throw (`signInFlow.ts`), `organization_video_quotas` RLS migration (guarded), fal-webhook route-safety baseline.
- **#596** `aae05de6` — control-module consume (conflict resolved to package-stub form).
- origin/main tip: `5f18d344` (incl. #590).

## 4. Key files (this session's changes — all merged)

| File                                                                   | Status                                  |
| ---------------------------------------------------------------------- | --------------------------------------- |
| `docs/agent-contract-v1.md`                                            | Created (#604)                          |
| `.planning/spec-agent-contract-v1-SHIPIT.md`                           | Created (#604)                          |
| `app/api/video/generate/route.ts`                                      | Modified — added `withRateLimit` (#605) |
| `components/marketing-agency/agent/AgentRunDetail.tsx`                 | Modified — restored SYN-1031 (#605)     |
| `lib/auth/signInFlow.ts`                                               | Modified — lazy `getJwtSecret()` (#605) |
| `supabase/migrations/20260704000000_rls_organization_video_quotas.sql` | Created — RLS, preview-safe (#605)      |
| `scripts/security/route-safety-baseline.json`                          | Modified — fal webhook baselined (#605) |
| `lib/unite-command-center/ontology/command-ontology.schema.ts`         | Merge-resolved to package stub (#596)   |

## 5. Running state

- **Local branch:** `main` @ `db49e8cb`, tree **clean**. Diverged from `origin/main` (`5f18d344`) by squash-merges — cosmetic; reconcile with `git fetch && git reset --hard origin/main` (classifier-gated — run manually).
- **Cache:** cleared 2.1 GB `.next/cache`.
- **origin/main CI:** GREEN [VERIFIED via `gh run view` on `fef2a17b`]: Unit Tests / Build / Type Check / Lint / RLS Checks all `success`.
- **Open PRs:** none. **Safe to stop:** yes.

## 6. Verification

- `gh run list --branch main --workflow=ci.yml --limit 1` → conclusion `success`.
- Agent Contract transport (live prod): `curl -s -X POST https://synthex.social/api/mcp/mcp -H "Authorization: Bearer <key>" -H 'Accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'` → 7 tools.
- **Local caveat:** full `npm test` / `npm run type-check` fail locally on `@unite-group/control-module` (git-pinned private pkg not installable here). Verify changed suites in isolation; trust CI. See memory `synthex-ci-devenv`.

## 7. Deferred + open questions

**Deferred (owner):**

- SYN-1066 — set an image-provider key in prod (`generate_image` blocked) · Owner · blocks image capability.
- SYN-1067 — apply staged `SYNTHEX_MCP_KEYS` (margot/pi/phill) + redeploy · Owner · blocks agent access.
- SYN-1068 (Margot Hermes wiring), SYN-1069 (Pi→MCP, phase-2).
- Fable-worthy backlog (from discovery): live RLS isolation gap (147/234 tables `using(true)`), 22-route Zod backlog, jest config duplicates, `PENDING-generative_video_engine.sql`.

**Open questions:** which image provider to fund; whether Margot/Pi need publish tools (phase-2).

## 8. Pick up here

**Start here:** `cd ~/Synthex && git fetch origin && git reset --hard origin/main` (stabilise workspace to `5f18d344`).
**Do not redo:** #604/#605/#596 (merged); the 4 CI fixes; the identity/scope decision; the control-module diagnosis (it's a local-only artifact).
**First command:** `cd ~/Synthex && git fetch origin && git log --oneline origin/main -3`

## 9. Risk notes

- A repo "land-the-plane" stop hook (`.claude/hooks/stop-verify-git.py`) auto-commits working-tree changes on turn-end — expect commits you didn't invoke.
- Local `main` is behind origin (squash-merge artifact) — reconcile before new work.
- RLS migration file created but **not applied to prod** (out-of-band owner step); its static-scanner coverage is satisfied.

## 10. Handoff quality check

No faked verification (CI conclusions cited from `gh`; local caveats stated honestly). No hidden running process (watchers all completed). Completed vs deferred explicit. Prod deploys + secret writes correctly left as owner gates.

Handoff complete. Next safe action: on resume, `git fetch origin && git reset --hard origin/main` to stabilise the local checkout at `5f18d344`.
