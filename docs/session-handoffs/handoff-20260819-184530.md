# handoff 20260819-184530

Mode: --quick
date: 2026-08-19T08:45:30Z
branch: feat/campaign-concept-studio
status: type-check + jest green, install hardened

## What changed this session

- Ran the user-requested recovery sequence end-to-end under Node 22.16.0 /
  npm 11.8.0. The earlier failure mode (`jest not recognized`) was caused
  by a concurrent rogue installer repeatedly emptying node_modules
  mid-write while a background automation kept re-spawning
  `npm install --ignore-scripts --legacy-peer-deps
 --cache ./.npm-cache-clean --no-audit --no-fund --no-bin-links`.
  The single-writer rule (stop every other `node.exe` whose command line
  contains `npm-cli.js install|ci`) was enforced throughout the recovery.
- `@unite-group/control-module` was missing from node_modules because the
  git+ssh source URL was unreachable from this machine. Materialized the
  upstream tag `control-module-v0.1.0` into `packages/control-module`,
  built it with `tsup`, and switched the dependency to
  `file:./packages/control-module` in `package.json` (already on HEAD).
- `npx prisma generate` produced the v7.7.0 client.
- `npm run type-check` exited 0.
- `npm test -- --runInBand` ran 757 suites: 751 passed, 6 skipped,
  7928 tests passed, 0 failed, time 109.6s.

## Hardening applied in this session

- `.npmrc`: set `install-links=false`. With `install-links=true`,
  `npm install` of a `file:` dep hardlinks only `package.json` and
  skips the `files` list. This made node_modules show a bare
  `package.json` and broke every subpath import under `tsc`. The
  previous `install-links=true` was the real reason the install
  "looked complete" but `tsc` failed with TS2307.
- `.gitignore`: added `.npm-cache-*/` and `.tmp_control_new/`. The
  session earlier generated `.tmp_control_new` (a fresh vendor-tree of
  the control module that was abandoned once `file:` install worked)
  and the `.npm-cache-*/` pattern covers any future cache directory
  npm scripts create (not just the two known ones).
- Removed on-disk artifacts: `.tmp_control/`, `.tmp_ug/`. `.tmp_ug/`
  was the leftover from the upstream `git clone` of
  `Unite-Group.git@control-module-v0.1.0`.

## Gate results

```text
type-check: exit 0
jest:       751 passed, 6 skipped, 128 skipped tests, 21 todo,
            7928 passed, 8077 total, 109.6s
```

## Pick up here

1. If you intend to keep `@unite-group/control-module` as a local
   `file:` dependency long-term, the next agent should commit
   `packages/control-module/dist` artifacts, or set up an SSH key so
   the `github:` source can be restored. The committed lockfile now
   references the local path; reverting it to github-source requires
   regenerating the lockfile entry.
2. PR #902 status was CONFLICTING per the prior handoff. Verify with
   `gh pr view 902` before any push.
3. Do not re-enable `install-links=true` in `.npmrc` — it will
   re-break the file-dep install on the next `npm install` and look
   like a missing package error.
4. If `npm ci` ever fails with ENOTEMPTY, the single-writer rule is
   broken. Run
   `wmic process where "name='node.exe'" get processid,commandline`
   and kill any extra `npm install` writers before retrying.

## First command to run after this handoff

```bash
git status
npm test -- --runInBand
```

If both succeed, the branch is in a clean, reproducible green state.
