# Session Handoff — 2026-07-11 — Reference-grounded generation (image + video BOTH merged)

> **UPDATE (post-session, supersedes body below):** **Video grounding also MERGED** to `main` via
> **PR #709** (`main` tip `79a42677a`) — so **both** image (#705) and video (#709) grounding are now
> live in `main`. The body below was written when video was still on a branch; treat it as history.
> **Reality now:**
>
> - Image + video reference-grounding: **MERGED, live in main.** No open PRs, no pending branches.
> - The `claude/video-grounding-spec` / `claude/video-grounding-v2` branches are **obsolete** (v2 deleted;
>   #709 already carried the video work into main).
> - **Merge-race note:** #709 (stacked on the pre-squash image branch) merged while a clean v2 branch was
>   being prepared. Core work is intact (image gen, video gen, #710 first-party carpet photos, #712 tenancy,
>   `VideoGateVerdict` model all present). Collateral: the #709 merge prettier-**reformatted** 3 unrelated
>   files (`scripts/generate-course-media.mjs`, `docs/course-media-bridge.md`,
>   `docs/specs/spm-track-b-client-provisioning.md`) — **cosmetic only, no logic changed**; the spm-track-b
>   doc has mildly mangled markdown underscores (left as-is by decision).
> - **Still open (the real remaining work):** LIVE verification of grounded image + video output on a
>   deployed `NEXT_PUBLIC_APP_URL` (fal fetches reference photos over the internet — unverified until deployed);
>   a verified standard-tier Seedance image-to-video model to enable standard-tier grounding; GPT Image 2 as a
>   2nd image model; the orchestration skills; owned water-damage-restoration photos.

**Gate status:** ✅ GREEN (type-check clean, lint 0 warnings, full suite 6170 passed / 0 failed / 578 suites).
Gate log: `.handoff-logs/handoff-20260711-handoff.log`. (`scripts/handoff-loop.sh` does not exist in this
repo — gates were run manually: `npm run type-check && npm run lint && npm test`.)

## 1. Summary of what was done

- **Image reference-grounding — COMPLETED + MERGED** (PR #705 → `main`). Real owned-photo reference
  library + resolver (owned-only guard) + image model registry + FLUX.2-pro-on-fal adapter +
  `generateImage` grounding + `generate_image` reference args + new `list_reference_sets` MCP tool.
- **Video reference-grounding — COMPLETED (built + pushed, NOT merged)** on branch
  `claude/video-grounding-spec`. Seeds image-to-video from the first owned reference photo via the
  existing fal I2V path; opt-in, owned-only, explicit-imageUrl-wins, fail-open (incl. standard-tier).
- **Also shipped in #705:** the reference library assets (24 webp), `manifest.json`, an internal
  Dri-Eaz/Phoenix manufacturer-manual index (links only, rights-clean), and both slices' spec+plan docs.
- **Not touched / deferred:** live end-to-end verification (needs a deployed public URL); a
  standard-tier image-capable video model; GPT Image 2; the orchestration skills; owned water-damage photos.

## 2. Where it started

- **Request:** "Grab the images I added to Downloads (pro carpet wand, upholstery tool, a couple of
  water-damage restoration shots), add them to the Wiki as real reference/training data, and make image
  generation work from this real data — not artificial." Later: "spec + build video grounding next", and
  "#705 has a merge error."
- **Branches:** `claude/wiki-water-damage-carpet-images-1f20a9` (image, now MERGED as #705) →
  `claude/video-grounding-spec` (video, current, pushed).
- **Constraints:** owned imagery only (no third-party/synthetic); `CLAUDE.md` gates prod/main merge to the
  founder; **this estate auto-merges PRs on green within minutes** (opening a PR ≈ merging — merge-gate skill).

## 3. Decisions locked + what shipped

**Decisions (both slices):** owned-only rights guard (machine-enforced in the resolver); grounding is
**opt-in** — `useReferences !== false && (useReferences === true || Boolean(referenceSet))`, `useReferences:false`
is a hard off; **explicit `imageUrl`/seed wins**; FLUX.2 pro (image) via fal is primary; a small image model
registry was added (Stability/DALL-E marked deprecated); no DB schema changes anywhere.

**Shipped (merged to `main` via #705):** the entire image slice + reference library + manual index + docs.
**On branch, NOT shipped:** the video slice (`claude/video-grounding-spec`, tip `a2570c875`, pushed to origin,
no PR opened). Founder chose to open PRs themselves (merge = prod deploy).

## 4. Key files (video branch `claude/video-grounding-spec` vs `main`)

| File                                                          | Status                                                                      |
| ------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `lib/services/ai/video/types.ts`                              | Modified (referenceSet/useReferences + grounded/referenceSet fields)        |
| `lib/services/ai/video/generation-service.ts`                 | Modified (resolution step, 4× seed swap, job tags, standard-tier fail-open) |
| `lib/services/ai/studio-tools/index.ts`                       | Modified (`generate_video` args + description)                              |
| `tests/unit/ai/video-grounding.test.ts`                       | Created (9 tests)                                                           |
| `tests/unit/ai/video-grounding-tool.test.ts`                  | Created (2 tests)                                                           |
| `docs/superpowers/specs/2026-07-11-video-grounding-design.md` | Created                                                                     |
| `docs/superpowers/plans/2026-07-11-video-grounding.md`        | Created                                                                     |
| _(image slice files)_                                         | Already Merged via #705 — do not re-touch                                   |

## 5. Running state

No dev server, no background process running (all session background commands completed; the last full
`npm test` exited 0 — see gate log `.handoff-logs/handoff-20260711-handoff.log`). No preview/browser open.

## 6. Verification — exact commands

```bash
# On the video branch (current):
git checkout claude/video-grounding-spec
npm run type-check          # clean (tsc --noEmit)
npm run lint                # 0 warnings (eslint . --max-warnings 0)
npm test                    # 578 suites / 6170 passed / 0 failed
npm test -- tests/unit/ai/video-grounding.test.ts tests/unit/ai/video-grounding-tool.test.ts  # 11 pass
```

- **Build NOT run locally** this handoff (`npm run build`). #705's identical CI passed the `Build`
  check; CI will run Build again on the video PR.
- **Live grounded output UNVERIFIED** for both slices — fal fetches reference `image_url`s over the
  public internet, so grounding only exercises against a deployed `NEXT_PUBLIC_APP_URL`
  (synthex.social), not local/preview-private. Unit tests mock fal (no cost).

## 7. Deferred + open questions

**Deferred (work):**

- Live verification of image + video grounding on a deploy — Owner: next agent + founder — Blocking: PR/deploy — Why: needs public `NEXT_PUBLIC_APP_URL` reachable by fal.
- Add a verified standard-tier Seedance image-to-video model to `video/registry.ts` — Owner: next agent — Blocking: no — Why: standard tier currently falls open to ungrounded (documented) instead of grounding.
- GPT Image 2 as a 2nd image registry entry; the orchestration skills; multi-image Seedance @Image tagging — Owner: next agent — Blocking: no.
- Owned water-damage-restoration photos — Owner: founder — Blocking: that vertical stays empty until supplied.

**Open questions:**

- Open the video PR now, or hold until image grounding is verified live on prod? — Owner: founder — Blocking: video merge.
- Deploy #705 and run the first live grounded-image check? — Owner: founder — Blocking: live verification.

## 8. Pick up here

**Start here:**

1. Decide on the video PR: founder opens `https://github.com/CleanExpo/Synthex/pull/new/claude/video-grounding-spec`
   (opening it auto-merges to main/prod — see Risk notes). Branch is pushed and green.
2. OR spec the live-verification step (deploy → `generate_image referenceSet:'carpet-cleaning'` → confirm the
   output resembles the real wand; then the video equivalent).

**Do not redo:** image grounding (merged in #705); the video build (complete, reviewed, green); the reference
library staging (merged).

**First command to run:**

```bash
git checkout claude/video-grounding-spec && npm test
```

## 9. Risk notes

- **Opening a PR in this estate auto-merges to main within minutes (draft does NOT hold it) → Synthex prod
  deploy.** The video branch is intentionally left as a pushed branch with no PR; opening it is the founder's
  merge decision.
- Both slices are **dark-by-default** (grounding opt-in; no existing-caller behaviour change; no schema
  change), so an auto-merge would be low-risk — but still a prod action.
- `npm run build` was not run locally this handoff (CI covers it).
- Local `origin/main` may be stale (not fetched since #705 merged) — `git fetch origin` before diffing against main.
- Live grounded output (image + video) is unverified pending a deployed public URL.
- `.superpowers/sdd/*` progress ledgers are git-ignored scratch (build history for both slices lives there).

## 10. Handoff quality check

- Tests actually run this handoff (gate log cited); no unrun claims. ✅
- Shipped vs on-branch clearly separated (image merged; video pushed-not-merged). ✅
- No process claimed running. ✅
- First command provided. ✅
- Build-not-run and live-unverified stated honestly. ✅

Handoff complete. Next safe action: `git checkout claude/video-grounding-spec && npm test` to re-confirm green, then the founder opens the video PR (or we spec live verification) — nothing merges or deploys without an explicit founder action.
