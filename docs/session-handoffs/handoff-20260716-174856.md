# Session Handoff — 2026-07-16 17:48:56 AEST

## 1. Summary — State: **SHIPPED** (Definition-of-Done: PASS)

Two pieces of work completed and closed this session:

- **RA-7038** — root-caused and disarmed the estate's phantom auto-committer (a global Claude Code `Stop` → `autogit ship` hook). Ticket closed Done; stray `origin/full-green` branch deleted.
- **UNI-2368** (DR AI-Website pilot) — re-checked Rana's background work (no regression) and shipped the pilot findings as **Synthex PR #755**, which is now **merged into `main`**. UNI-2368 is marked **Done** in Linear.

Tree is clean on `main`; the shipped code is verified green locally (12 site-generator suites / 71 tests). No unfinished session work. Remaining items are all founder-gated deploy steps (Deferred, §7), not incomplete work.

## 2. Where it started

- **Request:** "yeah crack on with UNI-2368 - Rana has been doing some work in the background. Re-check this for me" (preceded by RA-7038 pickup from "pull the last tasks and lets crack on").
- **Branch worked:** `feat/site-generator-trust-block` (now squash-merged; local copy stale).
- **Repos:** `~/Synthex` (pilot code); `~/.claude/settings.json` (RA-7038 hook fix).
- **Constraint:** [[carsi-designations-not-iicrc]] — certifications had to be a generic factual `BusinessProfile` field, never course/designation branding. Agent self-merge of PRs is blocked (founder gate).

## 3. Decisions locked + what shipped

**Shipped (merged to `main`):**

- **Synthex PR #755** — "feat(site-generator): DR pilot findings — trust block, call+enquiry CTA, insurance-first FAQs". Squash commit `2369aedd`, merged 2026-07-13 20:55Z, confirmed ancestor of local main `ae1727bd`. Covers UNI-2368 findings **#1 (generator-side intake/call CTA surface: `data-intake` + `href="#enquire"`), #2 (trust block + `certifications` field + schema.org `hasCredential`), #4 (insurance-first restoration FAQs)**.
- Related generator hardening also merged under UNI-2368: **#756** (service-area businesses, no storefront locality) and **#758** (entity-noun copy + declared-national coverage).

**RA-7038 (config, not a PR):** removed three `autogit` hooks from `~/.claude/settings.json` (`Stop`→`autogit ship`, and two `busy` hooks on `UserPromptSubmit`/`PostToolUse`). Backup at `/private/tmp/claude-501/-Users-phillmcgurk/9651b98a-a346-4064-9b83-f5a71c8f3137/scratchpad/settings.json.bak-ra7038`. Memory `hermes-agent-autogit-hazard.md` updated with the mechanism + re-arm risk (`mesh/bootstrap.sh` re-installs autogit on fresh machines).

**Decision locked:** `certifications` is factual business data (like reviews), validated through the same gate as page copy — a cert smuggling an ACL §18 superlative is blocked.

## 4. Key files

| File                                                                              | Status                                        |
| --------------------------------------------------------------------------------- | --------------------------------------------- |
| `lib/site-generator/types.ts` (`certifications?` on BusinessProfile)              | Modified — merged in main                     |
| `lib/site-generator/validators.ts` (`trustSignals` gated)                         | Modified — merged in main                     |
| `lib/site-generator/builder.ts` (renderTrust, CTA surface)                        | Modified — merged in main                     |
| `lib/site-generator/jsonld.ts` (`hasCredential`)                                  | Modified — merged in main                     |
| `lib/site-generator/industry-starters.ts` (insurance-first FAQs)                  | Modified — merged in main                     |
| `tests/unit/site-generator-trust.test.ts` + restoration-faqs + entity-noun suites | Created — merged in main                      |
| `~/.claude/settings.json`                                                         | Modified (RA-7038 hook removal); backup saved |

## 5. Running state

- **Branch / tree:** on `main`, tip `ae1727bd`, working tree clean, no stashes.
- **Stale artifact:** local branch `feat/site-generator-trust-block` (tip `d37cfbf`) and its remote `origin/feat/site-generator-trust-block` are pre-squash copies of the merged #755 — safe to delete (see §7).
- **Server/process:** none started this session.
- **Open PRs from this session:** none — #755 merged.
- **Blockers:** none for shipped work. Live capstone is founder-gated (§7).
- **Safe to stop:** yes.

## 6. Verification

- **Gate run:** no `scripts/handoff-loop.sh` in this repo; ran the targeted suite instead.
- **Command:** `npx jest tests/unit/site-generator` → **12 suites / 71 tests passed** (2026-07-16 this session). Includes trust-block, insurance-first FAQ, and entity-noun suites.
- **Merge verified:** `git merge-base --is-ancestor 2369aedd HEAD` → true; `gh pr view 755` → state MERGED. (CI gated the merge on 2026-07-13.)
- Note: local `tsc` flags unrelated `app/api/media/*` `imageGeneration` errors — stale local Prisma client, not a regression; CI runs `prisma generate`.

## 7. Deferred + open questions

**Founder-gated (owner: Phill) — UNI-2368 slice-2 / live capstone:**

1. **GBP OAuth connection for DR's org** (browser action) — then run `generateSiteFromGbp` for DR's real location. Blocking: live deploy.
2. **DR live phone number** (finding #3 data) — call-primary pattern needs a real number. Blocking: call CTA on live site.
3. **SendGrid sender domain** (drip) + **deploy sign-off** (human gate per ai-website skill). Blocking: go-live.
4. **Cross-repo CTA binding** — wire the generated `data-intake`/`#enquire` surface to the merged lead-capture lanes in **Unite-Group #807**. Owner: next builder session (Unite-Group repo). Blocking: functional intake on deployed site.

**Local cleanup (owner: next session, non-urgent):** 5. Delete stale merged branch: `git branch -D feat/site-generator-trust-block` (local) and `git push origin --delete feat/site-generator-trust-block` (remote). Safe — squash-merged as `2369aedd`.

**Credential rotation — founder-gated, must NOT be done by an agent:** UNI-2365 (Urgent, estate-wide secret-scanning alerts), UNI-2360 (ATO), RA-2989.

## 8. Pick up here

- **Start here:** the DR pilot generator is complete and merged. The next unit of value is **slice 2 (live capstone)**, which is founder-gated — do not start it without the founder completing the GBP OAuth + DR phone + deploy sign-off in §7.
- **Do not redo:** findings #1/#2/#4 (shipped in #755); the generator hardening (#756/#758); the RA-7038 hook fix.
- **First command to run** (safe cleanup only): `git branch -D feat/site-generator-trust-block`
- If continuing buildable scope instead, the only agent-ownable remaining item is §7.4 (Unite-Group #807 CTA binding) — a cross-repo task in `~/Unite-Group`, needs a fresh instruction.

## 9. Risk notes

- **Re-arm risk (RA-7038):** `mesh/bootstrap.sh` still `npm i -g autogit` + rewires the Stop hook on fresh machines — the fix is local to this machine's `settings.json` and does not propagate (hooks/settings are gitignored per [[enforcement-wiring-contract]]). Restore of the deleted branch if ever needed: `git push origin cdf1a046:refs/heads/full-green`.
- The `feat/site-generator-trust-block` local tip `d37cfbf` ≠ the squash `2369aedd` — this is the expected [[git-cherry-overreports-after-squash]] pattern, not divergence; content is fully in main.
- `~/.claude` has concurrent agents on 3 machines ([[claude-dir-concurrent-agent]]); re-check `settings.json` before assuming the hook stays disarmed.

## 10. Handoff quality check

- No faked verification — jest output and `gh pr view` cited from this session's tool results.
- Nothing left running.
- Completed (shipped #755, RA-7038) vs deferred (founder-gated slice 2, cross-repo #807, branch cleanup) is explicit.

Handoff complete. Next safe action: delete the stale `feat/site-generator-trust-block` branch; all further UNI-2368 progress is founder-gated (GBP OAuth + DR phone + deploy sign-off).
