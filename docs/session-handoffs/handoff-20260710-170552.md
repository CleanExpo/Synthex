# Session Handoff — nexus-viral pipeline: cards shipped, live 1→8 blocked on MCP key

`ts: 20260710-170552` · branch `claude/viral-method-cards-2d611b` @ `e9a74d75` · gate log `.handoff-logs/handoff-20260710-170552.log` (GREEN)

## ⏩ UPDATE — resumed 2026-07-10 (WS0 CLOSED · live 1→8 SHIPPED · new drift found)

_Supersedes the WS0 "blocked" status below._

**WS0 root cause was WRONG in the original handoff.** Not an "empty table" — the
`mcp_api_keys` table **did not exist** in prod (browser mint → HTTP 500
`The table public.mcp_api_keys does not exist`). Migration
`prisma/migrations/20260710120000_mcp_api_keys` (additive, idempotent) was never
applied; the **SYN-1085 founder-apply gate was still open**. Phill applied the DDL
via Supabase SQL Editor on prod project `znyjoyjsvjotlzjppzal`. Verified:
owner-session mint → HTTP 201; prod MCP `tools/list` → HTTP 200 + 16 tools.
(The `vercel env pull` path is a dead end: prod `ADMIN_API_KEY` is a Vercel
**Sensitive** var — pulls back length 0. The connected Supabase MCP is bound to the
**wrong project** `pwwwhoaxxtkmowifpuwf`, not prod `znyjoyjsvjotlzjppzal`.)

**Live 1→8 SHIPPED — real spend $1.6933.**

- Hero: `generate_video` (methodCard `stat-punch`, `viral-hero-short` grammar,
  draft Seedance 2.0 Fast, 7s, 9:16). 1st attempt failed on a **transient provider
  504** (`actualCostUsd: null` = not charged); retry rendered. Hero video_asset
  `cmrepa9qg000004lb7dalcsiz`; quota `spentTodayUsd 1.6933 / $5`.
- `derive_cuts` → **8 platform cuts** (youtube, instagram, tiktok, facebook,
  instagram_feed, linkedin, x, pinterest), YouTube-first. Every cut
  `subjectLost:false` + `publishState:queued_human_gated` — **double gate holds,
  nothing published.** Crop files render on the `*/30` cron (service-role).
- Session MCP key minted → used → **REVOKED** (0 active remaining). One earlier key
  leaked into the transcript via a parser bug and was revoked immediately.

**⚠ NEW INCIDENT — broader prod migration drift (needs Phill).** `mcp_api_keys` was
not the only unapplied migration. `approvals_list_pending` fails in prod:
`marketing_agency_claims.approval_status` column missing → migration
`20260710100000_add_claim_approval_status` also unapplied. `search_media_library`
also errors. **Recent Prisma migrations (≥ 2026-07-10) are not being applied by
migrate-on-deploy.** Still-pending in the repo, not in prod:
`20260710100000_add_claim_approval_status`, `20260710130000_syn_mcp_006_evidence_core`,
`PENDING-generative_video_engine.sql`. Answer to §7 open-question #2: **incident, not
intended state.** Action: apply the remaining migrations (same SQL-paste method) AND
diagnose why migrate-on-deploy skipped them.

---

## 1. Summary of what was done

- **Completed & merged (6 PRs):** viral method cards into the Synthex card composer (Synthex #657, SYN-1074); nexus-viral skill package + canonical youtube/instagram ids + money-maker doctrine (skills-library #3/#4/#5); `deriveSocialCut` adapter (Synthex #667); social-cut render pipeline + cron (Synthex #668, SYN-1075); `derive_cuts` MCP tool (Synthex #670); **fal model registry repair** — replaced retired Wan/Hailuo with verified-live Seedance/Kling/Veo (Synthex #676).
- **Completed & verified (not shipped as product):** full sandbox 1→8 E2E in Docker — 4 gated stages, Broadcast Grill 8/8 twice (round 1 caught real caption mojibake → remediated), YouTube search package generated. Artifacts in scratchpad (ephemeral).
- **Completed this turn:** /spm T3 spec for productionising the pipeline (8-seat bench + adversarial verify pass; caught a critical ungated-auto-publish flaw in the draft). Persisted to `docs/session-handoffs/2026-07-10-spm-nexus-viral-productionise.md`.
- **Partial / blocked:** live production 1→8 rerun. Hero submitted once via MCP ($0.35 quota held then released when the retired Wan model 404'd — proved the quota path). Rerun blocked on **WS0**: the SYN-1080 MCP auth cutover renamed the env key var and left `mcp_api_keys` empty, so prod returns 401 to all MCP callers. User authorised minting a session key; awaiting the raw key (browser-console mint snippet given).
- **Not touched:** WS1–WS4 build (orchestrator, canary, gate modules, release UI) — spec'd only, not built.

## 2. Where it started

Request: "@Artlist.zip" + the pasted viral-method-cards.json — land the nexus-viral viral method cards into Synthex. Expanded across the session to: land the full skill package, run a real sandbox 1→8, wire the render pipeline, run a live 1→8, then /spm-spec the productionisation. Constraints: draft-first spend; human-gated publish always; org-scoped; npm not pnpm; Supabase-only auth; additive migrations only; AU English.

## 3. Decisions locked + what shipped

- **Shipped:** the 6 PRs above are merged to `main` (verified via `gh pr view --json state`). main's post-merge workflows (incl. Deploy) ran green after each.
- **Decisions locked:** canonical Synthex platform ids (youtube/instagram, not reels/shorts) with YouTube-first money-maker doctrine; render pipeline is cron-driven, publish stays `queued_human_gated` (double human gate); model registry is data verified against live fal pages, not memory; /spm build order **WS0→WS2→WS3→WS1→WS4**, live-spend steps individually human-acked.
- This branch (`e9a74d75`) is itself already merged (#657); its own work needs no further action.

## 4. Key files

| File                                                                   | Status                                                                        |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `docs/session-handoffs/2026-07-10-spm-nexus-viral-productionise.md`    | Created — the build-ready spec (19 sections)                                  |
| `lib/services/ai/video/cards/viral-method-cards.{json,ts,schema.json}` | Merged (#657) — read-only now                                                 |
| `lib/video/social-derivation.ts` (`deriveSocialCut`)                   | Merged (#667) on main — NOT on this stale branch                              |
| `lib/video/social-cut-renderer.ts` + `app/api/cron/social-cut-render/` | Merged (#668) on main — NOT on this branch                                    |
| `lib/services/ai/video/registry.ts`                                    | Merged (#676) on main — this branch still holds the RETIRED models (stale)    |
| `app/api/mcp/auth.ts` + `app/api/admin/mcp-keys/route.ts`              | Merged (SYN-1080) on main — WS0 root cause + mint route                       |
| scratchpad `viral-e2e/`, `.mcp-run-key`, `out/live-*.json`             | EPHEMERAL — lost on session clear; spec + this handoff are the durable record |

## 5. Running state

- **No processes owned by this session are running** that survive a clear. Background monitors (PR CI watch `b1hdaf0yc`, deploy-gate polls) are session-scoped and end on clear.
- Gate log `.handoff-logs/handoff-20260710-170552.log`: type-check OK, lint OK, video-engine tests OK (all on this stale branch — main is further ahead and also green per merged-PR CI).

## 6. Verification — exact commands

```bash
# tree gate (this branch)
npm run type-check && npm run lint && npx jest --config jest.worktree.cjs __tests__/video-engine
# confirm merges on main
gh pr view 657 --json state ; gh pr view 667 --json state ; gh pr view 668 --json state ; gh pr view 670 --json state ; gh pr view 676 --json state
# WS0 blocker repro (prod returns 401 to legacy MCP callers)
curl --ssl-no-revoke -X POST https://synthex.social/api/mcp/mcp -H "Authorization: Bearer <old-key>" -H "Accept: application/json, text/event-stream" -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'
```

## 7. Deferred + open questions

**Deferred (owner / blocking / why):**

- Live 1→8 rerun — owner: next session — blocking: yes — why: needs the minted MCP session key (WS0).
- WS1–WS4 build — owner: `/goal` from the spec — blocking: no — why: spec'd, not started; WS0 is a prerequisite for WS1.
- Revoke the session MCP key after the run — owner: whoever runs it — blocking: no.

**Open questions (need Phill):**

- WS0 credential path: mint via browser console (snippet in-session) vs paste `ADMIN_API_KEY` vs authorise `vercel env pull`. User chose "mint" but the raw key hasn't been supplied yet.
- SYN-1080 cutover left every MCP caller locked out — is that a known/intended prod state or an incident to file?

## 8. Pick up here

**Start here:**

1. Get a production MCP key: in a browser logged into synthex.social as owner, Console → `fetch('/api/admin/mcp-keys',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({organizationId:'cmm2kwnt70000js04d6t2kpbm',userId:'86edbf72-f019-44eb-89e1-c567062c4d93',label:'nexus-viral session key',scopes:['*']})}).then(r=>r.json()).then(d=>console.log(d.rawKey||d))`
2. With the rawKey, run the staged live 1→8: `generate_video` (grilled prompt, viral-hero-short, draft — now routes to Seedance ~$1.69/7s) → poll `get_job` to rendered → `derive_cuts` (8 canonical specs) → let the `*/30` render cron produce files → verify 8 `video_assets` rows `ready` + `publish_queue` rows `queued_human_gated`.
3. Then either release-flow work (WS4) or run `/goal` on the spec in order WS0→WS2→WS3→WS1→WS4.

**Do not redo:** the 6 merged PRs; the sandbox E2E (passed); the registry repair (models are verified-live on main); the /spm spec (done, adversarially verified).

**First command to run:** `gh pr list --state open` (clear any new PRs from sibling sessions first — standing directive is watch-and-merge-on-green), then the browser mint in step 1.

## 9. Risk notes

- This branch is ~29 commits behind `main` and holds STALE code (retired models in registry.ts, no deriveSocialCut/render pipeline). All real work lives on `main` — resume against `origin/main`, not this working tree.
- Scratchpad artifacts (viral-e2e harness, grilled CopyBundle, live-run payloads, `.mcp-run-key` if minted) are EPHEMERAL and lost on clear. The spec + this handoff are the durable record; the grilled copy/search-package will need regenerating if not captured.
- Machine's curl needs `--ssl-no-revoke` (schannel can't do revocation checks here) — without it, all HTTPS to synthex.social fails with exit 35.
- `ADMIN_API_KEY` in local `.env.local` does NOT match production — do not rely on it for admin routes.
- Standing directive: watch ALL open PRs (own + sibling sessions), fix red checks, merge on 100% green, `--admin` squash (main is strict:true, zero required checks). Sibling sessions push frequently.

## 10. Handoff quality check

- Tests claimed passed = actually run (log cited). Merges claimed = verified via `gh`. No process claimed running post-clear. Completed vs deferred separated. First command provided. WS0 blocker + repro documented. Ephemeral-vs-durable boundary stated.

Handoff complete. Next safe action: in the next session, run `gh pr list --state open` to clear the PR queue, then obtain the MCP session key (browser mint snippet in §8) to unblock the live 1→8 rerun.
