# Session Handoff — Synthex Agent Contract v1 (SHIPIT)

**Status: READY** (docs-only diff; no code gates affected) · **2026-07-04 04:16**

## 1. Summary

- **Attempted:** make internal Synthex "SHIPIT ready" to be driven by Margot, Pi, and Phill.
- **Completed:** identity/scope reframe; canonical checkout moved onto `main`; MCP transport verified live in prod; Agent Contract v1 spec + doc written, committed, pushed; Linear board reconciled (epic + 4 children + 2 reclassifications + status update); a real prod defect found and filed.
- **Partial:** capability proof ran and **failed** (image generation blocked in prod) — filed, not fixed. Per-caller key proofs deferred (blocked on owner secret write).
- **Not touched:** any Margot/Hermes file (owned in-flight); any product code (this branch is docs-only).

## 2. Where it started

- **Request:** "Pull the Synthex project in… get it SHIPIT ready… internal app for Unite-Group, ready to work with Margot, Pi, and Me." Via /spm /judge /loop.
- **Branch:** `feat/agent-contract-v1` (off `main` @154ba7e4). Repo `CleanExpo/Synthex`, prod `synthex.social`, Vercel proj `prj_gbQmHn6quoHgG3AswRrDoUlYaF40`.
- **Constraints:** internal-tool identity (`main:CLAUDE.md` SSOT); Margot runtime off-limits; prod secret writes are classifier-gated.

## 3. Decisions locked + what shipped

**Locked:**

- Synthex is an **internal tool** → Stripe/OAuth/public-launch are **out of ship scope** (SSOT + founder framing). SYN-52 / SYN-1054 reclassified.
- SHIPIT = **Agent Contract v1**: publish the MCP contract, prove transport, prove Phill end-to-end, keys for margot/pi/phill; publish tools deferred to gated phase-2.
- Pi is modelled on its **real substrate** (file-symlink/Vision Board bridge), not MCP; Pi-via-MCP = phase-2.

**Shipped (committed + pushed on `feat/agent-contract-v1`):**

- `.planning/spec-agent-contract-v1-SHIPIT.md`, `docs/agent-contract-v1.md`. Commit `8d947863`.
- Linear: epic SYN-1065 + children SYN-1066/1067/1068/1069; comments on SYN-52/SYN-1054; project status update (onTrack).
- **Not shipped:** the two prod secret writes (owner-gated).

## 4. Key files

| File                                               | Status                                                          |
| -------------------------------------------------- | --------------------------------------------------------------- |
| `.planning/spec-agent-contract-v1-SHIPIT.md`       | Created (committed)                                             |
| `docs/agent-contract-v1.md`                        | Created (committed)                                             |
| `docs/session-handoffs/handoff-20260704-041626.md` | Created (this)                                                  |
| `app/api/mcp/[transport]/route.ts`                 | Read-only inspected (live, unchanged)                           |
| `app/api/mcp/auth.ts`                              | Read-only inspected                                             |
| `lib/services/ai/studio-tools/index.ts`            | Read-only inspected (7 tools)                                   |
| `lib/services/ai/image-generation.ts`              | Read-only inspected (provider fallback; root cause of SYN-1066) |

## 5. Running state

- **Branch:** `feat/agent-contract-v1`, tree **clean**, pushed (0/0 vs origin).
- **Diff:** docs-only vs `main` (2 files, +209). No server/process started this session.
- **Prod:** MCP endpoint live (verified last session). No open PR yet — branch pushed, PR not opened.
- **Cache:** cleared 4.7 GB `.next/cache`.
- **Safe to stop:** yes.
- `scripts/handoff-loop.sh` absent in this repo; code gates N/A for a docs-only diff.

## 6. Verification

- Transport (any valid key): `curl -s -X POST https://synthex.social/api/mcp/mcp -H "Authorization: Bearer <key>" -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'` → 7 tools. **Proven** with owner key.
- Capability: same with `method:tools/call name:generate_image` → **currently fails** (`All image generation providers failed`) until SYN-1066 fixed.
- Full recipe in `docs/agent-contract-v1.md` §8.

## 7. Deferred + open questions

**Deferred:**

- SYN-1067 apply staged keys + redeploy — Owner · blocks per-caller proofs · classifier-gated secret write.
- SYN-1066 set image-provider key + redeploy — Owner · blocks generate_image · missing prod secret.
- SYN-1068 Margot Hermes wiring — Margot's owner · blocked by 1067/1066 · hands-off rule.
- SYN-1069 Pi→MCP loop — phase-2 · needs Pi consumption-path decision.
- Open a PR for `feat/agent-contract-v1` (docs).

**Open questions:**

- Which image provider to fund (Stability/OpenAI/Gemini)? — Owner.
- Do Margot/Pi need publish tools (phase-2), or is generate/read enough for v1? — Owner.

## 8. Pick up here

**Start here:**

1. `cd ~/Synthex && git checkout feat/agent-contract-v1`
2. Do the two owner secret writes (SYN-1067, SYN-1066) + redeploy no-cache.
3. Regenerate margot/pi/phill keys (reuse owner org id from current `claude-code` entry in `SYNTHEX_MCP_KEYS`), re-run per-caller `tools/list` → 7, and capability proof → non-null asset. Close SYN-1065.

**Do not redo:** the spec, the contract doc, the transport verification, the Linear reconciliation, the identity/scope decision.

**First command to run:** `cd ~/Synthex && git checkout feat/agent-contract-v1 && git status`

## 9. Risk notes

- Staged key blob + prod-env backup were **shredded** from scratchpad for hygiene — keys must be regenerated on apply (trivial; ticket says how).
- `generate_video` (FAL) credentialed but **not** live-tested (cost avoidance) — assume-working, verify before relying.
- The auto-mode classifier will block the two secret writes again unless the owner runs them or adds a `vercel env` Bash permission rule.

## 10. Handoff quality check

No faked verification (transport proof pasted from real curl; capability failure reported honestly). No hidden running process. Completed vs deferred is explicit. Secret hygiene noted.

Handoff complete. Next safe action: on resume, `git checkout feat/agent-contract-v1` and perform the SYN-1067 / SYN-1066 owner secret writes.
