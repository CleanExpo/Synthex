# Session Handoff — AI-Websites program (Phases 3/4 shipped, Phase 2 mapped) + CARSI incident closed

**When:** 2026-07-10 19:05 (+10)
**Status:** READY (no local uncommitted session changes; all session code in merged/open PRs with green CI)
**Scope inferred from:** conversation context — AI-Websites product line build (`docs/superpowers/specs/2026-07-10-ai-websites-design.md`), CARSI DO incident review, Linear project setup.

---

## 1. Summary

- **Completed:** Phase 3 generator (Synthex) fully built + merged; Phase 4 buildable scope (MCP tool + skills) shipped; CARSI DO deploy fix verified (Rana's, read-only); Linear "AI Websites" project + 8 issues created; Phase 2 reuse map produced by a 5-agent parallel scout swarm.
- **Partial:** Phase 4 remainder (embed/leads/drip/provision tools) — gated on the Phase-2 engine.
- **Not touched (deliberately):** the Phase-2 build itself — see the concurrency finding in §9 (another effort is already building it).

## 2. Where it started

- **Request chain:** "continue Phase 3 → build the LLM copy hook" → "move to phase 4" → "check Rana's DO fix" → `/goal /nexus continue through the Phases, update Linear, pull next task, parallel/swarm sub-agents".
- **Branches:** work done on short-lived `phillmcgurk/aiw-*` branches off `origin/main` (Synthex), cleaned after each PR. `~/Synthex` now clean on `main`.
- **Constraints:** no self-merge of self-authored PRs (human gate); worktree isolation for builds; validators gate all generated copy; live GBP/deploy needs credentials/sign-off.

## 3. Decisions locked + what shipped

**Shipped (Synthex — CleanExpo/Synthex):**

- Phase 3 generator, MERGED: `#679` core+GBP seam, `#681` validator-gated LLM copy hook, `#682` industry starters, `#685` GBP fetcher + `generateSiteFromGbp` end-to-end capstone. 33 unit tests.
- `#686` (OPEN, MERGEABLE) — reachable `POST /api/ai-websites/generate` route + 5 route tests.
- `#688` (OPEN, MERGEABLE) — `generate_site_from_gbp` MCP studio tool + shared `generateSiteForOrg` service + 6 tests.

**Shipped (skills-library — CleanExpo/skills-library):**

- `#6` (OPEN) — `ai-website` entry-point orchestrator + `ai-website-generate` sub-skill; 2-place registered (index.md + README).

**Decisions locked:**

- CARSI: never seed courses on deploy (Rana's fix `aebb4392` removes the PRE_DEPLOY migrate/seed job; migrate-on-container-start). My superseded PR #550 CLOSED.
- Phase-2 MVP is single-tenant (`FOUNDER_USER_ID`), not multi-tenant site-key yet.
- New deps (Vercel AI SDK + embedding provider) are pre-authorised by the judge-passed Phase-1 plan.

## 4. Key files

| File                                                                            | Repo           | Status                                |
| ------------------------------------------------------------------------------- | -------------- | ------------------------------------- |
| `lib/site-generator/*` (11 files)                                               | Synthex        | Merged (main)                         |
| `app/api/ai-websites/generate/route.ts`                                         | Synthex        | Open PR #686                          |
| `lib/ai-websites/generate-for-org.ts` + `lib/services/ai/studio-tools/index.ts` | Synthex        | Open PR #688                          |
| `~/.claude/skills/ai-website{,-generate}/SKILL.md` + index.md + README.md       | skills-library | Open PR #6                            |
| `scripts/do-migrate.sh`, `app.yaml`, Dockerfiles                                | CARSI          | Read-only inspected (Rana's aebb4392) |
| `apps/web/supabase/migrations/*` (crm_leads, drip, pgvector)                    | Unite-Group    | Read-only scouted                     |

## 5. Running state

- **Branch/tree:** `~/Synthex` clean on `main`. `~/.claude/skills` on branch `aiw-phase4-ai-website-skills` (PR #6) — but see §9 for pre-existing unrelated uncommitted changes there.
- **No server/process running.**
- **Open PRs:** Synthex #686, #688 (both MERGEABLE, CI green); skills-library #6. All await human merge (self-authored, classifier-blocked).
- **Safe to stop:** YES — nothing half-written locally; all work is in PRs or Linear.

## 6. Verification

- Synthex generator + MCP + route tests, run this session: `cd ~/Synthex && npx jest tests/unit/site-generator tests/unit/api/ai-websites-generate-route.test.ts tests/unit/ai-websites/generate-for-org.test.ts` → **33 + 5 + 6 = 44 passed**. `tsc --noEmit` clean in the changed modules.
- CARSI live: `curl -s https://carsi.com.au/api/health` → **HTTP 200 healthy, production**.
- No `scripts/handoff-loop.sh` in Synthex; tree clean so no local gate to run — CI on the PRs is the gate (all green).

## 7. Deferred + open questions

- **[Owner: next session] RECONCILE with the concurrent Phase-2 effort** (see §9) — Blocking: all Phase-2 lane issues. Why: PRs #747/#748 + the "AI Front Desk" chat core already exist in Unite-Group; my Linear lanes (UNI-2355–2359) may duplicate them.
- **[Owner: Phill/Board] Public-surface + rate-limit design on the flagship** — Blocking: UNI-2355 lead-capture. Why: apps/web has no rate-limit lib and a founder private-access gate; a public write needs a deliberate gate-exemption.
- **[Owner: Phill/ops] Connect a GBP OAuth account** — Blocking: live end-to-end run of the Phase-3 route/tool. Why: verification needs a real connection.
- **[Owner: Phill] Merge PRs #686, #688 (Synthex), #6 (skills-library).**

## 8. Pick up here

**Start here:**

1. Inspect the concurrent Phase-2 work FIRST: `cd ~/Unite-Group && git log origin/main --oneline -15 | grep -i ai-website` and review merged PRs #747, #748, plus the `ai-websites/phase2-wiring` branch + `frontdesk/` chat core. Determine what the "Phase 2 engine core — tested foundation (#748)" already covers.
2. Reconcile the Linear lanes (UNI-2355 capture, 2356 drip, 2357 voice, 2358 RAG, 2359 chat) against #748 + the frontdesk core — close/merge duplicates, keep only genuine gaps.
3. Then build the first genuine gap, worktree-isolated per the shared-checkout rule.

**Do not redo:** the Phase-3 generator (merged), the MCP tool/skills (PRs open), the CARSI fix (Rana's, live), or re-scout the reuse map (it's in the UNI-2354 sub-issues).

**First command to run:** `cd ~/Unite-Group && git log origin/main --oneline -20`

## 9. Risk notes

- **CONCURRENCY (highest):** a separate effort is building Phase 2 in `~/Unite-Group` — worktree `.../scratchpad/aiw-phase2b` on branch `ai-websites/phase2-wiring` (3 ahead / 1 behind main), and merged PRs #747 (Phase 1 spec), #748 (Phase 2 engine core), #750 (AI Front Desk scaffold). My 5-agent scout scanned `main` (behind that branch), so my "greenfield chat" finding is true of main but a chat core already exists on the branch. **Do not build Phase-2 lanes without reconciling first.** I did NOT touch that worktree (it has an uncommitted `.gitignore` edit — another agent's).
- **skills-library uncommitted (not mine):** `~/.claude/skills` has pre-existing uncommitted deletions/mods (autonomy-ladder, context-cockpit, eeat, heygen-director, specialist-council SKILL.md; connections.md; pm-core; production-gate; qa-lead) — an in-flight flat→`skills/` restructure by another session. My PR #6 sits on top and may need reconciliation. I did NOT touch these.
- **~/Unite-Group + ~/CARSI dirty** = pre-existing artifacts (prior-session handoff logs/specs; CARSI unrelated), NOT from my read-only scouts.
- **Linear description typo:** the "AI Websites" project description has one stray garbled char ("�area") from an emoji — cosmetic.

## 10. Handoff quality check

- No faked verification — 44 tests + CARSI 200 are this-session tool results; PR states are grounded in `gh` output.
- No hidden "still running" — no processes; all 5 scout agents completed.
- Completed vs deferred is explicit; the concurrency risk is surfaced, not buried.

---

Handoff complete. Next safe action: reconcile the Linear Phase-2 lanes against the already-merged Unite-Group #748 "engine core" + the `ai-websites/phase2-wiring` frontdesk chat core before building anything new.
